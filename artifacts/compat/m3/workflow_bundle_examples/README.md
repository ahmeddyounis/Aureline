# Workflow Bundle Review Examples

This directory contains compatibility examples for the workspace workflow-bundle review packet.

- `workflow_bundle_review_examples.json` indexes the launch-wedge and imported-user fixtures.
- The examples preserve source class, certification, mirror/offline posture, review refs, drift refs, rollback refs, diagnostics refs, and support export refs without embedding raw user content.
