# Retained Import Reports

This directory stores export-safe retained migration report packets used by
settings, Help, and support/export surfaces. Rows keep source labels,
before/after diff refs, rollback checkpoint refs, shortcut-delta refs, and
lossy or unsupported mapping notes without raw source profile bodies.

