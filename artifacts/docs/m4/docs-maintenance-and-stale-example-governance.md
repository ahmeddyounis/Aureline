# Docs Maintenance And Stale Example Governance Artifact

The checked-in packet at `artifacts/docs/m4/docs-maintenance-and-stale-example-governance.json` certifies the stable docs-maintenance lane for the release branch.

It proves:

- render configs preserve CommonMark, extension, active-content, source/version, security, mirror/offline, and browser-handoff posture;
- suggestions are evidence-backed and diff-based;
- validation results keep rendered, syntax-checked, executed, stale, unsupported, and blocked states distinct;
- stale-example suppressions carry actor, reason, expiry, and evidence refs;
- README, changelog, onboarding, module docs, and support article classes have maintenance packets;
- Help/About, onboarding, migration guidance, release notes, docs feedback export, docs pack, CLI/headless, and support/community surfaces reuse the same packet vocabulary.

The artifact omits raw document bodies, rendered HTML, raw URLs, provider payloads, and ambient credentials. Support/community exports carry the same metadata-only packet.
