# Stable Docs Source, Result, Pack, and Citation Artifact

The checked-in packet at `artifacts/docs/stable_docs_source_result_pack_and_citation.json` is generated by:

```sh
cargo run -q -p aureline-docs --bin aureline_stable_docs_contract -- packet
```

The fixture corpus under `fixtures/docs/stable_docs_source_result_pack_and_citation/` covers:

- baseline stable cross-surface reuse;
- source/result freshness drift;
- citation exports that bundle raw pack bodies;
- pack detail sheets that hide owner/version/action state;
- citation drawers that drop inference markers;
- consumer projections that drop source precedence.
