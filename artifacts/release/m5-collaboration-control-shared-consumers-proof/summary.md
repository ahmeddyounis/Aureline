# Shared Collaboration-Control Consumers: One Vocabulary Across Surfaces

- Packet: `m5-collaboration-control-shared-consumers:stable:0001`
- Surface: `M5 collaboration-control shared consumers (one vocabulary across surfaces)`
- Consumer bindings: 18 (8 narrowed)
- Proof freshness SLO: 720 hours (last refresh: 2026-07-18T00:00:00Z)

## Consumer bindings

- **Shared terminal / debug view (a live shared terminal or debugger stream that begins view-first, names its single active driver, and shows the provenance of every input rather than letting presence imply control)** [`ccsc-sterm-view`]: object `shared_terminal_debug_view` on `shared_terminal_debug_view`, representation `desktop_full`, role `control_authority_disclosure`
- **Shared terminal / debug view (a live shared terminal or debugger stream that begins view-first, names its single active driver, and shows the provenance of every input rather than letting presence imply control)** [`ccsc-sterm-grant`]: object `shared_terminal_debug_view` on `control_grant_prompt`, representation `desktop_full`, role `control_authority_disclosure`
- **Shared terminal / debug view (a live shared terminal or debugger stream that begins view-first, names its single active driver, and shows the provenance of every input rather than letting presence imply control)** [`ccsc-sterm-support`]: object `shared_terminal_debug_view` on `support_export_packet`, representation `exported_redacted`, role `control_authority_disclosure`
- **Control grant (an explicit, time-boxed grant of terminal / debug write control that names its authority, enforces a single active driver, and shows its scope, expiry, and revoke / reclaim path)** [`ccsc-grant-prompt`]: object `control_grant` on `control_grant_prompt`, representation `desktop_full`, role `active_driver_disclosure`
- **Control grant (an explicit, time-boxed grant of terminal / debug write control that names its authority, enforces a single active driver, and shows its scope, expiry, and revoke / reclaim path)** [`ccsc-grant-view`]: object `control_grant` on `shared_terminal_debug_view`, representation `compact_narrowed`, role `active_driver_disclosure`
- **Control grant (an explicit, time-boxed grant of terminal / debug write control that names its authority, enforces a single active driver, and shows its scope, expiry, and revoke / reclaim path)** [`ccsc-grant-support`]: object `control_grant` on `support_export_packet`, representation `exported_redacted`, role `active_driver_disclosure`
- **Presenter token (the presenter / moderator token that names its holder, its handoff target, and its moderation scope, never letting two presenters drive one sensitive surface)** [`ccsc-presenter-handoff`]: object `presenter_token` on `presenter_handoff_sheet`, representation `desktop_full`, role `view_first_default_disclosure`
- **Presenter token (the presenter / moderator token that names its holder, its handoff target, and its moderation scope, never letting two presenters drive one sensitive surface)** [`ccsc-presenter-grant`]: object `presenter_token` on `control_grant_prompt`, representation `desktop_full`, role `view_first_default_disclosure`
- **Presenter token (the presenter / moderator token that names its holder, its handoff target, and its moderation scope, never letting two presenters drive one sensitive surface)** [`ccsc-presenter-support`]: object `presenter_token` on `support_export_packet`, representation `exported_redacted`, role `view_first_default_disclosure`
- **Consent envelope (the join-time consent scope that discloses recording, retention, guest scope, and route visibility consequences before a participant joins, never widening scope silently)** [`ccsc-consent-join`]: object `consent_envelope` on `collaboration_join_review_sheet`, representation `desktop_full`, role `consent_scope_disclosure`
- **Consent envelope (the join-time consent scope that discloses recording, retention, guest scope, and route visibility consequences before a participant joins, never widening scope silently)** [`ccsc-consent-retention`]: object `consent_envelope` on `collaboration_retention_sheet`, representation `remote_projected`, role `consent_scope_disclosure`
- **Consent envelope (the join-time consent scope that discloses recording, retention, guest scope, and route visibility consequences before a participant joins, never widening scope silently)** [`ccsc-consent-help`]: object `consent_envelope` on `help_docs`, representation `desktop_full`, role `consent_scope_disclosure`
- **Retention review (the recording / retention / sealed-archive review that names the recording state, retention mode and duration, and replayable-archive scope, never broadening retention silently)** [`ccsc-retention-sheet`]: object `retention_review` on `collaboration_retention_sheet`, representation `desktop_full`, role `recording_retention_state_disclosure`
- **Retention review (the recording / retention / sealed-archive review that names the recording state, retention mode and duration, and replayable-archive scope, never broadening retention silently)** [`ccsc-retention-support`]: object `retention_review` on `support_export_packet`, representation `exported_redacted`, role `recording_retention_state_disclosure`
- **Retention review (the recording / retention / sealed-archive review that names the recording state, retention mode and duration, and replayable-archive scope, never broadening retention silently)** [`ccsc-retention-help`]: object `retention_review` on `help_docs`, representation `compact_narrowed`, role `recording_retention_state_disclosure`
- **Session-restore view (the replay-free session-restore view that reattaches read-only, replays no prior input, preserves retention scope, and requires a fresh control grant before write control resumes)** [`ccsc-restore-view`]: object `session_restore_view` on `session_restore_view`, representation `desktop_full`, role `replay_free_restore_disclosure`
- **Session-restore view (the replay-free session-restore view that reattaches read-only, replays no prior input, preserves retention scope, and requires a fresh control grant before write control resumes)** [`ccsc-restore-guard`]: object `session_restore_view` on `paste_secret_guard`, representation `desktop_full`, role `replay_free_restore_disclosure`
- **Session-restore view (the replay-free session-restore view that reattaches read-only, replays no prior input, preserves retention scope, and requires a fresh control grant before write control resumes)** [`ccsc-restore-support`]: object `session_restore_view` on `support_export_packet`, representation `exported_redacted`, role `replay_free_restore_disclosure`
