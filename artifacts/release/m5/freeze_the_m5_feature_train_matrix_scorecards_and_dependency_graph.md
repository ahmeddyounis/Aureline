# M5 Feature-Train Matrix Artifact Companion

This file is the artifact-level companion document for the checked-in M5 feature-train matrix.

- **Canonical JSON**: `artifacts/release/m5/freeze_the_m5_feature_train_matrix_scorecards_and_dependency_graph.json`
- **Schema**: `schemas/governance/freeze_the_m5_feature_train_matrix_scorecards_and_dependency_graph.schema.json`
- **Typed consumer**: `crates/aureline-release/src/freeze_the_m5_feature_train_matrix_scorecards_and_dependency_graph/mod.rs`

The matrix is the single source of truth for M5 lane scope, scorecard posture, and dependency graph. All downstream surfaces ingest it directly.
