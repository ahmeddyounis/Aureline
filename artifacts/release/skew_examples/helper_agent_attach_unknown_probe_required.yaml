# Reviewer example: helper/agent attach outside declared adjacent window but inside same major line.
#
# Companion to fixtures/release/skew_cases/helper_agent_attach_unknown_probe_required.yaml
# and docs/release/skew_smoke_packet.md.

reviewer_example:
  name: helper_agent_attach_unknown_probe_required
  surface: helper_agent_attach
  skew_state: unknown_requires_probe
  outcome_label: probe_required
  promotion_decision: pending_probe
  one_paragraph_each:
    which_surface: >
      Helper/agent attach. Same major line, but one axis of the
      adjacent window (protocol floor, WIT version, or permission
      vocabulary) is exceeded.
    which_state: >
      Unknown requires probe. The packet refuses to label the
      combination supported, degraded, or blocked until a probe lands.
    user_experience: >
      The current client and agent pairing is outside the declared
      adjacent window but inside the same major line. Attach is not
      claimed as supported, degraded, or blocked yet; a probe will
      decide before the surface widens.
    report_and_support_routing: >
      Quotes compat_row:remote.attach_envelope_and_drift and
      skew_case:remote.attach_beyond_adjacent_same_major. Routes
      through exact_build_support plus compatibility_report. No
      recovery rung is claimed until diagnosis converges.
    promotion_decision_rationale: >
      Pending probe. Release evidence MUST cite this skew case and
      MUST NOT promote the combination until a compatible, degraded,
      or blocked verdict lands.

schema_version: 1
record_kind: skew_smoke_case_record
skew_case_id: skew_case:release.helper_agent_attach.unknown_probe_required

surface_class: helper_agent_attach
skew_state_class: unknown_requires_probe
outcome_label_class: probe_required
blocked_vs_degraded_class: unknown_requires_probe
promotion_decision_class: pending_probe

compatibility_row_ref: compat_row:remote.attach_envelope_and_drift
version_skew_register_ref: skew_case:remote.attach_beyond_adjacent_same_major

install_topology_refs: []
state_migration_refs: []

helper_agent_refs:
  - boundary.remote.agent_attach_envelope

support_packet_routing_classes:
  - exact_build_support
  - compatibility_report

preserved_state_classes:
  - user_authored_files
  - open_buffer_selection
  - workspace_trust_store
  - credential_store
  - remote_helper_authorization
  - managed_policy_overrides
  - session_restore_store

capability_impact_classes:
  - remote_helper_attach
  - ai_runtime_access
  - managed_control_plane_writes

recovery_rung_ref: null

explanation_fields:
  user_facing_summary: >
    The current client and agent pairing is outside the declared
    adjacent window but inside the same major line. Attach is not
    claimed as supported, degraded, or blocked yet; a probe will
    decide before the surface widens.
  compatibility_report_summary: >
    Compatibility report records the combination as untested against
    the remote attach row; outside-window posture is degraded pending
    the probe.
  support_summary: >
    Support exports carry the agent/client version and capability
    intersection so the probe has full context.
  promotion_summary: >
    Pending probe. Release evidence MUST cite this skew case and MUST
    NOT promote the combination until a verdict lands.

emitted_at: "2026-04-23T00:00:00Z"
