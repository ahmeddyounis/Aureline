# M5 Share-Eligibility and Downgrade-State Registries

- Packet: `m5-share-eligibility-and-downgrade-state-registries:stable:0001`
- Label: `M5 share-eligibility and downgrade-state registries emitting one durable append-only machine-readable share-eligibility descriptor per candidate shared object a surface could invite into a session — an unshared local buffer, an unsupported large-file mode, a binary asset, a declared generated output, or a policy-limited artifact — one typed field per record section: the object class, the eligibility class it earns (local-only, view-only, comment-only, unsupported-share, or full-shared-edit), the authority model that eligibility implies, and whether it may be shared, observed, or must stay local — so a surface keeps the object's true eligibility first rather than implying CRDT or full shared-edit support for an object class the platform cannot converge, with canonical / accessible / audit resolution-form coverage, and a machine-readable share-continuity-disposition record per candidate object that declares the disposition a user is offered before invite, share, or export continues (continue-local-only, share-view-only, share-comment-only, block-unsupported-share, or proceed-full-shared-edit) with its actor / time provenance and its shared-versus-local lineage — so the invite, share, and export flows never present an unsupported object class as CRDT or full-shared-edit capable, users see local-only / view-only / comment-only / unsupported-share truth before inviting participants or exporting session artifacts, a disposition never acts without actor / time provenance, and no disposition drops the object's eligibility lineage — rather than a green summary across the shared editor replica view, presence / cursor layer, comment / annotation / review-pin layer, collaboration degradation banner, help / docs, and support / export surfaces`
- Consumer surfaces: 6
- Scope selectors: changed_files_scope, pull_request_scope, base_head_range_scope, worktree_uncommitted_scope, full_tree_scope, saved_pack_snapshot_scope, pack_scope_unclassified
- Resolution forms: canonical_object, accessible_summary, audit_record
- Proof freshness SLO: 720 hours (last audit: 2026-07-15T00:00:00Z)

## Consumer surfaces

- **shared_editor_replica_view**: `stable`
  - Owner: Shared-editor-replica-view owner
  - Scope: The shared editor replica view resolves each candidate shared object the editor could invite into a session — an unshared local buffer, an unsupported large-file mode, a binary asset, a declared generated output, or a policy-limited artifact — to one typed share-eligibility descriptor naming the object class, the eligibility class it earns (local-only, view-only, comment-only, unsupported-share, or full-shared-edit), the authority model that eligibility implies, and whether it may be shared, observed, or must stay local, and to the share-continuity-disposition descriptor offering continue-local-only, share-view-only, share-comment-only, block-unsupported-share, or proceed-full-shared-edit with actor / time provenance; the eligibility class and its authority model are recorded before any invite or share action continues, and a descriptor that would let an unsupported object class read as CRDT or full-shared-edit capable degrades honestly instead of promoting a local-only or view-only object to full shared editing
  - Review-pack-record entries: 2 / review-pack-result entries: 2
- **presence_cursor_layer**: `stable`
  - Owner: Presence-cursor-layer owner
  - Scope: The presence / cursor layer resolves each shared object's eligibility to the share-eligibility descriptor that keeps the object class and eligibility class inspectable — carrying the authority model and the shared-versus-local lineage a participant must see — rather than letting a view-only or local-only object read as a fully editable shared replica; a descriptor that would drop the eligibility class and let an unsupported-share object read as convergent degrades honestly instead of implying full shared-edit support the object cannot back
  - Review-pack-record entries: 2 / review-pack-result entries: 2
- **support_export_packet**: `stable`
  - Owner: Support owner
  - Scope: Support resolves the recorded share-eligibility descriptors and their share-continuity dispositions while keeping the object class, eligibility class, authority model, and disposition bound to the export route, so an export or support packet can show whether it is quoting a local-only, view-only, comment-only, unsupported-share, or full-shared-edit object and what continuity disposition was taken, and preserve actor provenance; a disposition whose actor / time provenance is unstated and a disposition that would export an unsupported object class as shareable without its eligibility lineage degrade honestly so the object class, eligibility, and disposition are never dropped on export or companion handoff
  - Review-pack-record entries: 2 / review-pack-result entries: 1
- **comment_annotation_review_pin_layer**: `stable`
  - Owner: Comment-annotation-review-pin-layer owner
  - Scope: The comment / annotation / review-pin layer resolves each comment-eligible object to a share-eligibility descriptor that keeps the object class, eligibility class, and authority model as an inspectable record with a share-or-hold disposition; a descriptor whose disposition is unstated and a disposition that would let a comment-only or view-only object read as full-shared-edit capable degrade honestly instead of promoting a limited-eligibility object to full shared editing on the user's behalf
  - Review-pack-record entries: 2 / review-pack-result entries: 1
- **collaboration_degradation_banner**: `stable`
  - Owner: Collaboration-degradation-banner owner
  - Scope: The collaboration degradation banner resolves a share-attempt condition to a share-eligibility descriptor that names the distinct local-only / view-only / comment-only / unsupported-share / full-shared-edit class and surfaces the share-continuity disposition (continue-local-only, share-view-only, share-comment-only, block-unsupported-share, or proceed-full-shared-edit) as a sticky label rather than a generic shareable or broken badge; an object whose eligibility class is unstated and a distinct unsupported-share or local-only class collapsed into a generic shareable badge degrade honestly so a limited-eligibility object never disappears behind an undifferentiated shareable banner
  - Review-pack-record entries: 1 / review-pack-result entries: 1
- **help_docs**: `stable`
  - Owner: Help-docs owner
  - Scope: The help / docs feed carries the same share-eligibility and share-continuity-disposition truth, so a docs link whose object class is unstated, a disposition that presents an unsupported object class as CRDT or full-shared-edit capable, or a disposition that drops the eligibility lineage is visible in evidence — each object class named as an unshared local buffer, unsupported large-file mode, binary asset, declared generated output, or policy-limited artifact, and each disposition as continue-local-only, share-view-only, share-comment-only, block-unsupported-share, or proceed-full-shared-edit — rather than hidden behind a green summary
  - Review-pack-record entries: 1 / review-pack-result entries: 1
