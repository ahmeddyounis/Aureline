# Extension mirror and manual-import baseline artifacts

Checked baseline records for primary catalog, approved mirror, and manual
artifact import lanes. These records are generated from
`fixtures/extensions/m3/mirror_import/` through
`aureline_extensions::evaluate_mirror_import_baseline`.

Run:

```bash
cargo run --example dump_mirror_import_baseline_records -p aureline-extensions
```

