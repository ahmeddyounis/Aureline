# Effective control-stack examples.
#
# Companion to docs/settings/effective_control_stack_contract.md.
# These are compact pointers + summaries; the worked scenarios live in
# fixtures/settings/control_stack_cases/.

schema_version: 1
contract_doc_ref: docs/settings/effective_control_stack_contract.md
row_schema_ref: schemas/settings/effective_control_stack_row.schema.json
fixtures_dir_ref: fixtures/settings/control_stack_cases/

examples:
  - id: control_stack.example.offline_last_known_good_admin_bundle
    summary: Offline continuity via last-known-good signed admin bundle; managed override unreachable.
    fixture_ref: fixtures/settings/control_stack_cases/offline_last_known_good_admin_bundle.yaml

  - id: control_stack.example.expired_experiment
    summary: Expired experiment/flag binding is visible and falls back deterministically.
    fixture_ref: fixtures/settings/control_stack_cases/expired_experiment_binding.yaml

  - id: control_stack.example.policy_ceiling_overrides_local_preference
    summary: Policy ceiling narrows a broader local preference and remains explicit in the trace.
    fixture_ref: fixtures/settings/control_stack_cases/policy_ceiling_overrides_local_preference.yaml

  - id: control_stack.example.signed_bundle_mismatch
    summary: Signature mismatch blocks the candidate bundle and keeps last-known-good active.
    fixture_ref: fixtures/settings/control_stack_cases/signed_bundle_mismatch_last_known_good.yaml

  - id: control_stack.example.emergency_kill_switch_narrowing
    summary: Emergency kill switch narrows behavior immediately and stays auditable.
    fixture_ref: fixtures/settings/control_stack_cases/emergency_kill_switch_narrowing.yaml

