# Queue And Pressure Packets

This packet set records the beta evidence shape for resource-governor and
queue-lane truth. It joins the runtime snapshot, shell background-work status
projection, fixture corpus, schema boundary, and support-export projection.

The packet intentionally carries metadata and opaque ids only. It does not
include workspace content, raw paths, command lines, provider payloads, tokens,
or logs.
