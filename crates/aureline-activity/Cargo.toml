[package]
name = "aureline-activity"
description = "Notification-envelope, activity-object, badge-aggregate, fanout-receipt, and attention-routing contracts."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
