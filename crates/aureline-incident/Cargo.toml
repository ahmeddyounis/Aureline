[package]
name = "aureline-incident"
description = "Incident workspace and runbook packet alpha state projected through redacted support exports."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
aureline-crash = { path = "../aureline-crash" }
aureline-provider = { path = "../aureline-provider" }
aureline-support = { path = "../aureline-support" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
