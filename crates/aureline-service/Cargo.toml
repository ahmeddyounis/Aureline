[package]
name = "aureline-service"
description = "Frozen commercial-control-plane truth for managed lanes: meter families, entitlement and managed-state vocabulary, chargeback-scope ownership, forecast thresholds, grace-period rights, org-switch semantics, and the local-safe baseline every managed lane preserves."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
