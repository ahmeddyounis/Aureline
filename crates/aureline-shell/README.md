# aureline-shell

Desktop shell crate that owns the canonical shell-zone registry and the
desktop frame layout.

This crate is intended to be the live, user-facing shell container (as opposed
to the disposable `aureline-shell-spike` integration probe).

## Running

From the workspace root:

```
cargo run -p aureline-shell --bin aureline_shell
```

