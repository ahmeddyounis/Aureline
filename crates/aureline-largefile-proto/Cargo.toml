[package]
name = "aureline-largefile-proto"
description = "Prototype large-file path: paged reader, classification, and limited-mode capability split. Validates the ADR 0003 large-file mode without poisoning the normal piece-tree buffer."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[[bin]]
name = "largefile_proto"
path = "src/bin/largefile_proto.rs"

[lints]
workspace = true

[dependencies]
