[package]
name = "aureline-remote"
description = "Governed route objects, exposure-review sheets, and revocation truth for port-forward, tunnel, preview-route, and exposed-service rows."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_yaml = "0.9"
