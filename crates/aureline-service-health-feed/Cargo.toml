[package]
name = "aureline-service-health-feed"
description = "Shared service-health feed contract for desktop, headless, Help/About, diagnostics, and support export surfaces."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
