# Shell chrome screenshot baselines

Place checked-in PNG baselines in this folder.

Regenerate via:

  python3 tools/ci/capture_appearance_goldens.py --out-dir tests/golden/appearance/shell_chrome/baselines
