# yaml-language-server: $schema=../../../../schemas/framework/framework_support_strip.schema.json
__fixture__:
  name: strip_bridge_compat_degraded
  scenario: >
    A bridge / compatibility layer translates a non-native ecosystem's
    project model into Aureline's framework surfaces. Health is
    degraded_partial because the bridge cannot resolve every runtime
    capability; version compatibility is between_versions, downgrade
    behavior is summarised. Migration path action is exposed.
  expected:
    record_kind: framework_support_strip_record
    support_class: bridge_compatibility_layer
    pack_source_class: bridge_compatibility_layer
    health_class: degraded_partial
    findings: []

record_kind: framework_support_strip_record
framework_support_strip_schema_version: 1
framework_support_strip_id: framework.strip.legacy_jsx_bridge.degraded
captured_at: "2026-05-18T09:05:00Z"

surface_class: component_tree_surface

framework_identity_block:
  framework_family_class: react_or_jsx_family
  framework_name_label: "Legacy JSX bridge"
  framework_version_label: "0.9.x"
  framework_version_range_label: "0.8–1.0 bridged"

support_class: bridge_compatibility_layer

pack_or_bridge_source_block:
  pack_source_class: bridge_compatibility_layer
  pack_ref: null
  pack_version_label: null
  bridge_ref: framework.bridge.legacy_jsx.v1
  bridge_label: "Legacy JSX adapter v1"

health_block:
  health_class: degraded_partial
  freshness_class: warm_cached
  last_refresh_at: "2026-05-18T08:50:00Z"

scope_block:
  locality_class: mixed_local_and_remote
  workspace_scope_label: "Workspace HEAD (bridged)"
  workspace_scope_ref: workspace.head

compatibility_block:
  version_compatibility_class: between_versions
  supported_version_range_label: "0.8–1.0 bridged"
  downgraded_behavior_summary: >
    Component tree is read-only under the bridge; runtime confirmation is
    not available and routes are derived from configuration files only.

actions:
  - open_compatibility_details
  - open_migration_path
  - open_pack_docs
  - open_runtime_inspector

framework_certainty_row_record_ref: null
source_sync_chip_record_ref: null

summary: >
  Legacy JSX bridge running degraded; component tree and routes available
  in read-only mode with explicit downgrade behavior.
notes: null
