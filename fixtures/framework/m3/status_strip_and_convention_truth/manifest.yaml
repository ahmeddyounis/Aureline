__fixture__:
  name: status_strip_and_convention_truth_manifest
  scope: >
    Beta corpus of framework support / status strip records and framework-
    object certainty records (route / component / service / entity rows,
    convention-diagnostic rows, and generator-preview rows) that prove the
    cross-surface honesty contract on claimed framework wedges. Each case
    names the support class, pack/bridge source, certainty label, evidence
    anchors, and (where applicable) authored-vs-generated truth or file-
    effect rows. Surfaces (UI rows, audits, docs/help, support exports)
    MUST quote the same strip_id / certainty_id, support class, and
    certainty label the fixture emits.

schema_version: 1

case_refs:
  - fixtures/framework/m3/status_strip_and_convention_truth/strip_framework_pack_healthy.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/strip_bridge_compat_degraded.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/strip_heuristic_convention_only.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/row_exact_route_pack_backed.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/row_component_partial_evidence.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/row_service_bridge_with_caveat.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/diagnostic_hard_contract_violation.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/diagnostic_pack_capability_limitation.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/diagnostic_heuristic_suspicion.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/generator_pack_scaffold_review.yaml
  - fixtures/framework/m3/status_strip_and_convention_truth/generator_codemod_with_user_delete.yaml

expected_support_classes:
  - core_native
  - framework_pack
  - bridge_compatibility_layer
  - heuristic_convention_mode

expected_certainty_label_classes:
  - exact_pack_backed
  - derived_by_convention
  - partial_evidence
  - heuristic_suspicion

expected_framework_object_kinds:
  - framework_object_row
  - convention_diagnostic_row
  - generator_preview_row
