# yaml-language-server: $schema=../../../schemas/framework/framework_certainty_row.schema.json
__fixture__:
  name: no_framework_certainty
  scenario: >
    Diagnostics-lane fallback / inference reason row emitted for a file
    where only generic language and build evidence exists. The row
    lands on framework_unavailable with framework_primary_source_class
    = none and fallback_inference_reason_class =
    generic_language_signal_only, blocking any framework-specific claim
    upstream.
  axes:
    surface_class: diagnostics_lane_surface
    subject_kind_class: fallback_inference_reason_subject
    framework_certainty_class: framework_unavailable
    framework_primary_source_class: none
    fallback_inference_reason_class: generic_language_signal_only

record_kind: framework_certainty_row_record
framework_certainty_row_schema_version: 1
framework_certainty_row_id: framework.row.fallback.unavailable.0005
captured_at: "2026-04-30T18:35:30Z"

surface_class: diagnostics_lane_surface
subject_kind_class: fallback_inference_reason_subject
subject_label: "No framework certainty for this file"
subject_ref: framework.fallback.subject.diagnostics.0005

framework_certainty_class: framework_unavailable
framework_primary_source_class: none
framework_adapter_class: unsupported_or_unclaimed_adapter

framework_pack_ref: null
framework_adapter_revision_ref: null

freshness_class: unverified

proving_artifact_refs: []

source_of_certainty_chain:
  - certainty_source_class: language_server_fact
    provider_id_ref: provider.language_server.tsserver
    artifact_ref: null
    summary: "Generic TypeScript language-service signal only; no framework-pack overlay observed."
  - certainty_source_class: build_adapter_fact
    provider_id_ref: provider.build_adapter.npm_scripts
    artifact_ref: null
    summary: "Build adapter saw an npm-scripts target, not a framework-pack-typed build."

fallback_inference_reason_class: generic_language_signal_only

runtime_target_block: null
notebook_binding_block: null
route_mapping_block: null
component_tree_block: null

source_sync_chip_record_ref: null
preview_snapshot_record_ref: null
result_provenance_record_ref: language.result_provenance.diagnostics.unframework.0005
capability_negotiation_packet_ref: null

summary: >
  Aureline has no framework certainty for this file. Only generic
  TypeScript and npm-scripts evidence exists, and no framework pack is
  loaded, so the diagnostics lane emits a fallback / inference reason
  row that blocks any framework_proven claim by upstream surfaces.
notes: >
  Consumers reading this row MUST surface the
  generic_language_signal_only disclosure rather than re-typing "no
  framework detected"; the row is the single, shared explanation.
