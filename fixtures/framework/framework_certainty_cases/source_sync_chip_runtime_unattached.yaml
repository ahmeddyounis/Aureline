# yaml-language-server: $schema=../../../schemas/framework/source_sync_chip.schema.json
__fixture__:
  name: source_sync_chip_runtime_unattached
  scenario: >
    Diagnostics-lane source-sync chip emitted alongside the
    no_framework_certainty row. No preview lane is attached, no runtime
    is attached, and the source revision is unknown. The chip MUST NOT
    claim source_in_sync; it exposes open_canonical_source and
    open_framework_pack_status so the user can move toward evidence.

record_kind: source_sync_chip_record
source_sync_chip_schema_version: 1
source_sync_chip_id: framework.chip.diagnostics.unattached.0005
captured_at: "2026-04-30T18:35:30Z"

preview_snapshot_record_ref: null
preview_runtime_strip_record_ref: null
hot_reload_state_record_ref: null
framework_certainty_row_record_ref: framework.row.fallback.unavailable.0005

preview_lane_class: no_preview_lane_attached

source_sync_state_class: source_revision_unknown

source_revision_block:
  source_revision_class: unknown_revision
  source_revision_ref: null
  drifted_paths_count: null

runtime_revision_block:
  runtime_revision_class: runtime_revision_unattached_no_runtime
  runtime_revision_ref: null
  framework_adapter_revision_ref: null

hot_reload_status_class: unavailable

target_device_context_block:
  device_target_class: no_device_target_attached
  viewport_preset_class: preset_not_applicable
  target_environment_class: no_runtime_target_attached
  device_handle_ref: null
  redacted_target_label: null

fallback_open_actions:
  - open_canonical_source
  - open_framework_pack_status
  - export_metadata_only

runtime_log_ref: null
managed_runtime_request_ref: null
framework_pack_status_ref: framework.pack_status.workspace.0005

summary: >
  No preview lane attached, no runtime attached, source revision
  unknown. Chip routes the user to canonical source, the framework-pack
  status surface, and a metadata-only export.
notes: >
  This fixture demonstrates that a diagnostics surface with no preview
  attached still emits a source-sync chip with at least one
  fallback-open action — never a blank disclosure.
