# yaml-language-server: $schema=../../../schemas/framework/source_sync_chip.schema.json
__fixture__:
  name: source_sync_chip_drifted
  scenario: >
    Browser-lane source-sync chip composed with the stale_preview_mapping
    row. Source revision drifted since render, runtime revision is older
    than the working tree, hot reload is in restart_required, and the
    chip exposes open_canonical_source / open_diff_against_source and
    restart_preview_runtime fallback-open actions so the user is never
    given a dead-end "stale" label.

record_kind: source_sync_chip_record
source_sync_chip_schema_version: 1
source_sync_chip_id: framework.chip.drifted.0003
captured_at: "2026-04-30T18:32:45Z"

preview_snapshot_record_ref: preview.snapshot.browser.drifted.0003
preview_runtime_strip_record_ref: preview.strip.browser.drifted.0003
hot_reload_state_record_ref: preview.hot_reload.browser.drifted.0003
framework_certainty_row_record_ref: framework.row.source_sync.stale.0003

preview_lane_class: browser_preview_lane

source_sync_state_class: source_drifted_since_render

source_revision_block:
  source_revision_class: working_tree_with_pending_edits
  source_revision_ref: revision.working_tree.workspace.primary.dirty
  drifted_paths_count: 4

runtime_revision_block:
  runtime_revision_class: runtime_revision_drifted_from_source
  runtime_revision_ref: runtime.revision.nuxt_dev_server.older
  framework_adapter_revision_ref: framework.adapter.nuxt_pages.revision.older

hot_reload_status_class: restart_required

target_device_context_block:
  device_target_class: browser_window_target
  viewport_preset_class: desktop_default_preset
  target_environment_class: local_dev_server
  device_handle_ref: device.handle.browser.window.0003
  redacted_target_label: "Local Chromium window (desktop default)"

fallback_open_actions:
  - open_canonical_source
  - open_diff_against_source
  - restart_preview_runtime
  - reveal_in_route_explorer

runtime_log_ref: null
managed_runtime_request_ref: null
framework_pack_status_ref: null

summary: >
  Source drifted since render: working tree has pending edits across
  four paths, runtime revision predates the edits, and hot reload is
  in restart_required. Chip routes the user to canonical source / diff
  / runtime restart instead of collapsing into a vague stale label.
notes: null
