# yaml-language-server: $schema=../../../schemas/framework/source_sync_chip.schema.json
__fixture__:
  name: source_sync_chip_in_sync
  scenario: >
    Browser-lane source-sync chip composed with the
    fully_proven_framework_route row. Source revision is in sync at
    workspace HEAD, runtime revision is pinned to source, hot-reload is
    applied, and the only fallback-open action is no_action_already_in_sync
    plus reveal_in_route_explorer for navigation.

record_kind: source_sync_chip_record
source_sync_chip_schema_version: 1
source_sync_chip_id: framework.chip.in_sync.0001
captured_at: "2026-04-30T18:30:00Z"

preview_snapshot_record_ref: preview.snapshot.browser.in_sync.0001
preview_runtime_strip_record_ref: preview.strip.browser.in_sync.0001
hot_reload_state_record_ref: preview.hot_reload.browser.in_sync.0001
framework_certainty_row_record_ref: framework.row.route.proven.0001

preview_lane_class: browser_preview_lane

source_sync_state_class: source_in_sync

source_revision_block:
  source_revision_class: vcs_commit_pinned
  source_revision_ref: revision.commit.workspace.primary.head
  drifted_paths_count: 0

runtime_revision_block:
  runtime_revision_class: runtime_revision_pinned_to_source
  runtime_revision_ref: runtime.revision.next_dev_server.head
  framework_adapter_revision_ref: framework.adapter.next_app_router.revision.head

hot_reload_status_class: applied

target_device_context_block:
  device_target_class: browser_window_target
  viewport_preset_class: desktop_default_preset
  target_environment_class: local_dev_server
  device_handle_ref: device.handle.browser.window.0001
  redacted_target_label: "Local Chromium window (desktop default)"

fallback_open_actions:
  - no_action_already_in_sync
  - reveal_in_route_explorer
  - reveal_in_component_tree

runtime_log_ref: null
managed_runtime_request_ref: null
framework_pack_status_ref: null

summary: >
  Source in sync at workspace HEAD, runtime revision pinned, hot reload
  applied. Chip offers no_action_already_in_sync and reveal navigation
  to the framework-aware consumers.
notes: null
