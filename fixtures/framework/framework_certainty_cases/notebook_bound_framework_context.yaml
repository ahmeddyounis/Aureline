# yaml-language-server: $schema=../../../schemas/framework/framework_certainty_row.schema.json
__fixture__:
  name: notebook_bound_framework_context
  scenario: >
    Notebook framework-context inspector row for a notebook-aware
    framework. The notebook adapter projects a single cell onto the
    detected framework, but no kernel is currently consulted, so the
    row carries a notebook-binding block citing
    kernel_runtime_not_consulted and lands on framework_inferred.
    Demonstrates that the notebook framework-context surface forces a
    notebook_binding_block and never silently upgrades to
    framework_proven.
  axes:
    surface_class: notebook_framework_context_surface
    subject_kind_class: detected_framework_subject
    framework_certainty_class: framework_inferred
    framework_primary_source_class: project_graph_projection
    fallback_inference_reason_class: notebook_kernel_not_consulted

record_kind: framework_certainty_row_record
framework_certainty_row_schema_version: 1
framework_certainty_row_id: framework.row.notebook.framework_context.0004
captured_at: "2026-04-30T18:34:00Z"

surface_class: notebook_framework_context_surface
subject_kind_class: detected_framework_subject
subject_label: "Detected: SwiftUI widget family"
subject_ref: framework.detected.swiftui.notebook.0004

framework_certainty_class: framework_inferred
framework_primary_source_class: project_graph_projection
framework_adapter_class: swiftui_widget_family

framework_pack_ref: null
framework_adapter_revision_ref: null

freshness_class: warm_cached

proving_artifact_refs: []

source_of_certainty_chain:
  - certainty_source_class: notebook_adapter_projection
    provider_id_ref: provider.notebook_adapter.swiftui_companion
    artifact_ref: null
    summary: "Notebook adapter projected the cell onto the SwiftUI-shaped namespace it has cached."
  - certainty_source_class: project_graph_fact
    provider_id_ref: provider.project_graph.workspace
    artifact_ref: null
    summary: "Project graph corroborates the SwiftUI imports without proving the runtime."
  - certainty_source_class: syntax_fact
    provider_id_ref: provider.syntax.swift
    artifact_ref: null
    summary: "Syntax-level fallback confirmed the View struct shape."

fallback_inference_reason_class: notebook_kernel_not_consulted

runtime_target_block: null

notebook_binding_block:
  notebook_document_ref: notebook.document.swift_examples.0004
  cell_id_ref: notebook.cell.swiftui_card.0004
  kernel_session_id_ref: null
  notebook_note_classes:
    - notebook_cell_projection
    - kernel_runtime_not_consulted

route_mapping_block: null
component_tree_block: null

source_sync_chip_record_ref: framework.chip.notebook.unattached.0004
preview_snapshot_record_ref: null
result_provenance_record_ref: language.result_provenance.notebook_context.swiftui.0004
capability_negotiation_packet_ref: null

summary: >
  The notebook framework-context inspector reports SwiftUI as a
  notebook-cell projection. No kernel was consulted, so the row stays
  at framework_inferred and discloses kernel_runtime_not_consulted as
  the fallback / inference reason.
notes: null
