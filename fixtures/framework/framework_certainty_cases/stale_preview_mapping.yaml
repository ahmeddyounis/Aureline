# yaml-language-server: $schema=../../../schemas/framework/framework_certainty_row.schema.json
__fixture__:
  name: stale_preview_mapping
  scenario: >
    Preview-card source-synchronization row whose framework pack ran
    against an older revision but the working tree has moved since.
    framework_stale with proving_artifact_stale fallback, paired with a
    source_sync_chip_record that reports source_drifted_since_render.
    The row blocks any framework_proven claim while the source / runtime
    revisions disagree.
  axes:
    surface_class: preview_card_surface
    subject_kind_class: source_synchronization_subject
    framework_certainty_class: framework_stale
    framework_primary_source_class: framework_pack_analysis
    fallback_inference_reason_class: proving_artifact_stale
    freshness_class: stale

record_kind: framework_certainty_row_record
framework_certainty_row_schema_version: 1
framework_certainty_row_id: framework.row.source_sync.stale.0003
captured_at: "2026-04-30T18:32:45Z"

surface_class: preview_card_surface
subject_kind_class: source_synchronization_subject
subject_label: "Source ↔ runtime drift"
subject_ref: framework.source_sync.subject.preview_card.0003

framework_certainty_class: framework_stale
framework_primary_source_class: framework_pack_analysis
framework_adapter_class: vue_or_template_family

framework_pack_ref: framework.pack.nuxt_pages.v2
framework_adapter_revision_ref: framework.adapter.nuxt_pages.revision.older

freshness_class: stale

proving_artifact_refs:
  - framework.proving_artifact.nuxt_pages.routes_manifest.older

source_of_certainty_chain:
  - certainty_source_class: framework_pack_analysis
    provider_id_ref: provider.framework_pack.nuxt_pages
    artifact_ref: framework.proving_artifact.nuxt_pages.routes_manifest.older
    summary: "Framework pack ran against an earlier source revision; the manifest is past its freshness floor."
  - certainty_source_class: project_graph_fact
    provider_id_ref: provider.project_graph.workspace
    artifact_ref: null
    summary: "Project graph confirms paths shifted since the manifest was produced."

fallback_inference_reason_class: proving_artifact_stale

runtime_target_block:
  target_environment_class: local_dev_server
  sandbox_posture_class: trusted_local_runtime
  preview_snapshot_record_ref: preview.snapshot.browser.drifted.0003
  execution_context_record_ref: execution_context.workspace.dev.older

notebook_binding_block: null
route_mapping_block: null
component_tree_block: null

source_sync_chip_record_ref: framework.chip.drifted.0003
preview_snapshot_record_ref: preview.snapshot.browser.drifted.0003
result_provenance_record_ref: null
capability_negotiation_packet_ref: null

summary: >
  Framework pack evidence is stale: the routes manifest was produced
  against an older source revision, the working tree has moved, and the
  paired source-sync chip routes the user to canonical source plus a
  runtime restart action.
notes: >
  framework_stale rows MUST keep the stale label everywhere a consumer
  surface projects them; the fallback_inference_reason of
  proving_artifact_stale is what the diagnostics lane and AI-assist
  context lane quote rather than re-typing "out of date".
