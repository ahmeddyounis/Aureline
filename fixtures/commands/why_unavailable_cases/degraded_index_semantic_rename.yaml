record_kind: why_unavailable_case_record
why_unavailable_case_schema_version: 1
case_id: "why-unavailable:degraded_index:semantic_rename:01"
reason_class_id: degraded_index

source_refs:
  grammar_ref: docs/ux/disabled_reason_grammar.md
  class_catalog_ref: artifacts/ux/disabled_reason_classes.yaml#degraded_index
  degraded_mode_ref: docs/ux/degraded_mode_pattern.md

command:
  command_id: "cmd:editor.rename_symbol"
  command_revision_ref: "cmd-rev:editor.rename_symbol:2026.04.28-01"
  canonical_verb: editor.rename_symbol
  action_label_text: "Rename Symbol"
  action_label_ref: "label:editor.rename_symbol:primary"
  disabled_reason_codes:
    - freshness_floor_unmet
  capability_scope_class: recoverable_durable_mutation

explanation:
  current_cause:
    message_id: "msg:why_unavailable.degraded_index.semantic_rename.cause"
    source_text: "{action_label} is limited because {dependency_label} is not current for {scope_label}."
    cause_owner_class: semantic_index
    machine_reason_refs:
      - disabled_reason_code: freshness_floor_unmet
    placeholder_values:
      action_label: "Rename Symbol"
      dependency_label: "semantic index"
      scope_label: "current workspace"
  safe_next_step:
    message_id: "msg:why_unavailable.degraded_index.semantic_rename.next_step"
    source_text: "Use text search in {scope_label}."
    action_kind: command
    command_id: "cmd:search.find_in_workspace"
    revalidates_gates: true
  alternate_route:
    route_kind: detail_surface
    label_text: "Open index status"
    command_id: "cmd:index.open_status"
    lost_capability_text: "Semantic rename remains unavailable until the index is current."
    revalidates_gates: true
  explanation_freshness:
    freshness_class: cached_snapshot
    evaluated_at: "2026-04-28T18:07:00Z"
    freshness_text: "Last checked 2 min ago"
  docs_help_anchor_ref:
    pack_id: "pack:project:aureline:01"
    anchor_id: "docs:anchor:editor:semantic_index_readiness"
    anchor_kind: docs_page_anchor
  message_template_refs:
    compact: "msg:why_unavailable.degraded_index.compact"
    short: "msg:why_unavailable.degraded_index.semantic_rename.short"
    detail: "msg:why_unavailable.degraded_index.semantic_rename.detail"
    status_or_banner: "msg:why_unavailable.degraded_index.semantic_rename.banner"
    diagnostics: "msg:why_unavailable.degraded_index.semantic_rename.diagnostics"
    docs_help: "msg:why_unavailable.degraded_index.semantic_rename.docs"
  placeholder_kinds:
    action_label: action_label
    dependency_label: dependency_label
    scope_label: scope_label
    relative_time: relative_time

surface_projections:
  command_palette:
    chip_text: "Index degraded"
    short_text: "Rename Symbol is limited because the semantic index is not current for current workspace."
    footer_action: "Use text search"
  menu:
    chip_text: "Index degraded"
    disabled_detail: "Semantic rename is paused while the workspace index catches up."
    why_unavailable_route: command_diagnostics
  status_or_banner:
    heading: "Semantic rename is waiting on the index"
    body: "Use text search now or open index status. Last checked 2 min ago."
  diagnostics:
    reason_class_id: degraded_index
    remediation_ref: "diag-remediation:editor.rename_symbol:degraded_index:2026.04.28-01"
  docs_help:
    anchor_id: "docs:anchor:editor:semantic_index_readiness"
    preserves_next_step: true
  accessibility_narration:
    text: "Rename Symbol, index degraded. Use text search, or open index status."

support_export:
  redaction_class: metadata_safe_default
  export_fields:
    - reason_class_id
    - command_id
    - disabled_reason_code
    - explanation_freshness
    - alternate_route

parity_assertions:
  same_reason_class_across_surfaces: true
  alternate_route_is_concrete: true
  cached_freshness_disclosed: true

