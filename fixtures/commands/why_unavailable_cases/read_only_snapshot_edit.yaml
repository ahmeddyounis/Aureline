record_kind: why_unavailable_case_record
why_unavailable_case_schema_version: 1
case_id: "why-unavailable:read_only:snapshot_edit:01"
reason_class_id: read_only

source_refs:
  grammar_ref: docs/ux/disabled_reason_grammar.md
  class_catalog_ref: artifacts/ux/disabled_reason_classes.yaml#read_only
  state_recovery_ref: docs/ux/state_and_recovery_taxonomy.md

command:
  command_id: "cmd:editor.edit_recovered_snapshot"
  command_revision_ref: "cmd-rev:editor.edit_recovered_snapshot:2026.04.28-01"
  canonical_verb: editor.edit_recovered_snapshot
  action_label_text: "Edit Recovered Snapshot"
  action_label_ref: "label:editor.edit_recovered_snapshot:primary"
  disabled_reason_codes:
    - workspace_trust_restricted
  capability_scope_class: reversible_local_mutation

explanation:
  current_cause:
    message_id: "msg:why_unavailable.read_only.snapshot_edit.cause"
    source_text: "{action_label} is read-only here because {current_cause}."
    cause_owner_class: recovered_snapshot
    machine_reason_refs:
      - state_token: read_only_degraded
      - disabled_reason_code: workspace_trust_restricted
    placeholder_values:
      action_label: "Edit Recovered Snapshot"
      current_cause: "the recovered snapshot preserves prior work without write authority"
      scope_label: "recovered snapshot"
  safe_next_step:
    message_id: "msg:why_unavailable.read_only.snapshot_edit.next_step"
    source_text: "Open live source."
    action_kind: command
    command_id: "cmd:editor.open_live_source"
    revalidates_gates: true
  alternate_route:
    route_kind: read_only_surface
    label_text: "Copy from snapshot"
    command_id: "cmd:editor.copy_from_snapshot"
    lost_capability_text: "The snapshot remains inspect-only."
    revalidates_gates: true
  explanation_freshness:
    freshness_class: live_evaluated
    evaluated_at: "2026-04-28T18:12:00Z"
  docs_help_anchor_ref:
    pack_id: "pack:project:aureline:01"
    anchor_id: "docs:anchor:restore:recovered_snapshot_read_only"
    anchor_kind: docs_page_anchor
  message_template_refs:
    compact: "msg:why_unavailable.read_only.compact"
    short: "msg:why_unavailable.read_only.snapshot_edit.short"
    detail: "msg:why_unavailable.read_only.snapshot_edit.detail"
    status_or_banner: "msg:why_unavailable.read_only.snapshot_edit.banner"
    diagnostics: "msg:why_unavailable.read_only.snapshot_edit.diagnostics"
    docs_help: "msg:why_unavailable.read_only.snapshot_edit.docs"
  placeholder_kinds:
    action_label: action_label
    current_cause: freeform_string
    scope_label: scope_label

surface_projections:
  command_palette:
    chip_text: "Read-only"
    short_text: "Edit Recovered Snapshot is read-only here because the recovered snapshot preserves prior work without write authority."
    footer_action: "Open live source"
  menu:
    chip_text: "Read-only"
    disabled_detail: "Open live source to edit, or copy from the snapshot."
    why_unavailable_route: command_diagnostics
  status_or_banner:
    heading: "Snapshot is read-only"
    body: "Open live source to edit. You can still copy from the snapshot."
  diagnostics:
    reason_class_id: read_only
    remediation_ref: "diag-remediation:editor.edit_recovered_snapshot:read_only:2026.04.28-01"
  docs_help:
    anchor_id: "docs:anchor:restore:recovered_snapshot_read_only"
    preserves_next_step: true
  accessibility_narration:
    text: "Edit Recovered Snapshot, read-only. Open live source, or copy from snapshot."

support_export:
  redaction_class: metadata_safe_default
  export_fields:
    - reason_class_id
    - command_id
    - state_token
    - alternate_route

parity_assertions:
  same_reason_class_across_surfaces: true
  preserved_capability_named: true
  hidden_write_queue_absent: true

