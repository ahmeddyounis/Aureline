record_kind: why_unavailable_case_record
why_unavailable_case_schema_version: 1
case_id: "why-unavailable:unsupported_mode:voice_command:01"
reason_class_id: unsupported_current_mode

source_refs:
  grammar_ref: docs/ux/disabled_reason_grammar.md
  class_catalog_ref: artifacts/ux/disabled_reason_classes.yaml#unsupported_current_mode
  voice_contract_ref: docs/ux/voice_and_dictation_contract.md

command:
  command_id: "cmd:task.run_deploy"
  command_revision_ref: "cmd-rev:task.run_deploy:2026.04.28-01"
  canonical_verb: task.run_deploy
  action_label_text: "Run Deploy Task"
  action_label_ref: "label:task.run_deploy:primary"
  disabled_reason_codes:
    - client_scope_excludes_surface
  capability_scope_class: externally_visible_mutation

explanation:
  current_cause:
    message_id: "msg:why_unavailable.unsupported_mode.voice_command.cause"
    source_text: "{action_label} is not supported in {scope_label}."
    cause_owner_class: current_mode
    machine_reason_refs:
      - disabled_reason_code: client_scope_excludes_surface
    placeholder_values:
      action_label: "Run Deploy Task"
      scope_label: "companion voice mode"
      target_class: "desktop command surface"
  safe_next_step:
    message_id: "msg:why_unavailable.unsupported_mode.voice_command.next_step"
    source_text: "Switch to {target_class}."
    action_kind: external_handoff
    command_id: "cmd:desktop.open_command_surface"
    revalidates_gates: true
  alternate_route:
    route_kind: command
    label_text: "Open task details"
    command_id: "cmd:task.open_details"
    lost_capability_text: "Task details are inspect-only from companion voice mode."
    revalidates_gates: true
  explanation_freshness:
    freshness_class: live_evaluated
    evaluated_at: "2026-04-28T18:15:00Z"
  docs_help_anchor_ref:
    pack_id: "pack:project:aureline:01"
    anchor_id: "docs:anchor:voice:unsupported_mutating_commands"
    anchor_kind: docs_page_anchor
  message_template_refs:
    compact: "msg:why_unavailable.unsupported_current_mode.compact"
    short: "msg:why_unavailable.unsupported_current_mode.voice_command.short"
    detail: "msg:why_unavailable.unsupported_current_mode.voice_command.detail"
    status_or_banner: "msg:why_unavailable.unsupported_current_mode.voice_command.banner"
    diagnostics: "msg:why_unavailable.unsupported_current_mode.voice_command.diagnostics"
    docs_help: "msg:why_unavailable.unsupported_current_mode.voice_command.docs"
  placeholder_kinds:
    action_label: action_label
    scope_label: scope_label
    target_class: target_class

surface_projections:
  command_palette:
    chip_text: "Unsupported here"
    short_text: "Run Deploy Task is not supported in companion voice mode."
    footer_action: "Switch to desktop command surface"
  menu:
    chip_text: "Unsupported here"
    disabled_detail: "Open task details here, or switch to the desktop command surface to run."
    why_unavailable_route: command_diagnostics
  status_or_banner:
    heading: "Deploy cannot run from companion voice mode"
    body: "Switch to the desktop command surface, or open task details in inspect-only mode."
  diagnostics:
    reason_class_id: unsupported_current_mode
    remediation_ref: "diag-remediation:task.run_deploy:unsupported_mode:2026.04.28-01"
  docs_help:
    anchor_id: "docs:anchor:voice:unsupported_mutating_commands"
    preserves_next_step: true
  accessibility_narration:
    text: "Run Deploy Task, unsupported here. Switch to desktop command surface."

support_export:
  redaction_class: metadata_safe_default
  export_fields:
    - reason_class_id
    - command_id
    - disabled_reason_code
    - scope_label
    - alternate_route

parity_assertions:
  same_reason_class_across_surfaces: true
  supported_surface_named: true
  hidden_invocation_absent: true

