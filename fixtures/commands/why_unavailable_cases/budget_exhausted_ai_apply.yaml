record_kind: why_unavailable_case_record
why_unavailable_case_schema_version: 1
case_id: "why-unavailable:budget_exhausted:ai_apply:01"
reason_class_id: quota_or_budget_exhausted

source_refs:
  grammar_ref: docs/ux/disabled_reason_grammar.md
  class_catalog_ref: artifacts/ux/disabled_reason_classes.yaml#quota_or_budget_exhausted
  degraded_mode_ref: docs/ux/degraded_mode_pattern.md

command:
  command_id: "cmd:ai.apply_patch"
  command_revision_ref: "cmd-rev:ai.apply_patch:2026.04.28-01"
  canonical_verb: ai.apply_patch
  action_label_text: "Apply AI Patch"
  action_label_ref: "label:ai.apply_patch:primary"
  disabled_reason_codes: []
  capability_scope_class: recoverable_durable_mutation

explanation:
  current_cause:
    message_id: "msg:why_unavailable.budget_exhausted.ai_apply.cause"
    source_text: "{action_label} is paused because {budget_name} is exhausted."
    cause_owner_class: workspace_budget
    machine_reason_refs:
      - state_token: budget_exhausted
    placeholder_values:
      action_label: "Apply AI Patch"
      budget_name: "workspace AI budget"
      scope_label: "managed AI requests"
  safe_next_step:
    message_id: "msg:why_unavailable.budget_exhausted.ai_apply.next_step"
    source_text: "Open {budget_name} details."
    action_kind: detail_surface
    command_id: "cmd:ai.open_budget_details"
    revalidates_gates: true
  alternate_route:
    route_kind: read_only_surface
    label_text: "Open patch review manually"
    command_id: "cmd:review.open_patch_review"
    lost_capability_text: "Managed AI generation and apply automation remain paused."
    revalidates_gates: true
  explanation_freshness:
    freshness_class: live_evaluated
    evaluated_at: "2026-04-28T18:17:00Z"
  docs_help_anchor_ref:
    pack_id: "pack:project:aureline:01"
    anchor_id: "docs:anchor:ai:budget_exhausted"
    anchor_kind: docs_page_anchor
  message_template_refs:
    compact: "msg:why_unavailable.quota_or_budget_exhausted.compact"
    short: "msg:why_unavailable.quota_or_budget_exhausted.ai_apply.short"
    detail: "msg:why_unavailable.quota_or_budget_exhausted.ai_apply.detail"
    status_or_banner: "msg:why_unavailable.quota_or_budget_exhausted.ai_apply.banner"
    diagnostics: "msg:why_unavailable.quota_or_budget_exhausted.ai_apply.diagnostics"
    docs_help: "msg:why_unavailable.quota_or_budget_exhausted.ai_apply.docs"
  placeholder_kinds:
    action_label: action_label
    budget_name: budget_name
    scope_label: scope_label

surface_projections:
  command_palette:
    chip_text: "Budget exhausted"
    short_text: "Apply AI Patch is paused because workspace AI budget is exhausted."
    footer_action: "Open workspace AI budget details"
  menu:
    chip_text: "Budget exhausted"
    disabled_detail: "Open patch review manually; managed AI apply remains paused."
    why_unavailable_route: command_diagnostics
  status_or_banner:
    heading: "Managed AI budget is exhausted"
    body: "Editing, save, search, and local review continue. Open budget details or review the patch manually."
  diagnostics:
    reason_class_id: quota_or_budget_exhausted
    remediation_ref: "diag-remediation:ai.apply_patch:budget_exhausted:2026.04.28-01"
  docs_help:
    anchor_id: "docs:anchor:ai:budget_exhausted"
    preserves_next_step: true
  accessibility_narration:
    text: "Apply AI Patch, budget exhausted. Open budget details, or open patch review manually."

support_export:
  redaction_class: metadata_safe_default
  export_fields:
    - reason_class_id
    - command_id
    - state_token
    - budget_name
    - alternate_route

parity_assertions:
  same_reason_class_across_surfaces: true
  unaffected_local_capabilities_named: true
  alternate_route_avoids_exhausted_budget: true

