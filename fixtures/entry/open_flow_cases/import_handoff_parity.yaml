record_kind: open_flow_case_record
open_flow_case_schema_version: 1
case_id: open_flow:import_handoff:parity:01
title: Import handoff parity across Start Center, palette, and deep-link intent review
expected_open_route:
  open_route_id: open_route.import_handoff_or_portable_state
  entry_route_id_family: workspace_entry.import
  entry_verb: import
  target_kind: handoff_packet
  resulting_mode: extract_then_review
  open_flow_sheet_class: import_artifact
  command_identity:
    ux_command_id_ref: command.workspace.import_handoff_packet
    stable_command_id: cmd:workspace.import_handoff_packet
  required_previews:
    open_flow_sheet_record: required
    import_compare_or_dry_run_preview: required_when_mutation_possible
  recents:
    recent_work_target_kind: handoff_packet
surfaces:
  - surface_class: start_center
    invocation: import_primary_action
    uses_open_route_id: open_route.import_handoff_or_portable_state
    uses_command_ref: command.workspace.import_handoff_packet
    uses_stable_command_id: cmd:workspace.import_handoff_packet
  - surface_class: command_palette
    invocation: palette_import_handoff
    uses_open_route_id: open_route.import_handoff_or_portable_state
    uses_command_ref: command.workspace.import_handoff_packet
    uses_stable_command_id: cmd:workspace.import_handoff_packet
  - surface_class: deep_link
    invocation: deep_link_intent_review_then_import
    uses_open_route_id: open_route.import_handoff_or_portable_state
    uses_command_ref: command.workspace.import_handoff_packet
    uses_stable_command_id: cmd:workspace.import_handoff_packet
__fixture__:
  name: import_handoff_parity
  scenario: >
    Import is not “open”. Start Center, palette, and deep-link invocations all resolve
    to the same import route, require the same review posture, and preserve the same
    command identity and resulting mode.
  contract_sections:
    - artifacts/entry/open_flow_truth.md §4.8
    - artifacts/entry/open_flow_truth.md §5

