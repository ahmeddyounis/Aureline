record_kind: open_flow_case_record
open_flow_case_schema_version: 1
case_id: open_flow:open_workspace_manifest:parity:01
title: Open workspace manifest parity across entry surfaces
expected_open_route:
  open_route_id: open_route.open_workspace_manifest
  entry_route_id_family: workspace_entry.open_workspace
  entry_verb: open
  target_kind: workspace_manifest
  resulting_mode: workspace_with_roots
  open_flow_sheet_class: open_local_target
  command_identity:
    ux_command_id_ref: command.workspace.open_workspace
    stable_command_id: cmd:workspace.open_workspace
  required_previews:
    open_flow_sheet_record: required
    scope_consequence_switch_preview: required_when_replacing_active_workspace_or_dirty_buffers
  recents:
    recent_work_target_kind: workspace_manifest
surfaces:
  - surface_class: start_center
    invocation: open_workspace_primary_action
    uses_open_route_id: open_route.open_workspace_manifest
    uses_command_ref: command.workspace.open_workspace
    uses_stable_command_id: cmd:workspace.open_workspace
  - surface_class: main_menu
    invocation: file_menu_open_workspace
    uses_open_route_id: open_route.open_workspace_manifest
    uses_command_ref: command.workspace.open_workspace
    uses_stable_command_id: cmd:workspace.open_workspace
  - surface_class: command_palette
    invocation: palette_open_workspace
    uses_open_route_id: open_route.open_workspace_manifest
    uses_command_ref: command.workspace.open_workspace
    uses_stable_command_id: cmd:workspace.open_workspace
  - surface_class: deep_link
    invocation: deep_link_intent_review_then_open
    uses_open_route_id: open_route.open_workspace_manifest
    uses_command_ref: command.workspace.open_workspace
    uses_stable_command_id: cmd:workspace.open_workspace
__fixture__:
  name: open_workspace_manifest_parity
  scenario: >
    Opening a workspace manifest must be the same route everywhere: Start Center,
    main menu, command palette, and deep-link intent review all resolve to the same
    route id, command identity, and open-flow sheet semantics.
  contract_sections:
    - artifacts/entry/open_flow_truth.md §4.3
    - artifacts/entry/open_flow_truth.md §5

