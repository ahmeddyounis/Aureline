# Git Branch Switch Alpha Fixtures

These fixtures exercise the preview-first branch switch, create, and checkout
contract in `aureline-git`.

Run them with:

```sh
cargo test -p aureline-git --test branch_switch_alpha
```
