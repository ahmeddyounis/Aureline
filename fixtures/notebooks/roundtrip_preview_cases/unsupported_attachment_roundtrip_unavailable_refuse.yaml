# yaml-language-server: $schema=../../../schemas/notebooks/roundtrip_preview.schema.json
__fixture__:
  name: unsupported_attachment_roundtrip_unavailable_refuse
  scenario: >-
    A notebook contains an attachment/output class the structured editor
    cannot interpret (schema-version mismatch or unsupported attachment
    kind). Round-trip risk is unavailable; the surface refuses rewrite
    and renders a tombstone preview with safe next actions (inspect raw,
    export evidence, or update the renderer).
  seed_section: Round-trip unavailable; refuse rewrite; tombstone preview
  exercised_classes:
    subject_class: notebook_document
    structured_round_trip_risk_class: round_trip_unavailable
    preview_representation_class: tombstone_preview
    apply_gate_class: refuse_rewrite
  linked_docs:
    - docs/notebooks/notebook_trust_and_roundtrip_preview_contract.md
    - docs/adr/0022-notebook-document-model-kernel-transport-trust-and-diff-merge.md

roundtrip_preview_schema_version: 1
record_kind: structured_round_trip_preview_sheet_record
preview_id: rtpreview.nb.doc.roundtrip_unavailable.unsupported_attachment.v1

subject:
  subject_class: notebook_document
  subject_ref: nb.subject.doc.unsupported_attachment
  display_label: Notebook preview (unsupported attachment; structured round-trip unavailable)
  artifact_class: null
  notebook_document_ref: nb.doc.imported.public.notebook.01
  cell_id_ref: null
  output_id_ref: null

trust_posture:
  notebook_trust_rung: untrusted_quarantined_for_review
  per_cell_trust_overrides: []
  document_trust_state: document_trust_inherited_from_workspace
  kernel_trust_state: kernel_trust_unavailable_no_kernel
  output_trust_state: null
  widget_trust_state: null
  kernel_transport_class: null

authored_source:
  source_format_class: notebook_ipynb_json
  source_artifact_ref: nb.source.ipynb.imported_public.01
  source_fingerprint_ref: hash.nb.doc.imported_public.01.ipynb.v1
  authored_summary_label: Canonical `.ipynb` is preserved, but the structured editor cannot safely interpret one attachment/output class.

effective_preview:
  preview_representation_class: tombstone_preview
  effective_representation_ref: nb.preview.ipynb.unsupported_attachment.tombstone.v1
  effective_summary_label: Structured preview is unavailable; the surface refuses rewrite rather than guessing or dropping unknown fields.

round_trip:
  structured_round_trip_risk_class: round_trip_unavailable
  irreversibility_flag: false
  disclosure_flag_set: []
  reversal_hint_ref: nb.open.raw_json_fallback

raw_preservation:
  raw_bytes_posture: preserved_in_recovery_journal
  preserved_raw_artifact_ref: nb.preserve.raw.ipynb.unsupported_attachment.v1
  preservation_summary_label: Raw notebook bytes are preserved so the reviewer can inspect, export, or attach to support without rewriting.

rerun_requirement:
  intent_class: manual_recovery_required
  action_ref: action.nb.update_renderer_or_open_raw
  summary_label: Update the renderer or use raw/compare-only mode; structured round-trip is not proven for this attachment class.

apply_gate_class: refuse_rewrite

safe_next_actions:
  - action_class: open_authored_source
    action_label: Open the notebook as raw JSON (compare-only)
    destination_ref: nb.open.raw_json_fallback
  - action_class: keep_read_only
    action_label: Keep the notebook in review-only mode
    destination_ref: null
  - action_class: preserve_raw_copy
    action_label: Export or attach preserved raw notebook bytes as evidence
    destination_ref: nb.preserve.raw.ipynb.unsupported_attachment.v1

captured_at: "2026-05-06T21:38:40Z"

