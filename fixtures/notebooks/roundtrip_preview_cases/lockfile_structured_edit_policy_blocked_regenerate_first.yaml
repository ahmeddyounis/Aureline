# yaml-language-server: $schema=../../../schemas/notebooks/roundtrip_preview.schema.json
__fixture__:
  name: lockfile_structured_edit_policy_blocked_regenerate_first
  scenario: >-
    A user attempts to apply a structured edit to a lockfile. Admin
    policy blocks lockfile round-trip edits (generated artifact); the
    surface refuses rewrite and offers regenerate-first safe actions.
  seed_section: Lockfile policy blocked; regenerate-first
  exercised_classes:
    subject_class: structured_lockfile
    structured_round_trip_risk_class: round_trip_policy_blocked
    apply_gate_class: refuse_rewrite
    rebuild_intent_class: regenerate_required_before_authoritative_claims
  linked_docs:
    - docs/notebooks/notebook_trust_and_roundtrip_preview_contract.md
    - docs/architecture/generated_artifact_safe_edit_policy.md

roundtrip_preview_schema_version: 1
record_kind: structured_round_trip_preview_sheet_record
preview_id: rtpreview.lockfile.policy_blocked.v1

subject:
  subject_class: structured_lockfile
  subject_ref: ws.file.cargo_lock
  display_label: Structured preview (Cargo.lock; policy-blocked structured edits)
  artifact_class: structured_lockfile
  notebook_document_ref: null
  cell_id_ref: null
  output_id_ref: null

trust_posture:
  notebook_trust_rung: null
  per_cell_trust_overrides: []
  document_trust_state: document_trust_inherited_from_workspace
  kernel_trust_state: kernel_trust_unavailable_no_kernel
  output_trust_state: null
  widget_trust_state: null
  kernel_transport_class: null

authored_source:
  source_format_class: structured_lockfile_text
  source_artifact_ref: ws.source.cargo_lock.v1
  source_fingerprint_ref: hash.ws.cargo_lock.bytes.v1
  authored_summary_label: Lockfile is a generated artifact; direct structured edits are not the authoritative path under policy.

effective_preview:
  preview_representation_class: tombstone_preview
  effective_representation_ref: ws.preview.cargo_lock.policy_blocked.v1
  effective_summary_label: Structured apply is blocked by policy; preview is refusal-only with regenerate-first guidance.

round_trip:
  structured_round_trip_risk_class: round_trip_policy_blocked
  irreversibility_flag: false
  disclosure_flag_set:
    - policy_narrowed
  reversal_hint_ref: action.ws.regenerate_lockfile

raw_preservation:
  raw_bytes_posture: preserved_in_open_buffer
  preserved_raw_artifact_ref: null
  preservation_summary_label: No rewrite applies; raw preservation is not required for denial, but the authored lockfile remains available for compare and support evidence.

rerun_requirement:
  intent_class: regenerate_required_before_authoritative_claims
  action_ref: action.ws.regenerate_lockfile
  summary_label: Regenerate the lockfile from canonical inputs; do not edit lockfile structure directly.

apply_gate_class: refuse_rewrite

safe_next_actions:
  - action_class: rerun_or_regenerate
    action_label: Regenerate the lockfile from canonical inputs
    destination_ref: action.ws.regenerate_lockfile
  - action_class: open_authored_source
    action_label: Open the lockfile as raw text (read-only)
    destination_ref: ws.source.cargo_lock.v1
  - action_class: contact_admin_for_policy_review
    action_label: Contact admin to review policy if structured edits are required
    destination_ref: policy.ticket.lockfile_structured_edit_request

captured_at: "2026-05-06T21:38:10Z"

