# yaml-language-server: $schema=../../../schemas/notebooks/roundtrip_preview.schema.json
__fixture__:
  name: yaml_manifest_comments_dropped_compare_first
  scenario: >-
    A structured editor previews a YAML manifest round-trip where
    comments and formatting annotations will not survive. The surface
    requires compare-first review and preserves a raw copy before apply.
  seed_section: YAML manifest; comment/formatting loss; compare-first
  exercised_classes:
    subject_class: structured_manifest
    structured_round_trip_risk_class: lossy_metadata_only
    apply_gate_class: require_compare_first_review
    raw_bytes_posture: preserved_in_recovery_journal
  linked_docs:
    - docs/notebooks/notebook_trust_and_roundtrip_preview_contract.md
    - docs/editor/decode_recovery_and_save_consequence_contract.md

roundtrip_preview_schema_version: 1
record_kind: structured_round_trip_preview_sheet_record
preview_id: rtpreview.manifest.yaml.comments_dropped.v1

subject:
  subject_class: structured_manifest
  subject_ref: ws.file.tool_config_yaml
  display_label: Structured preview (tool config YAML; comments dropped)
  artifact_class: null
  notebook_document_ref: null
  cell_id_ref: null
  output_id_ref: null

trust_posture:
  notebook_trust_rung: null
  per_cell_trust_overrides: []
  document_trust_state: document_trust_inherited_from_workspace
  kernel_trust_state: kernel_trust_unavailable_no_kernel
  output_trust_state: null
  widget_trust_state: null
  kernel_transport_class: null

authored_source:
  source_format_class: yaml_manifest
  source_artifact_ref: ws.source.tool_config_yaml.v1
  source_fingerprint_ref: hash.ws.tool_config_yaml.bytes.v1
  authored_summary_label: Authored YAML includes comments and formatting notes that do not round-trip through the structured form.

effective_preview:
  preview_representation_class: normalised_preview
  effective_representation_ref: ws.preview.tool_config_yaml.structured.v1
  effective_summary_label: Preview shows the resolved structured YAML values; comment loss is explicitly disclosed.

round_trip:
  structured_round_trip_risk_class: lossy_metadata_only
  irreversibility_flag: false
  disclosure_flag_set:
    - representation_normalised
  affected_fields:
    - /
  reversal_hint_ref: ws.restore.raw.tool_config_yaml.before_apply.v1

raw_preservation:
  raw_bytes_posture: preserved_in_recovery_journal
  preserved_raw_artifact_ref: ws.preserve.raw.tool_config_yaml.before_apply.v1
  preservation_summary_label: Raw YAML bytes are preserved in a recovery journal so comment/formatting loss is reversible by restore.

rerun_requirement:
  intent_class: none_required
  action_ref: null
  summary_label: No rerun required; the preview is about serialization fidelity.

apply_gate_class: require_compare_first_review

safe_next_actions:
  - action_class: open_compare_diff
    action_label: Compare YAML before apply (comments/formatting loss highlighted)
    destination_ref: ws.diff.tool_config_yaml.v1
  - action_class: preserve_raw_copy
    action_label: Preserve raw YAML bytes in a recovery journal
    destination_ref: ws.preserve.raw.tool_config_yaml.before_apply.v1
  - action_class: open_authored_source
    action_label: Open authored YAML text
    destination_ref: ws.source.tool_config_yaml.v1

captured_at: "2026-05-06T21:37:10Z"

