# Offboarding exit packet fixtures

Worked cases for `schemas/governance/offboarding_exit_packet.schema.json`.

These fixtures demonstrate that the exit packet references sibling
export families (usage export, portability, support bundles, receipts)
by opaque id and does not embed their payloads.

