# Standards adoption evidence bundle (JSON Schema Draft 2020-12).

schema_version: 1

evidence_case_id: standards_evidence.json_schema_2020_12
standard_row_ref: artifacts/governance/standards_matrix.yaml#standard.json_schema_2020_12

owner:
  lane: governance_and_policy
  dri: "@ahmeddyounis"

claim_vectors:
  - claim_vector_id: standards_claim.validator_and_fixtures
    claim_summary: >
      Claim “JSON schemas are Draft 2020-12 and validated as such”.
    minimum_evidence_bundle:
      - evidence_path_class: upstream_validator_harness
        requirement: >
          A Draft 2020-12 validator-grade harness exists and is run against the
          repository schema set and representative instance fixtures.
        artifact_refs:
          - ci/contract_validation.sh
          - tools/validate_contract_example_pack.py
      - evidence_path_class: example_export_fixture
        requirement: >
          At least one representative schema + instance fixture pair exists and is
          validated under Draft 2020-12 semantics.
        artifact_refs:
          - schemas/governance/decision_index.schema.json
          - fixtures/governance/waiver_cases/active_within_expiry_single_maintainer_backup.yaml

