__fixture__:
  name: oss_bug_public_tracker
  scenario: >
    A contributor hits a reproducible editor crash on a specific
    language-server response shape. No live secret material, no
    device-bound content. Contributor files in the public issue
    tracker with a redacted log excerpt and the exact-build id.
  contract_sections:
    - issue_classes.oss_bug
    - escalation_paths.public_issue_tracker
  pairs_with: []

record_kind: issue_route_example
schema_version: 1

fixture_id: oss_bug_public_tracker
issue_class: oss_bug
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: internal
status: seeded

resolved_routing:
  default_route_class: public_issue_tracker
  privacy_class: public
  disclosure_class: public_immediate
  public_summary_expectation: recommended
  redaction_class: field_safe_default
  owning_forum: architecture_council

resolved_escalation_chain:
  primary:
    - architecture_council
  topical: []
  release_gate:
    - release_council

filing_contract:
  intake_surface: repo_issue_tracker
  required_report_fields:
    - reproducer_steps
    - exact_build_identity_ref
    - deployment_profile_id
    - redacted_log_excerpt
  forbidden_report_fields:
    - raw_credential_bytes
    - raw_partner_identifier
    - live_support_bundle_body

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Report is public at filing.
  - rule_ref: rule:security_route_owns_raw_secret_payloads
    outcome: satisfied
    evidence: Redacted log excerpt carries no raw secret material.
