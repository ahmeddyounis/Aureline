__fixture__:
  name: supportability_issue_public_tracker
  scenario: >
    A contributor reports that the Project Doctor "runtime probe"
    returns an unhelpful label for a known safe-mode condition. The
    report is about the probe prose and recovery-ladder step ids; it
    carries no device, account, or workspace content. Report lands
    on the public tracker.
  contract_sections:
    - issue_classes.supportability_issue
    - escalation_paths.public_issue_tracker
  pairs_with:
    - supportability_escalation_private_support

record_kind: issue_route_example
schema_version: 1

fixture_id: supportability_issue_public_tracker
issue_class: supportability_issue
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: internal
status: seeded

resolved_routing:
  default_route_class: public_issue_tracker
  privacy_class: public
  disclosure_class: public_on_fix
  public_summary_expectation: recommended
  redaction_class: field_safe_with_route_metadata
  owning_forum: product_scope_review

resolved_escalation_chain:
  primary:
    - product_scope_review
  topical:
    - architecture_council
    - open_community_sync
  release_gate:
    - release_council

filing_contract:
  intake_surface: repo_issue_tracker
  required_report_fields:
    - doctor_probe_id
    - recovery_ladder_step_ref
    - exact_build_identity_ref
  forbidden_report_fields:
    - raw_support_bundle_body
    - account_identifier

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Report is public at filing.
