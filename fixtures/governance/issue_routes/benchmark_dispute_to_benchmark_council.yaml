__fixture__:
  name: benchmark_dispute_to_benchmark_council
  scenario: >
    An external reviewer disputes a published benchmark number on a
    reference hardware class, citing a corpus-validity concern on the
    seeded typing corpus. The dispute lands in the benchmark-council
    queue; performance council owns the adjudication; the resolution
    surfaces in release evidence and docs.
  contract_sections:
    - issue_classes.benchmark_dispute
    - escalation_paths.benchmark_council_queue
  pairs_with: []

record_kind: issue_route_example
schema_version: 1

fixture_id: benchmark_dispute_to_benchmark_council
issue_class: benchmark_dispute
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: public
status: seeded

resolved_routing:
  default_route_class: benchmark_council_queue
  privacy_class: public
  disclosure_class: public_on_fix
  public_summary_expectation: required
  redaction_class: field_safe_default
  owning_forum: performance_council

resolved_escalation_chain:
  primary:
    - performance_council
  topical:
    - architecture_council
    - compatibility_ecosystem_review
    - open_community_sync
  release_gate:
    - release_council
    - shiproom_executive_scope_review

filing_contract:
  intake_surface: benchmark_council_intake
  required_report_fields:
    - benchmark_id
    - corpus_id
    - protected_fitness_id
    - hardware_class_id
    - claim_manifest_row_ref_or_null
  forbidden_report_fields:
    - raw_credential_bytes
    - partner_identifier_without_consent

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Dispute is public at filing.
  - rule_ref: rule:release_gate_co_sign
    outcome: satisfied
    evidence: >
      Release-gate forums are named in the escalation chain; the
      release bearing the adjudication outcome must record their
      decision.
