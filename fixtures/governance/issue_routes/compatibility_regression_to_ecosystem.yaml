__fixture__:
  name: compatibility_regression_to_ecosystem
  scenario: >
    A previously-working TextMate-grammar importer regresses on a
    specific grammar shape after a stable release. No partner NDA,
    no sensitive content. Report lands on the public tracker with
    importer / bridge / SDK surface ids; compatibility ecosystem
    review owns the queue and co-signs the release-packet outcome.
  contract_sections:
    - issue_classes.compatibility_regression
    - escalation_paths.public_issue_tracker
  pairs_with: []

record_kind: issue_route_example
schema_version: 1

fixture_id: compatibility_regression_to_ecosystem
issue_class: compatibility_regression
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: public
status: seeded

resolved_routing:
  default_route_class: public_issue_tracker
  privacy_class: public
  disclosure_class: public_on_fix
  public_summary_expectation: required
  redaction_class: field_safe_with_route_metadata
  owning_forum: compatibility_ecosystem_review

resolved_escalation_chain:
  primary:
    - compatibility_ecosystem_review
  topical:
    - architecture_council
    - product_scope_review
  release_gate:
    - release_council
    - shiproom_executive_scope_review

filing_contract:
  intake_surface: repo_issue_tracker
  required_report_fields:
    - importer_or_bridge_or_sdk_id
    - prior_version_behaviour
    - current_version_behaviour
    - affected_archetype_id
    - exact_build_identity_ref
  forbidden_report_fields:
    - partner_identifier_without_consent
    - live_support_bundle_body

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Report is public at filing.
  - rule_ref: rule:release_gate_co_sign
    outcome: satisfied
    evidence: >
      Release-gate forums are named in the escalation chain; the
      release carrying the fix must record their outcome.
