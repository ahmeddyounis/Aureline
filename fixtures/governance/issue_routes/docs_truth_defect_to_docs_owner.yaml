__fixture__:
  name: docs_truth_defect_to_docs_owner
  scenario: >
    A contributor finds that a public docs page claims a behaviour
    ("safe mode never touches extensions") that is contradicted by
    the observed runtime. They file on the public tracker, quote the
    docs URL and the claim-manifest row, and request either a
    correction, a claim-manifest addition, or a withdrawal. Product
    scope review owns the correction programme.
  contract_sections:
    - issue_classes.docs_truth_defect
    - escalation_paths.public_issue_tracker
  pairs_with: []

record_kind: issue_route_example
schema_version: 1

fixture_id: docs_truth_defect_to_docs_owner
issue_class: docs_truth_defect
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: public
status: seeded

resolved_routing:
  default_route_class: public_issue_tracker
  privacy_class: public
  disclosure_class: public_immediate
  public_summary_expectation: required
  redaction_class: field_safe_default
  owning_forum: product_scope_review

resolved_escalation_chain:
  primary:
    - product_scope_review
  topical:
    - architecture_council
    - open_community_sync
  release_gate:
    - release_council

filing_contract:
  intake_surface: repo_issue_tracker
  required_report_fields:
    - docs_url_or_path
    - claim_manifest_row_ref_or_null
    - observed_behaviour_description
  forbidden_report_fields:
    - raw_credential_bytes
    - partner_identifier

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Report is public at filing.
