__fixture__:
  name: perf_regression_to_performance_council
  scenario: >
    A benchmark run reports a 17% regression on the protected
    keystroke-to-paint fitness function against the seeded
    corpus on the reference hardware class. Report lands on the
    public tracker; performance council owns triage and must record
    a scorecard or packet outcome.
  contract_sections:
    - issue_classes.perf_regression
    - escalation_paths.public_issue_tracker
  pairs_with: []

record_kind: issue_route_example
schema_version: 1

fixture_id: perf_regression_to_performance_council
issue_class: perf_regression
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: internal
status: seeded

resolved_routing:
  default_route_class: public_issue_tracker
  privacy_class: public
  disclosure_class: public_immediate
  public_summary_expectation: required
  redaction_class: field_safe_with_route_metadata
  owning_forum: performance_council

resolved_escalation_chain:
  primary:
    - performance_council
  topical:
    - architecture_council
  release_gate:
    - release_council
    - shiproom_executive_scope_review

filing_contract:
  intake_surface: repo_issue_tracker
  required_report_fields:
    - benchmark_id
    - fitness_function_id
    - corpus_id
    - hardware_class_id
    - budget_delta
    - exact_build_identity_ref
  forbidden_report_fields:
    - raw_credential_bytes
    - live_support_bundle_body

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Report is public at filing.
  - rule_ref: rule:release_gate_co_sign
    outcome: satisfied
    evidence: >
      Release-gate forums (release_council,
      shiproom_executive_scope_review) are present in the escalation
      chain; a release bearing the regression cannot close without
      their recorded outcome.
