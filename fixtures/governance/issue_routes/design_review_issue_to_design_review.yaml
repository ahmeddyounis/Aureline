__fixture__:
  name: design_review_issue_to_design_review
  scenario: >
    A contributor reports that a launch-surface button uses a raw
    hex colour instead of the seeded design token on the high-contrast
    theme. Not an accessibility blocker by itself; a design-system
    defect. Report lands on the public tracker; accessibility review
    owns the queue because design-system integrity sits under the
    same lane as accessibility.
  contract_sections:
    - issue_classes.design_review_issue
    - escalation_paths.public_issue_tracker
  pairs_with:
    - accessibility_defect_to_accessibility_review

record_kind: issue_route_example
schema_version: 1

fixture_id: design_review_issue_to_design_review
issue_class: design_review_issue
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: public
status: seeded

resolved_routing:
  default_route_class: public_issue_tracker
  privacy_class: public
  disclosure_class: public_on_fix
  public_summary_expectation: recommended
  redaction_class: field_safe_default
  owning_forum: accessibility_review

resolved_escalation_chain:
  primary:
    - accessibility_review
  topical:
    - architecture_council
    - product_scope_review
  release_gate:
    - release_council

filing_contract:
  intake_surface: repo_issue_tracker
  required_report_fields:
    - component_or_token_id
    - surface_id
    - theme_or_locale_context
  forbidden_report_fields:
    - raw_credential_bytes
    - live_support_bundle_body

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Report is public at filing.
