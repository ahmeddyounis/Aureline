__fixture__:
  name: waiver_request_governance_packet
  scenario: >
    A lane owner opens a waiver request against a protected fitness
    function because a hardware-class corpus is temporarily
    unavailable. The request lands as a governance-packet PR using
    the waiver template; architecture council approves and the
    performance council co-signs. Waiver body is public; correction
    intent and expiry are public.
  contract_sections:
    - issue_classes.waiver_request
    - escalation_paths.governance_packet_queue
  pairs_with: []

record_kind: issue_route_example
schema_version: 1

fixture_id: waiver_request_governance_packet
issue_class: waiver_request
owner_dri: "@ahmeddyounis"
owning_lane: governance_packets
visibility_class: public
status: seeded

resolved_routing:
  default_route_class: governance_packet_queue
  privacy_class: public
  disclosure_class: public_immediate
  public_summary_expectation: required
  redaction_class: field_safe_default
  owning_forum: architecture_council

resolved_escalation_chain:
  primary:
    - architecture_council
  topical:
    - performance_council
    - security_trust_review
    - accessibility_review
    - compatibility_ecosystem_review
    - product_scope_review
  release_gate:
    - release_council

filing_contract:
  intake_surface: governance_packet_pr
  required_report_fields:
    - protected_lane_ref
    - mitigation
    - expiry
    - correction_intent
    - decision_index_row_ref
  forbidden_report_fields:
    - pr_comment_only_request
    - oral_agreement_only_record

disclosure_transition_cited: null
consistency_rule_checks:
  - rule_ref: rule:private_class_never_silently_public
    outcome: not_applicable
    evidence: Waiver is public at filing.
