# yaml-language-server: $schema=../../../schemas/governance/deprecation_metadata.schema.json
__fixture__:
  name: command_hidden_alias_deprecated
  scenario: >
    Legacy CLI and automation traffic still sends the old open-folder
    alias. The parser resolves it directly to the canonical command id,
    but the alias has been deprecated and is no longer listed in general
    docs/help. Machine-readable metadata, support export, and the
    compatibility report still disclose the alias until the retirement
    horizon closes.
  doc_sections:
    - Canonical rows and alias rows
    - Notice and visibility rules

record_kind: interface_lifecycle_metadata_row
interface_lifecycle_schema_version: 1
surface_contract_ref: artifacts/governance/stable_surface_inventory.yaml#command_plane.command_descriptor_and_invocation_session
interface_family: command_id
row_role: alias
interface_id: alias:workspace.open_folder:legacy_file_open_folder
canonical_interface_id: cmd:workspace.open_folder
interface_lifecycle_state: deprecated
introduced_in_version: "0.8"
non_experimental_since_version: "1.0"
deprecated_in_version: "1.4"
removal_target_version: "1.6"
retired_in_version: null
replacement_posture: replacement_required
replacement_interface_ids:
  - cmd:workspace.open_folder
alias_kind: hidden_compat_alias
help_visibility: hidden_from_docs_and_help
required_notice_surfaces:
  - machine_readable_metadata
  - compatibility_report
  - support_export
support_window:
  rule_mode: all_of
  windows:
    - value: 2
      unit: minor_releases
    - value: 12
      unit: months
  policy_source_ref: .t2/docs/Aureline_PRD.md §5.54 Stable deprecation floor
  notes: Hidden compatibility aliases inherit the stable command-id overlap floor.
migration_guide_ref: docs/governance/interface_lifecycle_policy.md
compatibility_notes:
  - "Parser resolution stays one hop: the alias resolves directly to cmd:workspace.open_folder."
  - General docs/help hides the alias, but compatibility and support surfaces keep disclosing it until retirement.
