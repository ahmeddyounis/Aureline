# yaml-language-server: $schema=../../../schemas/governance/deprecation_metadata.schema.json
__fixture__:
  name: record_class_id_deprecated
  scenario: >
    A record-class id is being retired in favor of a narrower canonical
    class. Support/export and boundary-manifest lanes need the
    replacement metadata so they do not silently relabel historical
    packets under the new class id.
  doc_sections:
    - Canonical metadata row
    - Surface-family mapping

record_kind: interface_lifecycle_metadata_row
interface_lifecycle_schema_version: 1
surface_contract_ref: artifacts/governance/stable_surface_inventory.yaml#governance.record_class_registry_rows
interface_family: record_class_id
row_role: canonical
interface_id: support_bundle_archive_legacy
canonical_interface_id: support_bundle_archive_legacy
interface_lifecycle_state: deprecated
introduced_in_version: "1.0"
non_experimental_since_version: "1.0"
deprecated_in_version: "1.5"
removal_target_version: "1.7"
retired_in_version: null
replacement_posture: replacement_required
replacement_interface_ids:
  - support_bundle_archive
alias_kind: null
help_visibility: visible_on_explicit_lookup_only
required_notice_surfaces:
  - release_notes
  - docs_help
  - machine_readable_metadata
  - support_export
  - migration_guide
support_window:
  rule_mode: all_of
  windows:
    - value: 2
      unit: minor_releases
    - value: 12
      unit: months
  policy_source_ref: .t2/docs/Aureline_PRD.md §5.54 Stable surface deprecation metadata
  notes: Record-class ids are stable support/export joins and must preserve overlap before retirement.
migration_guide_ref: docs/governance/record_class_governance.md
compatibility_notes:
  - Historical support bundles continue to cite support_bundle_archive_legacy until they are re-emitted under the replacement class.
  - The replacement row support_bundle_archive must exist before any boundary or support surface hides the legacy class.
