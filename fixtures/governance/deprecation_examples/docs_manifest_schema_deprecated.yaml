# yaml-language-server: $schema=../../../schemas/governance/deprecation_metadata.schema.json
__fixture__:
  name: docs_manifest_schema_deprecated
  scenario: >
    The docs-pack manifest schema family is moving to a successor URI.
    Publishers and validators have to disclose both the replacement and
    the overlap window so older tooling can fail closed instead of
    silently interpreting the new fields under the old schema id.
  doc_sections:
    - Canonical metadata row
    - Replacement-chain rules

record_kind: interface_lifecycle_metadata_row
interface_lifecycle_schema_version: 1
surface_contract_ref: artifacts/governance/stable_surface_inventory.yaml#docs.docs_pack_manifest
interface_family: docs_manifest_schema
row_role: canonical
interface_id: https://aureline.dev/schemas/docs/docs_pack_manifest.v1.schema.json
canonical_interface_id: https://aureline.dev/schemas/docs/docs_pack_manifest.v1.schema.json
interface_lifecycle_state: deprecated
introduced_in_version: "1.0"
non_experimental_since_version: "1.0"
deprecated_in_version: "1.6"
removal_target_version: "1.8"
retired_in_version: null
replacement_posture: replacement_required
replacement_interface_ids:
  - https://aureline.dev/schemas/docs/docs_pack_manifest.v2.schema.json
alias_kind: null
help_visibility: visible_on_explicit_lookup_only
required_notice_surfaces:
  - release_notes
  - docs_help
  - machine_readable_metadata
  - compatibility_report
  - migration_guide
support_window:
  rule_mode: all_of
  windows:
    - value: 2
      unit: minor_releases
    - value: 12
      unit: months
  policy_source_ref: .t2/docs/Aureline_PRD.md §5.54 Stable surface deprecation metadata
  notes: Schema-family deprecations still publish machine-readable notice even when the UI only renders them on explicit lookup.
migration_guide_ref: docs/docs/docs_pack_manifest_contract.md
compatibility_notes:
  - v1 readers remain supported during the overlap window, but publishers must emit the v2 family before the v1 retirement target.
  - Tooling may not treat the v2 schema as an in-place rewrite of the v1 URI.
