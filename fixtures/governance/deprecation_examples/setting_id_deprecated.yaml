# yaml-language-server: $schema=../../../schemas/governance/deprecation_metadata.schema.json
__fixture__:
  name: setting_id_deprecated
  scenario: >
    A stable setting id is being retired in favor of a more precise
    replacement. The canonical id remains visible in docs/help and
    release notes while profile import/export and migration tooling
    redirect writes to the replacement id during the overlap window.
  doc_sections:
    - Lifecycle-state rules
    - Support-window rules

record_kind: interface_lifecycle_metadata_row
interface_lifecycle_schema_version: 1
surface_contract_ref: artifacts/governance/stable_surface_inventory.yaml#settings.setting_ids_and_effective_values
interface_family: setting_id
row_role: canonical
interface_id: workspace.restore.auto_open_behavior
canonical_interface_id: workspace.restore.auto_open_behavior
interface_lifecycle_state: deprecated
introduced_in_version: "1.0"
non_experimental_since_version: "1.1"
deprecated_in_version: "1.5"
removal_target_version: "1.7"
retired_in_version: null
replacement_posture: replacement_required
replacement_interface_ids:
  - workspace.startup.restore_behavior
alias_kind: null
help_visibility: visible_in_docs_and_help
required_notice_surfaces:
  - release_notes
  - docs_help
  - machine_readable_metadata
  - migration_guide
support_window:
  rule_mode: all_of
  windows:
    - value: 2
      unit: minor_releases
    - value: 12
      unit: months
  policy_source_ref: .t2/docs/Aureline_PRD.md §5.54 Stable deprecation floor
  notes: null
migration_guide_ref: docs/state/migration_and_restore_playbook.md
compatibility_notes:
  - Existing profile imports may rewrite the deprecated id to workspace.startup.restore_behavior during compatible restore.
  - The legacy id stays readable for the published overlap window and must not be repurposed afterward.
