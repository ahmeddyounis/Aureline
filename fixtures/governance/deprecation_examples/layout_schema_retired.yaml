# yaml-language-server: $schema=../../../schemas/governance/deprecation_metadata.schema.json
__fixture__:
  name: layout_schema_retired
  scenario: >
    An older pane-tree schema family completed its deprecation window
    and is now retired. The row remains queryable so restore tooling and
    support export can explain why older bundles degrade to evidence-only
    or manual-review restore paths.
  doc_sections:
    - Lifecycle-state rules
    - Replacement-chain rules

record_kind: interface_lifecycle_metadata_row
interface_lifecycle_schema_version: 1
surface_contract_ref: artifacts/governance/stable_surface_inventory.yaml#state.portable_profile_and_layout_restore
interface_family: layout_schema
row_role: canonical
interface_id: https://aureline.dev/schemas/workspace/pane_tree.v0.schema.json
canonical_interface_id: https://aureline.dev/schemas/workspace/pane_tree.v0.schema.json
interface_lifecycle_state: retired
introduced_in_version: "0.9"
non_experimental_since_version: "1.0"
deprecated_in_version: "1.4"
removal_target_version: "2.0"
retired_in_version: "2.0"
replacement_posture: replacement_required
replacement_interface_ids:
  - https://aureline.dev/schemas/workspace/pane_tree.schema.json
alias_kind: null
help_visibility: visible_on_explicit_lookup_only
required_notice_surfaces:
  - release_notes
  - docs_help
  - machine_readable_metadata
  - compatibility_report
  - migration_guide
support_window:
  rule_mode: all_of
  windows:
    - value: 2
      unit: major_releases
  policy_source_ref: .t2/docs/Aureline_PRD.md §9.10 Workspace and profile readability
  notes: Historical row retained so restore tooling can prove the retirement followed the published deprecation window.
migration_guide_ref: docs/workspace/layout_serialization_contract.md
compatibility_notes:
  - v0 bundles no longer hydrate as live topology; restore tooling routes them through the documented downgrade path.
  - The replacement schema URI is the current pane-tree family and is the only canonical target for new exports.
