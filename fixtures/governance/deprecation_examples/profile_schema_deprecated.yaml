# yaml-language-server: $schema=../../../schemas/governance/deprecation_metadata.schema.json
__fixture__:
  name: profile_schema_deprecated
  scenario: >
    Portable-profile export is moving to a successor schema family. The
    metadata row uses a stronger readability promise than the generic
    stable default because profile files must stay readable across
    multiple major versions.
  doc_sections:
    - Support-window rules
    - Surface-family mapping

record_kind: interface_lifecycle_metadata_row
interface_lifecycle_schema_version: 1
surface_contract_ref: artifacts/governance/stable_surface_inventory.yaml#state.portable_profile_and_layout_restore
interface_family: profile_schema
row_role: canonical
interface_id: https://aureline.dev/schemas/profile/portable_profile.v1.schema.json
canonical_interface_id: https://aureline.dev/schemas/profile/portable_profile.v1.schema.json
interface_lifecycle_state: deprecated
introduced_in_version: "1.0"
non_experimental_since_version: "1.0"
deprecated_in_version: "2.0"
removal_target_version: "3.0"
retired_in_version: null
replacement_posture: replacement_required
replacement_interface_ids:
  - https://aureline.dev/schemas/profile/portable_profile.v2.schema.json
alias_kind: null
help_visibility: visible_on_explicit_lookup_only
required_notice_surfaces:
  - release_notes
  - docs_help
  - machine_readable_metadata
  - migration_guide
support_window:
  rule_mode: all_of
  windows:
    - value: 2
      unit: major_releases
  policy_source_ref: .t2/docs/Aureline_PRD.md §9.10 Workspace and profile readability
  notes: Profile exports must remain readable for at least two major versions before the older schema family retires.
migration_guide_ref: docs/state/migration_and_restore_playbook.md
compatibility_notes:
  - Compatible restore may translate v1 profile bodies through declared equivalence-map rows while the overlap window remains open.
  - The v1 schema URI remains historical metadata even after retirement and cannot be reused for the v2 meaning.
