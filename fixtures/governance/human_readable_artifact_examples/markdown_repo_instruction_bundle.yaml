# yaml-language-server: $schema=../../../schemas/governance/human_readable_artifact_family.schema.json
__fixture__:
  name: markdown_repo_instruction_bundle
  scenario: >
    Example registry row for a repo-authored Markdown instruction bundle that is
    freely editable and reviewed in VCS.
  doc_sections:
    - How to use this registry
    - Worked examples

schema_version: 1

artifact_families:
  - id: repo_instruction_bundle_markdown
    title: Repo instruction bundle (agent and policy guidance)
    description: Example row for Markdown instruction bundles consumed by assist tooling.
    owner_class: workspace
    authorship_class: workspace_authored
    editability_class: freely_editable
    format_class: markdown
    schema_home: null
    comment_policy: preserve_verbatim
    ordering_policy: stable_order_preserved
    unknown_field_policy: preserve_verbatim
    vcs_review_posture: expected_in_vcs
    portability_expectation: shared_workspace_only
    canonical_locations:
      - "AGENTS.md"
      - "CLAUDE.md"
      - "**/AGENTS.md"
      - "**/CLAUDE.md"
    policy_refs:
      - docs/ai/prompt_injection_and_taint_contract.md
      - docs/ai/context_assembly_contract.md
      - docs/governance/human_readable_artifact_registry.md
    notes: |
      Illustrative instruction bundle excerpt:

      ```markdown
      # Repository instructions

      - Follow the repo's architecture and safety contracts.
      - Avoid inventing format rules; cite the shared artifact registry.
      ```

