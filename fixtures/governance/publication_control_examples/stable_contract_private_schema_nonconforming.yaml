# yaml-language-server: $schema=../../../schemas/governance/publication_control_example.schema.json
__fixture__:
  name: stable_contract_private_schema_nonconforming
  scenario: >
    A surface claims stability for an external contract, but the schema
    and samples are private-only (or available only via a vendor portal).
    This is non-conforming because ecosystem participation would require
    a hidden contract and the project could not honestly claim open or
    self-host friendly posture for the surface.
  contract_sections:
    - "Open-vs-paid matrix :: prohibited hidden prerequisites"
    - "Public-interface stability :: stable surfaces publish schemas"

record_kind: publication_control_example
publication_control_example_schema_version: 1
example_id: publication_control_example:stable_contract.private_schema.nonconforming
title: Stable contract with private-only schema/samples (nonconforming)
artifact_family_class: sdks_schemas_wit_protocol_samples

controls:
  license_lane_classes:
    - apache_2_0_public_source
  code_visibility_requirement: public_source_optional
  spec_visibility_requirement: public_spec_required_for_stable
  stable_surface_schema_docs_requirement: public_schema_and_docs_required_for_stable
  sample_availability_requirement: public_samples_required_for_stable
  issue_routing_requirement: mixed_public_private_allowed_with_matrix

evidence_refs:
  - docs/governance/public_interface_stability_matrix.md
  - docs/governance/drift_blocking_rules.md

expected_verdict: nonconforming

