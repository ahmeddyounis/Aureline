{
  "case_name": "macro_recorder_stable",
  "expect": {
    "entrypoint_tokens": [
      "notebook",
      "task_test_debug",
      "request_api",
      "package",
      "incident",
      "ai_assistant"
    ],
    "expected_finding_kinds": [],
    "is_stable": true,
    "promotion_state": "stable",
    "validation_finding_count": 0
  },
  "mutation": "none",
  "record_kind": "m5_macro_recorder_case",
  "scenario": "Every entrypoint binds a session panel whose sessions declare target and storage scope, recorded macros stay profile-local, unsupported commands are flagged and block save, replay fails closed on a context or scope mismatch, repository content defines no macro, cross-scope promotion is explicit, and captures stay UI-only, so the packet is stable.",
  "schema_version": 1
}
