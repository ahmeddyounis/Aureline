{
  "description": "A host-translating pack must reuse stable host message ids and never embed a locale tag in an id.",
  "expected_codes": [
    "strings.unknown_host_id",
    "strings.id_carries_locale_tag"
  ]
}
