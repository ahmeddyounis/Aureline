# Shared Terminal Debug Control Plane Fixtures

These fixtures cover the exposed shared terminal, debugger, presenter/follow, support-follow, and restore/rejoin lanes qualified by `artifacts/collab/m4/shared-terminal-debug-control-plane.json`.

The canonical packet is the complete truth source. Each fixture records the case coverage and points back to the control id in the packet.
