# yaml-language-server: $schema=../../../schemas/execution/terminal_export_review.schema.json
__fixture__:
  name: restore_no_rerun_attestation_fresh_session
  scenario: >
    Worked restore_no_rerun_attestation_record showing the only path
    back to write authority on a restored pane: the user explicitly
    initiates a fresh terminal session, the host mints a fresh
    terminal_session_record_id and a fresh command-dispatch descriptor,
    and the attestation cites both. restore_authority_state_class =
    fresh_session_minted_with_explicit_user_initiation;
    user_impact_label_class =
    live_session_continued_with_explicit_user_initiation;
    auto_rerun_forbidden remains true (the const constraint enforces
    this regardless of any other field); input_admission_forbidden_
    until_fresh_session and clipboard_bridge_admission_forbidden_
    until_fresh_session may resolve to false on this row only.
  exercised_classes_summary:
    record_kind: restore_no_rerun_attestation_record
    restore_authority_state_class: fresh_session_minted_with_explicit_user_initiation
    auto_rerun_forbidden: true
    input_admission_forbidden_until_fresh_session: false
    clipboard_bridge_admission_forbidden_until_fresh_session: false
    user_impact_label_class: live_session_continued_with_explicit_user_initiation
    redaction_class: metadata_and_hashes_only

record_kind: restore_no_rerun_attestation_record
terminal_export_review_schema_version: 1
attestation_id: execution.terminal.restore_no_rerun_attestation.fresh_session.0001

prior_terminal_session_record_ref: execution.terminal_session.restored_transcript.evidence_only.0001
prior_terminal_session_restore_metadata_ref: terminal.protocol.session_restore_metadata.evidence_only.0001

restore_authority_state_class: fresh_session_minted_with_explicit_user_initiation
fresh_terminal_session_record_ref: execution.terminal_session.fresh_after_restore.zsh.full_signals
fresh_command_dispatch_descriptor_ref: command_dispatch.descriptor.user_initiated_fresh_session

auto_rerun_forbidden: true
input_admission_forbidden_until_fresh_session: false
clipboard_bridge_admission_forbidden_until_fresh_session: false

user_impact_label_class: live_session_continued_with_explicit_user_initiation

audit_event_refs:
  - audit_event.terminal_export_review.restore_no_rerun_attestation_published
  - audit_event.terminal_export_review.restore_no_rerun_attestation_fresh_session_minted_under_user_initiation

denial_reason_class: null

captured_at: "2026-04-25T12:20:00Z"
redaction_class: metadata_and_hashes_only
