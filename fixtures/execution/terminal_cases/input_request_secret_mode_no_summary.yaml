# yaml-language-server: $schema=../../../schemas/execution/command_boundary_marker.schema.json
__fixture__:
  name: input_request_secret_mode_no_summary
  scenario: >
    Worked input_request_record for a secret-mode prompt opened on
    behalf of a running command. input_request_kind_class =
    interactive_secret_mode_no_echo_request, secret_mode_class =
    secret_mode_no_echo_no_transcript_capture (the schema's allOf
    gate requires this class to cite at least one credential-handle
    class ref AND resolve result_summary_class to no_summary_secret_
    or_redacted). The prompt opened with a typed monotonic deadline
    expires_at; the user submitted the secret and the prompt closed
    with result_user_submitted_secret_no_summary — only the
    input-request id and the typed result class project, never the
    entered bytes.
  exercised_classes_summary:
    record_kind: input_request_record
    input_request_kind_class: interactive_secret_mode_no_echo_request
    secret_mode_class: secret_mode_no_echo_no_transcript_capture
    result_class: result_user_submitted_secret_no_summary
    result_summary_class: no_summary_secret_or_redacted
    redaction_class: metadata_and_hashes_only

record_kind: input_request_record
command_boundary_marker_schema_version: 1
input_request_id: execution.terminal.input_request.secret_mode_no_summary.0001

terminal_session_record_ref: execution.terminal_session.live_local_shell.zsh.full_signals
command_boundary_marker_ref: execution.terminal.boundary.live_local_shell.zsh.command.0001

input_request_kind_class: interactive_secret_mode_no_echo_request
secret_mode_class: secret_mode_no_echo_no_transcript_capture
trust_state_ref_at_open: trust_state.workspace_trusted.local_desktop
policy_source_ref_at_open: admin_policy_source.foundations.secret_broker

credential_handle_class_refs:
  - secret_broker.credential_handle_class.api_token_bearer

opened_at: "2026-04-25T12:00:30Z"
expires_at: "2026-04-25T12:01:00Z"
result_class: result_user_submitted_secret_no_summary
result_summary_class: no_summary_secret_or_redacted
closed_at: "2026-04-25T12:00:45Z"

audit_event_refs:
  - audit_event.input_request.opened
  - audit_event.input_request.closed_with_typed_result

denial_reason_class: null
redaction_class: metadata_and_hashes_only
