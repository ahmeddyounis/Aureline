# yaml-language-server: $schema=../../../schemas/execution/test_run_summary.schema.json
__fixture__:
  name: snapshot_update_review
  scenario: >
    Worked, pre-implementation snapshot_review_record for a
    pre-apply preview of an incoming snapshot / golden update.
    snapshot_review_kind_class = pre_apply_preview_required;
    snapshot_baseline_identity_class = vcs_committed_baseline (the
    prior baseline lives in the VCS); snapshot_change_summary pins
    the file-count truth (1 added, 0 removed, 4 updated minor, 1
    updated major, 0 redaction-only, 7 unchanged, 0 unreviewable, 13
    total); snapshot_review_decision_class = review_pending_no
    _decision (no apply has been admitted yet). baseline_artifact
    _event_ref pins the prior bytes; proposed_artifact_event_ref
    pins the proposed bytes; rollback_path_ref is non-null because
    the baseline is non-first-capture; the contract guarantee is
    that an apply decision MUST cite a command-dispatch descriptor
    and a typed rollback path. Auto-applying without this preview
    would emit the typed denial under snapshot_review_record_must
    _not_admit_silent_baseline_overwrite.
  exercised_classes_summary:
    record_kind: snapshot_review_record
    snapshot_review_kind_class: pre_apply_preview_required
    snapshot_baseline_identity_class: vcs_committed_baseline
    snapshot_review_decision_class: review_pending_no_decision
    redaction_class: metadata_and_hashes_only

record_kind: snapshot_review_record
test_run_summary_schema_version: 1
snapshot_review_record_id: execution.snapshot_review.crate_a.golden_render.0001

parent_run_ref: execution.run.test_run.snapshot_update.crate_a.0001
parent_attempt_ref: execution.attempt.test_run.snapshot_update.crate_a.0001.attempt_0

discovery_state_ref: execution.test_discovery_state.local_authoritative.crate_a.snapshot.0001

subject_test_item_refs:
  - execution.test_item.local_authoritative.crate_a.golden_render.case_layout_root
  - execution.test_item.local_authoritative.crate_a.golden_render.case_layout_dark
  - execution.test_item.local_authoritative.crate_a.golden_render.case_layout_compact
  - execution.test_item.local_authoritative.crate_a.golden_render.case_layout_wide
  - execution.test_item.local_authoritative.crate_a.golden_render.case_layout_added
  - execution.test_item.local_authoritative.crate_a.golden_render.case_layout_major

snapshot_review_kind_class: pre_apply_preview_required
snapshot_baseline_identity_class: vcs_committed_baseline

baseline_artifact_event_ref: execution.artifact_event.snapshot.crate_a.golden_render.baseline.0001
proposed_artifact_event_ref: execution.artifact_event.snapshot.crate_a.golden_render.proposed.0001

snapshot_change_summary:
  added_count: 1
  removed_count: 0
  updated_minor_count: 4
  updated_major_count: 1
  updated_redaction_only_count: 0
  unchanged_count: 7
  unreviewable_redaction_limited_count: 0
  total_file_count: 13

snapshot_review_decision_class: review_pending_no_decision

command_dispatch_descriptor_ref: null
rollback_path_ref: vcs.local_history.snapshot.crate_a.golden_render.baseline.0001

review_summary_headline: >
  Six golden-render baselines proposed for update; one new baseline,
  four minor textual diffs, one major structural diff against the
  VCS-committed prior.

audit_event_refs:
  - audit_event.snapshot_review_record.published

denial_reason_class: null

captured_at: "2026-04-25T13:15:00Z"
redaction_class: metadata_and_hashes_only
approval_ticket_ref: null

notes: >
  Pre-apply preview for a golden / snapshot update against a
  VCS-committed baseline; awaiting an explicit user decision before
  the apply path can be admitted.
