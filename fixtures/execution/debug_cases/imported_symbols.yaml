# yaml-language-server: $schema=../../../schemas/execution/mapping_quality.schema.json
__fixture__:
  name: imported_symbols
  scenario: >
    Worked, pre-implementation mapping_quality_record for a frame
    whose instruction pointer resolves through a third-party crate
    (serde 1.0.197) loaded as an external dependency. The joined
    debug_artifact_entry_record resolved
    mapping_imported_symbol_external_dependency_only against a
    dependency package digest; the renderer opens an inspect-only
    summary card naming the dependency, the pinned version, and the
    symbol identity rather than jumping to local workspace source.
    fallback_action_class is open_imported_symbol_summary;
    navigation_target.target_kind_class is
    imported_dependency_summary_ref. expected_build_match_fields is
    empty (this is not an exact-build mapping); mismatch_reasons is
    empty (the row is not build-mismatched, it is correctly resolved
    to an imported dependency).
  exercised_classes_summary:
    record_kind: mapping_quality_record
    quality_class: mapping_imported_symbol_external_dependency_only
    fallback_action_class: open_imported_symbol_summary
    evidence_state_class: evidence_resolved_terminal
    dependency_kind_class: third_party_crate_or_npm_module
    redaction_class: metadata_and_hashes_only

record_kind: mapping_quality_record
mapping_quality_schema_version: 1
mapping_quality_record_id: execution.mapping_quality.imported_symbol.serde_deserialize.0001

debug_artifact_ref: debug-artifact:third_party:serde:1.0.197:dependency_summary:0001
module_identity_ref: module-identity:third_party:serde:1.0.197:libserde
frame_ref: execution.debug_frame.local_attach.exact_build.serde_deserialize_request.5
watch_or_variable_ref: null

address_summary:
  address_kind_class: instruction_pointer_in_module
  module_offset_present: true

quality_class: mapping_imported_symbol_external_dependency_only
fallback_action_class: open_imported_symbol_summary
evidence_state_class: evidence_resolved_terminal

expected_build_match_fields: []

mismatch_reasons: []

imported_dependency_summary:
  dependency_kind_class: third_party_crate_or_npm_module
  display_label: "serde 1.0.197"
  version_or_revision_ref: dependency-pin:serde:1.0.197
  package_or_module_id_ref: package-id:crates.io:serde:1.0.197

navigation_target:
  target_kind_class: imported_dependency_summary_ref
  source_ref: dependency-summary:serde:1.0.197:Deserialize::deserialize
  line_number: null
  column_number: null

audit_event_refs:
  - audit_event.mapping_quality_record.published

denial_reason_class: null

captured_at: "2026-04-25T12:40:00Z"
redaction_class: metadata_and_hashes_only
approval_ticket_ref: null

notes: >
  Imported-dependency frame; "Go to source" opens an inspect-only
  summary card naming serde 1.0.197 and the symbol identity, never
  collapsing into a local-source jump.
