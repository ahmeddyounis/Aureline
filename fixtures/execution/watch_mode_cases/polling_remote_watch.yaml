# yaml-language-server: $schema=../../../schemas/execution/watch_controller_state.schema.json
__fixture__:
  name: polling_remote_watch
  scenario: >
    Worked watch_controller_state_record for a remote target where
    watcher events are not authoritative and the controller falls back
    to fixed interval polling. Inline markers that depend on this row
    must carry the polling disclosure.
  exercised_classes_summary:
    record_kind: watch_controller_state_record
    state_class: polling
    degradation_reason_class: workspace_watcher_polling_fallback
    debounce_policy_class: fixed_interval_polling_debounce
    batch_policy_class: coalesced_batch
    host_boundary_class: remote_workspace_agent_runtime
    redaction_class: metadata_and_hashes_only

record_kind: watch_controller_state_record
watch_controller_state_schema_version: 1
watch_controller_state_record_id: execution.watch_controller.remote.polling.0001

test_session_ref: execution.test_session.remote.watch.0001
selection_ref: execution.test_selection.remote.current_root.0001
discovery_state_ref: execution.test_discovery_state.remote.current_root.0001
latest_run_ref: execution.run.test_watch.remote.0001
latest_attempt_ref: execution.attempt.test_watch.remote.0001.attempt_5
context_snapshot_ref: execution.context_snapshot.remote.watch.0001

state_class: polling
degradation_reason_class: workspace_watcher_polling_fallback

debounce_policy:
  debounce_policy_class: fixed_interval_polling_debounce
  quiet_period_ms: 2000
  max_delay_ms: 10000
  policy_source_ref: execution.watch_policy.remote.polling

batch_policy:
  batch_policy_class: coalesced_batch
  max_change_count: 50
  max_test_item_count: 150
  max_cycle_age_ms: 12000
  coalescing_key_ref: execution.watch_coalescing.remote.current_root

backlog_summary:
  pending_change_count: 4
  pending_test_item_count: 12
  oldest_pending_age_ms: 2500
  backlog_budget_breached: false

power_summary:
  power_state_class: remote_power_state_unknown
  power_saver_active: false
  thermal_throttle_active: false
  power_policy_ref: null

remote_summary:
  remote_state_class: remote_latency_degraded
  host_boundary_class: remote_workspace_agent_runtime
  latency_bucket_class: latency_high
  last_heartbeat_at: "2026-04-25T14:10:10Z"
  remote_capability_ref: runtime.remote_agent_capability.watch.polling.0001

target_health_summary:
  target_health_class: target_healthy
  watcher_health_class: watcher_polling
  adapter_health_class: adapter_healthy
  target_context_snapshot_ref: execution.context_snapshot.remote.watch.0001
  drift_summary_ref: runtime.drift_summary.remote.watch.polling.0001

last_successful_cycle:
  cycle_id: execution.watch_cycle.remote.0001.cycle_5
  attempt_ref: execution.attempt.test_watch.remote.0001.attempt_5
  completed_at: "2026-04-25T14:10:08Z"
  cycle_freshness_class: degraded_cached

pause_summary:
  is_paused: false
  pause_reason_class: not_paused
  paused_at: null
  paused_by_actor_ref: null
  resume_action_ref: null

result_surface_disclosure:
  degradation_visible_to_result_consumers: true
  hidden_diagnostics_only: false
  surface_classes:
    - editor_inline_results
    - test_tree
    - test_summary
    - cli_output
    - support_export
  status_item_ref: ux.status_item.test_watch.remote.polling.0001
  support_export_ref: support.export_ref.test_watch.remote.polling.0001
  disclosure_summary: Remote watch is polling; result consumers show polling freshness instead of live event truth.

audit_event_refs:
  - watch_controller_state_record_published
  - watch_controller_state_record_degradation_narrowed
  - watch_controller_state_record_result_surface_disclosure_updated

denial_reason_class: null

captured_at: "2026-04-25T14:10:15Z"
redaction_class: metadata_and_hashes_only
approval_ticket_ref: null

notes: Remote target remains usable, but watcher fidelity is polling-only.
