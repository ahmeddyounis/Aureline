# yaml-language-server: $schema=../../../schemas/execution/attempt.schema.json
__fixture__:
  name: waiting_input_step
  scenario: >
    Worked attempt-input-request packet for an attempt that has paused
    in lifecycle_state_class = waiting_input. The attempt is the first
    attempt of an automation workflow run that is about to publish a
    deploy artifact to a managed artifact store; before it can advance
    it MUST receive an irreversible-action confirmation from the user.
    input_request_kind_class = irreversible_action_confirmation;
    irreversible_action_summary_class = irreversible_publish_to_managed
    _artifact_store; command_dispatch_descriptor_ref points at the
    descriptor that opened the prompt. result_class =
    result_pending_no_typed_result_yet (the deadline has not passed
    yet); result_summary_class = no_summary_secret_or_redacted (the
    contract pins this for irreversible-action confirmations regardless
    of the typed result, since the prompt is about an irreversible
    action class label rather than a value the user types). closed_at
    is null while pending.
  exercised_classes_summary:
    record_kind: attempt_input_request_record
    input_request_kind_class: irreversible_action_confirmation
    irreversible_action_summary_class: irreversible_publish_to_managed_artifact_store
    result_class: result_pending_no_typed_result_yet
    result_summary_class: no_summary_secret_or_redacted
    redaction_class: metadata_and_hashes_only

record_kind: attempt_input_request_record
attempt_schema_version: 1
input_request_record_id: execution.attempt_input_request.workflow.deploy_publish.confirm.0001

parent_attempt_ref: execution.attempt.workflow.deploy_publish.0001.attempt_0
parent_run_ref: execution.run.workflow.deploy_publish.0001

input_request_kind_class: irreversible_action_confirmation
credential_handle_class_refs: []
irreversible_action_summary_class: irreversible_publish_to_managed_artifact_store
approval_ticket_ref: null
command_dispatch_descriptor_ref: command_dispatch.descriptor.workflow.deploy_publish.confirm

result_class: result_pending_no_typed_result_yet
result_summary_class: no_summary_secret_or_redacted
result_summary_text: null

opened_at: "2026-04-25T12:10:00Z"
expires_at: "2026-04-25T12:25:00Z"
closed_at: null

audit_event_refs:
  - audit_event.attempt_input_request_record.opened

denial_reason_class: null

captured_at: "2026-04-25T12:10:00Z"
redaction_class: metadata_and_hashes_only

notes: >
  Attempt waiting for an irreversible-action confirmation before
  publishing a deploy artifact; the parent attempt sits in
  lifecycle_state_class = waiting_input until the typed result is
  received or the deadline passes (closing with result_timed_out_no_
  value_provided).
