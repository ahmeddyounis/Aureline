# yaml-language-server: $schema=../../../schemas/execution/artifact_event.schema.json
__fixture__:
  name: managed_job_with_artifact_stream
  scenario: >
    Worked artifact-event record on the per-attempt artifact rail of a
    managed-workspace provider job that is streaming an AI response
    chunk. parent_run_ref points at the provider job run (run_kind_
    class = provider_job_run, host_boundary_class = managed_workspace
    _agent_runtime), parent_attempt_ref points at the running attempt
    (lifecycle_state_class = running). artifact_event_kind_class =
    artifact_emitted_streaming_chunk; chunk_index = 4 with is_final
    _chunk = false (more chunks pending). artifact_class = ai_response
    _message_chunk; finalization_class = finalization_pending_streaming
    _in_progress (the artifact has not received its finalize event
    yet). retention_class = retained_managed_with_redaction (the
    managed workspace retains chunks server-side under the default
    redaction posture); raw_artifact_body_ref points at the retained
    chunk. media_class = utf8_text_plain. side_effect_attribution
    _class = no_observed_side_effects_attributable_to_artifact (an AI
    response chunk does not by itself publish a side effect).
  exercised_classes_summary:
    record_kind: artifact_event_record
    artifact_event_kind_class: artifact_emitted_streaming_chunk
    artifact_class: ai_response_message_chunk
    finalization_class: finalization_pending_streaming_in_progress
    retention_class: retained_managed_with_redaction
    redaction_class: metadata_and_hashes_only
    media_class: utf8_text_plain
    side_effect_attribution_class: no_observed_side_effects_attributable_to_artifact

record_kind: artifact_event_record
artifact_event_schema_version: 1
artifact_event_record_id: execution.artifact_event.provider_job.ai_chunk.0004

parent_run_ref: execution.run.provider_job.managed_ai.0001
parent_attempt_ref: execution.attempt.provider_job.managed_ai.0001.attempt_0

artifact_event_kind_class: artifact_emitted_streaming_chunk
artifact_class: ai_response_message_chunk
artifact_ref: execution.artifact.provider_job.managed_ai.response_message.0001

chunk_index: 4
is_final_chunk: false
replacement_artifact_ref: null
withdrawal_actor_ref: null

finalization_class: finalization_pending_streaming_in_progress
retention_class: retained_managed_with_redaction
raw_artifact_body_ref: retention.managed.provider_job.ai_chunk.0004
raw_artifact_bytes_reported: 1872
raw_artifact_hash: "sha256:9f1c4b7a3e2d0a5f8b6c1d4e7f0a2c5b8d1e4f7a0c3e6b9d2f5a8c1e4b7d0a3f"

media_class: utf8_text_plain
redaction_class: metadata_and_hashes_only
credential_handle_class_refs: []
approval_ticket_ref: null

side_effect_attribution_class: no_observed_side_effects_attributable_to_artifact
side_effect_attribution_note: >
  Streaming AI response chunk; no host write, no remote target write,
  no irreversible publication.

task_event_envelope_event_id: tooling.task_event_envelope.provider_job.managed_ai.chunk_4

summary: >
  Streaming chunk 4 (utf-8 text plain, 1872 bytes) of an AI response
  message; awaiting finalize event before the artifact is immutable.

audit_event_refs:
  - audit_event.artifact_event_record.published
  - audit_event.artifact_event_record.streaming_chunk_attached

denial_reason_class: null

captured_at: "2026-04-25T12:05:00Z"
