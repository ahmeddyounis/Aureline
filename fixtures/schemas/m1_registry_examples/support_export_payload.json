{
  "registry_example_kind": "schema_registry_envelope_example",
  "schema_registry_row_entry_id": "support_export.m1_payload_baseline",
  "pinned_schema_ref": "schemas/support/support_bundle_manifest.schema.json",
  "pinned_schema_version_uri": "https://aureline.dev/schemas/support/support_bundle_manifest.schema.json",
  "pinned_record_kind": "support_bundle_manifest_record",
  "pinned_schema_version": 1,
  "envelope_fields_demonstrated": [
    "collection_schema_version",
    "record_kind"
  ],
  "consent_class_demonstrated": "export_only_on_user_request",
  "default_posture_class_demonstrated": "user_initiated_export_only",
  "redaction_class_demonstrated": "high_risk_explicit_review_required",
  "documentation_note": "Canonical M1 envelope example for the support-export payload family. The support bundle's pinned schema names its version field as collection_schema_version rather than schema_version; the M1 seed validation lane normalises either field name and asserts the pinned integer matches. The example carries only the discriminator and the pinned version so the registry-side pin is auditable without re-stating the full support-bundle manifest contract."
}
