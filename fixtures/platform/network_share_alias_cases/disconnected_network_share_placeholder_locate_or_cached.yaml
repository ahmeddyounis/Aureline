$schema: ../../../schemas/platform/system_open_target_packet.schema.json
__fixture__:
  name: disconnected_network_share_placeholder_locate_or_cached
  scenario: >
    A user opens a file on a network share that is currently disconnected. The
    resolver emits a placeholder record with recovery actions (locate, open
    cached context read-only when available, or cancel) rather than opening an
    empty window.
  exercised_axes:
    record_kind: system_open_target_placeholder_record
    external_path: network_share_disconnected
    availability: remote_unreachable
    recovery: locate_cached_cancel
  contract_sections:
    - "5. External-path failure degradation"

record_kind: system_open_target_placeholder_record
system_open_target_schema_version: 1
packet_id: platform:system-open-target:network-share:disconnected:placeholder:01
source_surface_class: system_open
requested_action_class: open_existing_context
active_profile_ref: null
availability_class: remote_unreachable
placeholder_reason_class: remote_unreachable

literal_target:
  literal: "\\\\SERVER\\Share\\src\\App.ts"
  format_class: windows_unc_path
  display_label: "\\\\SERVER\\Share\\src\\App.ts"

presentation_path: null

external_path_classes:
  - network_share_unc

cached_context_ref: "cached_context:network-share:app.ts:01"

recovery_actions:
  - action_class: locate_missing_target
    label: "Locate"
  - action_class: open_read_only_cached_view
    label: "Open cached context (read-only)"
  - action_class: cancel
    label: "Close"

emitted_at: "2026-05-06T12:00:00Z"

