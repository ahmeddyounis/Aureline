# Native Desktop Contract Fixtures

This fixture pack exercises desktop entry and reopen truth for system open,
file associations, auth callbacks, protocol handlers, recent items, native
notifications, badges, reveal-in-shell, interruption recovery, and
per-platform drills.

The packet fixture is intentionally compact: it covers every required surface
and drill class while avoiding raw paths, secrets, callback bodies, provider
payloads, and customer identifiers.

