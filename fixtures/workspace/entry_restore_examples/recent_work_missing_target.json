{
  "$schema": "../../../schemas/workspace/entry_and_restore_result.schema.json",
  "__fixture__": {
    "name": "recent_work_missing_target",
    "scenario": "A Start Center recent-work row for a repository whose folder was moved on disk between sessions. target_kind = local_repo_root, target_state = missing_target (ADR-0006 canonical owner no longer finds the folder at the captured path / inode). restore_availability = layout_only because the prior session layout is still rehydratable without the file bytes. trust_state = trusted (previously admitted) but authority re-evaluation will happen once a new target is located. Safe recovery actions exposed: locate_missing_target, remove_from_recents, and reveal_in_explorer (explorer shows the parent dir). The row preserves stale-but-inspectable metadata; it never claims reachable.",
    "target_kinds_exercised": ["local_repo_root"],
    "recent_work_target_states_exercised": ["missing_target"],
    "portability_classes_exercised": ["local_only"],
    "restore_availabilities_exercised": ["layout_only"],
    "safe_recovery_actions_exercised": [
      "locate_missing_target",
      "remove_from_recents",
      "reveal_in_explorer"
    ],
    "doc_sections": ["§2 Recent-work entry record", "§6.7 Recent-work row with missing target"]
  },
  "record_kind": "recent_work_entry_record",
  "entry_and_restore_schema_version": 1,
  "recent_work_id": "recent-2026041916-0007",
  "presentation_label": "acme/payments",
  "presentation_subtitle": "~/Code/payments (path may have moved)",
  "target_kind": "local_repo_root",
  "target_state": "missing_target",
  "portability_class": "local_only",
  "trust_state": "trusted",
  "restore_availability": "layout_only",
  "pinned": false,
  "last_opened_at": "mono:4811:23:55:00.0000",
  "filesystem_identity_ref": "fs-identity-local-repo-payments-0",
  "safe_recovery_actions": [
    "locate_missing_target",
    "remove_from_recents",
    "reveal_in_explorer"
  ],
  "recovery_checkpoint_refs": [
    {
      "recovery_class": "restore_from_session_checkpoint",
      "checkpoint_ref": "ckpt-session-payments-last-good-0001"
    }
  ]
}
