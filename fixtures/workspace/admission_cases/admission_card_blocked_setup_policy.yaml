# yaml-language-server: $schema=../../../schemas/workspace/admission_checkpoint.schema.json
$schema: "https://aureline.dev/schemas/workspace/admission_checkpoint.schema.json"
__fixture__:
  name: admission_card_blocked_setup_policy
  scenario: >
    The blocked_setup_card paired with the restricted_clone_policy_blocked_setup
    checkpoint. The card lists blocked_by_policy in blocked_reason_classes, ties
    to the policy block refs, surfaces continue_in_restricted_mode as the bypass
    action, and remains keyboard reachable with severity blocked.
  contract_sections:
    - "§5.1 admission_card_class (blocked_setup_card)"
    - "§5.2 admission_card_record required fields"
    - "§5.5 rule 3 (No generic failure copy)"
record_kind: admission_card_record
admission_card_schema_version: 1
admission_card_id: "card.restricted.blocked_setup"
emitted_at: "ts.monotonic.restricted_clone"
admission_checkpoint_ref: "admission.checkpoint.restricted_clone"
admission_card_class: blocked_setup_card
card_severity_class: blocked
referenced_task_refs:
  - "task.restricted.dependency_restore"
referenced_policy_block_refs:
  - "policyblock.restricted.package_install"
  - "policyblock.restricted.public_registry"
referenced_recommendation_refs:
  - "rec.restricted.review_bundle"
blocked_reason_classes:
  - blocked_by_policy
archetype_recommendation_source_classes:
  - admin_policy
  - detected_facts
setup_location_classes:
  - local_machine
bypass_action: continue_in_restricted_mode
presentation_label: "Setup blocked by managed policy"
summary: "Dependency restore is blocked by managed policy on package install and public registry contact. Continue in restricted mode keeps editing available."
keyboard_reachable: true
