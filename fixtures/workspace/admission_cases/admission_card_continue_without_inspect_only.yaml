# yaml-language-server: $schema=../../../schemas/workspace/admission_checkpoint.schema.json
$schema: "https://aureline.dev/schemas/workspace/admission_checkpoint.schema.json"
__fixture__:
  name: admission_card_continue_without_inspect_only
  scenario: >
    The continue_without_card paired with the missing_remote_agent checkpoint.
    The card carries bypass_action inspect_only so the user can anchor the
    linked review object even though the remote agent is unreachable.
  contract_sections:
    - "§5.1 admission_card_class (continue_without_card)"
    - "§3.6 continue_without_class (inspect_only)"
record_kind: admission_card_record
admission_card_schema_version: 2
admission_card_id: "card.deep_link.continue_without"
emitted_at: "ts.monotonic.deep_link_missing_remote_agent"
admission_checkpoint_ref: "admission.checkpoint.deep_link_missing_remote_agent"
admission_card_class: continue_without_card
card_severity_class: attention_required
bypass_action: inspect_only
presentation_label: "Inspect linked review without remote attach"
summary: "Inspect-only opens the linked review object as read-only context until the remote agent reconnects."
keyboard_reachable: true
