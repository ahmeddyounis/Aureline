# yaml-language-server: $schema=../../../schemas/workspace/admission_checkpoint.schema.json
$schema: "https://aureline.dev/schemas/workspace/admission_checkpoint.schema.json"
__fixture__:
  name: admission_banner_policy_restricted
  scenario: >
    The admission_banner_record paired with the
    restricted_clone_policy_blocked_setup checkpoint. The banner resolves to
    policy_restricted_banner with severity blocked and bypass_action
    continue_in_restricted_mode. The banner cites a status-bar chip and
    attention-inbox routing ref so notifications and the project doctor render
    the same explanation.
  contract_sections:
    - "§5.3 admission_banner_class (policy_restricted_banner)"
    - "§5.4 admission_banner_record required fields"
    - "§6 rule 6 (Notifications quote the banner class)"
record_kind: admission_banner_record
admission_banner_schema_version: 1
admission_banner_id: "banner.restricted.policy_restricted"
emitted_at: "ts.monotonic.restricted_clone"
admission_checkpoint_ref: "admission.checkpoint.restricted_clone"
admission_banner_class: policy_restricted_banner
severity_class: blocked
bypass_action: continue_in_restricted_mode
presentation_label: "Setup is policy-restricted"
summary: "Managed policy blocks dependency restore and public registry contact. Continue in restricted mode keeps editing and index warmup available."
attention_inbox_routing_ref: "attention.inbox.restricted_clone"
status_bar_chip_ref: "status_bar.chip.restricted_clone"
notification_event_ref: "notification.event.restricted_clone.policy_blocked"
keyboard_reachable: true
