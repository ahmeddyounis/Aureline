# yaml-language-server: $schema=../../../schemas/workspace/recommendation_source_row.schema.json
$schema: "https://aureline.dev/schemas/workspace/recommendation_source_row.schema.json"
__fixture__:
  name: bundle_suggestion_from_admin_policy
  scenario: >
    A workflow-bundle suggestion is derived from admin policy posture (for example,
    recommending an org-approved bundle). Policy blocks remain separate records; this
    row is a recommendation provenance row.
  contract_sections:
    - "Source classes (admin_policy)"
    - "Evidence hooks (basis_signal_admin_policy)"
record_kind: recommendation_source_row_record
recommendation_source_row_schema_version: 1
row_id: "rec_source_row.policy.org_approved_bundle_suggestion"
minted_at: "ts.monotonic.policy.bundle_suggestion"
admission_checkpoint_ref: "admission.checkpoint.policy_guided_open"
subject_kind: workflow_bundle
subject_ref: "bundle.org.approved.launch_wedge"
recommendation_source_classes:
  - admin_policy
evidence_hooks_reserved:
  - basis_signal_admin_policy
basis:
  - basis_kind: policy_ref
    basis_ref: "policy.bundle_suggestion.org_approved.001"
    source_class: admin_policy
    summary: "Signed policy suggests an org-approved workflow bundle as the preferred reviewed setup path."
summary: "Bundle suggestion is explainable: it is derived from admin policy, and remains separate from any policy-blocked requirements."

