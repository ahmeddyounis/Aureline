# yaml-language-server: $schema=../../../schemas/workspace/recommendation_source_row.schema.json
$schema: "https://aureline.dev/schemas/workspace/recommendation_source_row.schema.json"
__fixture__:
  name: archetype_suggestion_heuristic_inference_ambiguous
  scenario: >
    Workspace archetype guidance uses a heuristic inference due to mixed/conflicting
    signals. The inference label must remain visible and the basis must be cited.
  contract_sections:
    - "Source classes (heuristic_inference)"
    - "Evidence hooks (heuristic_inference_applied)"
record_kind: recommendation_source_row_record
recommendation_source_row_schema_version: 1
row_id: "rec_source_row.archetype.heuristic_inference.mixed_workspace"
minted_at: "ts.monotonic.mixed_workspace.heuristic_row"
admission_checkpoint_ref: "admission.checkpoint.mixed_boundary_choice_pending"
subject_kind: workspace_archetype
subject_ref: "archetype.mixed_workspace.probable_web_subroot"
confidence_class: mixed_conflicting
support_claim_class: generic_no_claim
detector_state: partial
recommendation_source_classes:
  - heuristic_inference
  - detected_facts
  - mixed_recommendation_source
evidence_hooks_reserved:
  - heuristic_inference_applied
  - basis_signal_filesystem_layout
  - basis_signal_manifest
basis:
  - basis_kind: heuristic_rule_ref
    basis_ref: "heuristic.rule.probable_root_selection.001"
    source_class: heuristic_inference
    summary: "Heuristic selects a probable project root for guidance; user can switch to a plain explorer or choose a different boundary."
  - basis_kind: detection_signal_ref
    basis_ref: "signal.mixed.filesystem_layout"
    source_class: detected_facts
    summary: "Filesystem layout suggests multiple plausible roots."
  - basis_kind: detection_signal_ref
    basis_ref: "signal.mixed.manifest"
    source_class: detected_facts
    summary: "Manifests exist in more than one subtree; signals conflict."
summary: "Archetype guidance is explainable: it is a heuristic inference based on mixed evidence, with equal access to bypass and boundary-choice actions."

