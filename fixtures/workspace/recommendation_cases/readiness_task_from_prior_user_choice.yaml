# yaml-language-server: $schema=../../../schemas/workspace/recommendation_source_row.schema.json
$schema: "https://aureline.dev/schemas/workspace/recommendation_source_row.schema.json"
__fixture__:
  name: readiness_task_from_prior_user_choice
  scenario: >
    A readiness-task suggestion remains visible but de-emphasized after the user
    previously chose set-up-later or dismissed the task. Exercises
    prior_user_choice mixed with detected_facts.
  contract_sections:
    - "Source classes (prior_user_choice, detected_facts, mixed_recommendation_source)"
    - "Evidence hooks (basis_signal_previous_user_choice)"
record_kind: recommendation_source_row_record
recommendation_source_row_schema_version: 1
row_id: "rec_source_row.readiness.dependency_restore.deferred_by_user"
minted_at: "ts.monotonic.remembered_route_set_up_later.row"
admission_checkpoint_ref: "admission.checkpoint.remembered_route_set_up_later"
subject_kind: readiness_task
subject_ref: "task.remembered.dependency_restore_dismissed"
readiness_task_ref: "task.remembered.dependency_restore_dismissed"
recommendation_ref: "rec.remembered.review_bundle"
recommendation_source_classes:
  - prior_user_choice
  - detected_facts
  - mixed_recommendation_source
evidence_hooks_reserved:
  - basis_signal_previous_user_choice
  - basis_signal_manifest
basis:
  - basis_kind: user_choice_ref
    basis_ref: "user_choice.remembered.set_up_later"
    source_class: prior_user_choice
    summary: "User previously chose set-up-later; the preference is applied as a narrowing hint only."
  - basis_kind: detection_signal_ref
    basis_ref: "signal.remembered.manifest"
    source_class: detected_facts
    summary: "A manifest indicates dependency restore is relevant, but it remains deferred."
summary: "Readiness task remains explainable: it is relevant to the workspace, but it is shown as deferred due to a prior explicit user choice."

