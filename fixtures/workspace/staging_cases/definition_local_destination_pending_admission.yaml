__fixture__:
  name: local_destination_pending_admission definition
  scenario: Bytes exist in a user-chosen destination but are not yet admitted as workspace state.
  doc_sections:
    - Target materialization class (closed)
record_kind: materialization_class_definition_record
materialization_class_schema_version: 1
target_materialization_class: local_destination_pending_admission
class_label: Local destination pending admission
durability_class: durable_local
write_posture_ceiling_class: writes_uncommitted_destination
treat_as_durable_workspace_state: false
support_export_as_durable_workspace_state: false
eligible_for_restore_last_session: false
default_cleanup_posture_class: retain_for_review
required_disclosure_fields:
  - location_label
  - non_durable_label_visible
  - durable_promotion_requires_review
  - cleanup_posture_class
  - promotion_action_class
  - export_posture_class
  - recovery_posture_class
reviewer_summary: A user destination is not treated as workspace state until explicitly admitted as the workspace root.

