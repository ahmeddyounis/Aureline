# Editor continuity fixtures

These fixtures drive the editor-continuity test harness and cover:

- close/reopen continuity (caret, dirty state, shared buffer authority);
- split + move continuity (caret, dirty state, shared buffer authority); and
- restore handoff continuity (dirty buffer + caret state preserved across runtime handoff).

