__fixture__:
  name: extension_embedded_surface_policy_blocked_external_open_only
  scenario: >
    An extension-hosted embedded web surface is denied by policy. The
    host renders the boundary card with a policy-blocked state and
    offers external open only when policy allows, via a typed handoff
    (never raw URL launch from extension chrome).
  matrix_row_ref: web_surface_row.extension_embedded_web_surfaces.embedded_webview
  references:
    - docs/web/scoped_browser_surface_matrix.md
    - docs/adr/0015-embedded-surface-boundary-and-auth-handoff.md
    - docs/ux/embedded_surface_boundary_cards.md

extension_surface:
  delivery_mode: embedded_webview_in_desktop_shell
  boundary_state: policy_blocked
  required_disclosure:
    owner_label: "Extension publisher"
    publisher_or_service_label: "Extension web surface"
    origin_label: "https://extension-ui.example.invalid"
    capability_limitations:
      - cannot_issue_native_approval
      - cannot_raise_workspace_trust
  allowed_host_actions:
    - inspect_policy_reason
    - open_in_browser_via_typed_handoff_if_permitted
  forbidden_paths:
    - raw_url_launch_from_extension_chrome
    - embedded_impersonation_of_reserved_native_surfaces

