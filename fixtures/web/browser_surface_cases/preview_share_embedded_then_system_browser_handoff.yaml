__fixture__:
  name: preview_share_embedded_then_system_browser_handoff
  scenario: >
    A preview is rendered inside a scoped embedded surface. The user
    requests a shareable link; policy requires a typed system-browser
    handoff sheet before any share link is created.
  matrix_row_ref: web_surface_row.preview_and_share.embedded_webview
  references:
    - docs/web/scoped_browser_surface_matrix.md
    - docs/runtime/browser_runtime_contract.md
    - docs/remote/attach_tunnel_port_forward_contract.md

preview:
  delivery_mode: embedded_webview_in_desktop_shell
  preview_state: live_preview_rendered
  requested_action: request_share_link
  policy_gate:
    requires_step_up: true
    requires_handoff_sheet: true
    denied_by_default_for_public: true
  required_disclosure:
    route_truth:
      termination_label: "remote agent (workspace-bound)"
      audience_label: "workspace_only"
      ttl_label: "expires after short duration"
      revoke_path_present: true
    handoff:
      handoff_target: system_browser_handoff
      reason: "Share link creation requires external consent and audit."
  local_core_fallback:
    available: true
    description: >
      If embedded preview is blocked, provide route truth and a snapshot
      export path without creating a shareable link.

