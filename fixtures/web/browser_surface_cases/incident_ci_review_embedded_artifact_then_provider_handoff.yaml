__fixture__:
  name: incident_ci_review_embedded_artifact_then_provider_handoff
  scenario: >
    Incident/CI evidence is rendered in-product from structured
    artifacts (logs, check summaries). Opening a provider-owned CI page
    requires a typed system-browser handoff with explicit provider
    identity and return anchor.
  matrix_row_ref: web_surface_row.incident_and_ci_review.embedded_webview
  references:
    - docs/web/scoped_browser_surface_matrix.md
    - artifacts/web/scoped_browser_capabilities.yaml
    - docs/adr/0015-embedded-surface-boundary-and-auth-handoff.md

review:
  evidence_render_surface:
    delivery_mode: embedded_webview_in_desktop_shell
    write_scope: write_scope.comments_and_approvals_only
    provenance_disclosure:
      source: "structured artifact graph (redaction applied)"
      freshness: "warm_cached"
      redaction_class: metadata_safe_default
  provider_open:
    requested_action: open_provider_ci_page
    provider_identity:
      provider_label: "CI provider"
      origin_label: "https://ci.example.invalid"
      acting_identity_label: "delegated credential (session-scoped)"
    handoff:
      required: true
      delivery_mode: system_browser_handoff
      return_anchor_label: "Return to incident evidence view"
      raw_url_visible_to_user: false

