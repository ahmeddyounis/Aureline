__fixture__:
  name: docs_viewing_embedded_offline_snapshot
  scenario: >
    Docs viewing in a scoped embedded surface. The docs body is rendered
    from a declared source with explicit source/version/freshness and
    an offline snapshot posture. The embedded body has no filesystem,
    secret, or device-permission authority.
  matrix_row_ref: web_surface_row.docs_viewing.embedded_webview
  references:
    - docs/web/scoped_browser_surface_matrix.md
    - artifacts/web/scoped_browser_capabilities.yaml
    - docs/ux/embedded_surface_boundary_cards.md

surface:
  surface_id: docs_viewing
  delivery_mode: embedded_webview_in_desktop_shell
  boundary_state: offline_snapshot
  plain_language_summary: >
    Viewing docs from a cached, verified pack while offline. Live
    external navigation and mutations are unavailable.
  required_disclosure:
    owner_label: "Aureline project"
    origin_label: "local signed docs pack"
    publisher_or_service_label: "Docs pack publisher"
    capability_limitations:
      - live_network_mutation_disabled_when_offline
      - cookies_or_storage_outside_product_boundary
  capability_envelope:
    allowed_write_scope: write_scope.none
    file_access_posture: file_access.none
    secret_handling_posture: secrets.no_secret_bytes
    device_permission_posture: device_permissions.none
    copy_export_posture:
      copy_allowed: true
      raw_url_copy_allowed: false
      export_by_reference_only: true
  local_core_fallback:
    available: true
    description: >
      If embedded rendering fails closed, fall back to a local-core
      markdown/plain-text renderer while preserving the same
      source/version/freshness disclosure.

