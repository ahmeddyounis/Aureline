{
  "__fixture__": {
    "name": "last_test_exact_prior_target",
    "scenario": "A pytest item was last run on the local target. The current test context resolves to SSH, but the selected mode is exact prior target; the prepared attempt keeps the prior target while the comparison sheet discloses current target drift before dispatch."
  },
  "record_kind": "rerun_exact_vs_current_case",
  "schema_version": 1,
  "expect": {
    "command_id": "cmd:test.rerun_last",
    "selected_target_mode": "exact_prior_target",
    "dispatch_state": "review_required",
    "requires_review_before_dispatch": true,
    "keyboard_reachable": true,
    "required_diff_fields": [
      "target_identity.target_class",
      "target_identity.canonical_target_id",
      "target_identity.working_directory",
      "target_identity.local_vs_managed_boundary_visible",
      "target_confidence.reasons",
      "prebuild_metadata.compatibility_fingerprint",
      "mixed_version_drift.state"
    ]
  }
}
