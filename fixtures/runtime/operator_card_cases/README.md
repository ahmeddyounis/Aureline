# Operator card composite cases

These fixtures are **composite** examples used by:

- `docs/runtime/certified_wedge_operator_card.md`

Each case quotes (by fixture ref and/or inline projection) the canonical
managed-workspace lifecycle, notebook trust/round-trip preview, and
install-review fact-grid fields so reviewers can validate an end-to-end
story without chasing multiple directories.

## Cases

- `local_only_wedge.yaml`
- `provider_backed_wedge.yaml`
- `notebook_widget_trust_risk.yaml`
- `activation_budget_exceeded.yaml`
- `incompatible_extension.yaml`
- `provider_offline_local_continuation.yaml`

