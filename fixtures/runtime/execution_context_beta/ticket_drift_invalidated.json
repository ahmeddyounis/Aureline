{
  "__fixture__": {
    "name": "ticket_drift_invalidated",
    "scenario": "A stored ticket was minted against the local-host lane (local terminal seed). The user now requests a remote-attach task. The ticket-drift evaluator MUST invalidate the stored binding with typed drift rows covering target_class, canonical_target_id, and working_directory."
  },
  "record_kind": "execution_context_beta_drift_case",
  "schema_version": 1,
  "stored": {
    "constructor": "local_terminal_seed",
    "command_id": "terminal.open",
    "trust_state": "trusted",
    "observed_at": "mono:0"
  },
  "fresh": {
    "constructor": "remote_attach_task_seed",
    "command_id": "task.run.ssh_remote",
    "target_class": "ssh_remote",
    "trust_state": "trusted",
    "observed_at": "mono:1"
  },
  "expect": {
    "outcome": "invalidated",
    "fresh_lane": "remote_attach",
    "drift_fields": [
      "target_class",
      "canonical_target_id"
    ]
  }
}
