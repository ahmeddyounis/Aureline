{
  "__fixture__": {
    "name": "remote_lane",
    "scenario": "SSH remote task seed resolves through the beta remote_attach lane. The boundary cue MUST light and the mixed-version posture MUST be unchecked."
  },
  "record_kind": "execution_context_beta_case",
  "schema_version": 1,
  "input": {
    "constructor": "remote_attach_task_seed",
    "command_id": "task.run.ssh_remote",
    "target_class": "ssh_remote",
    "trust_state": "trusted",
    "observed_at": "mono:1"
  },
  "expect": {
    "lane": "remote_attach",
    "target_class": "ssh_remote",
    "surface": "task",
    "toolchain_class": "build_driver_runtime",
    "boundary_cue_visible": true,
    "has_degraded_field": false
  }
}
