{
  "__fixture__": {
    "name": "nix_flake_case",
    "scenario": "Workspace declares only flake.nix. The beta resolver does not embed a Nix evaluator, so the source MUST be labelled unsupported with the unsupported_body_parse note while still tracking the content digest for drift detection."
  },
  "record_kind": "environment_capsule_beta_case",
  "schema_version": 1,
  "workspace_dir": "workspaces/nix_flake",
  "archetype_hint": "library_or_sdk",
  "expect": {
    "primary_source_token": "nix_flake",
    "drift_state": "in_sync",
    "prebuild_reuse_state": "candidate",
    "capsule_id": "capsule.beta.nix_flake.metadata",
    "expected_source_classes": ["nix_flake"],
    "expected_confidence_by_class": {
      "nix_flake": "unsupported"
    },
    "conflict_notes": []
  }
}
