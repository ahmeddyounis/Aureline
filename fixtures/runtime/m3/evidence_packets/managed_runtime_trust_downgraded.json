{
  "__fixture__": {
    "name": "managed_runtime_trust_downgraded",
    "scenario": "Managed-workspace runtime trace evidence captured with trust state trusted. Replay context resolves the same managed workspace but the trust state has downgraded to restricted. The comparator must surface the typed trust-downgrade block and refuse replay without review."
  },
  "record_kind": "runtime_evidence_packet_case",
  "schema_version": 1,
  "case_id": "evidence_packets.managed_runtime_trust_downgraded",
  "scenario": "managed_runtime_trust_downgraded",
  "expect": {
    "lane": "runtime",
    "evidence_kind": "runtime_trace_evidence",
    "compatibility": "incompatible_trust_state_downgraded",
    "permits_replay_without_review": false,
    "blocks_replay": true,
    "required_incompatibility_reasons": ["trust_state_downgraded"]
  }
}
