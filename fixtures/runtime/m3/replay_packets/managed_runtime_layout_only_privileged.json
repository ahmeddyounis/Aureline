{
  "__fixture__": {
    "name": "managed_runtime_layout_only_privileged",
    "scenario": "Managed-workspace runtime evidence whose replay context downgraded trust state. The captured action is privileged (managed-workspace dispatch). The pack must resolve fidelity layout_only, reopen allow_inspect_no_rerun, and refuse live rerun so the privileged action is never silently re-fired."
  },
  "record_kind": "runtime_replay_pack_case",
  "schema_version": 1,
  "case_id": "replay_packets.managed_runtime_layout_only_privileged",
  "scenario": "managed_runtime_layout_only_privileged",
  "expect": {
    "fidelity": "layout_only",
    "subject_privilege": "privileged",
    "reopen_decision": "allow_inspect_no_rerun",
    "forbids_live_rerun": true,
    "comparator_blocks_replay": true,
    "covers_required_artefact_classes": true
  }
}
