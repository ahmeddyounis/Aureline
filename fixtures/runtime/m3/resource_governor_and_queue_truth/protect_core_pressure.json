{
  "record_kind": "resource_governor_truth_fixture",
  "schema_version": 1,
  "case_id": "protect_core_pressure",
  "scenario": "Battery, thermal, CPU, network, disk, memory, and optional-service pressure combine into protect-core. Background lanes must name paused, narrowed, coalesced, checkpointed, and denied work while foreground save and cancellation remain admitted.",
  "expect": {
    "governor_state": "protect_core",
    "required_pressure_dimensions": [
      "cpu",
      "memory",
      "disk",
      "battery_thermal",
      "network",
      "optional_service_quota"
    ],
    "required_lane_tokens": [
      "foreground",
      "interactive_background",
      "maintenance",
      "provider_overlay",
      "upload_replication"
    ],
    "required_lane_state_flags": [
      "paused",
      "narrowed",
      "coalesced",
      "checkpointed",
      "denied"
    ],
    "required_protected_actions": [
      "editing",
      "save",
      "explicit_cancellation",
      "quick_open",
      "navigation"
    ],
    "required_admitted_requests": [
      "admission.save.active_buffer",
      "admission.cancel.index_refresh"
    ],
    "required_blocked_override_ids": [
      "override.workspace.indexing.hot_set_only",
      "override.profile.ai_context_background"
    ],
    "required_checkpoint_labels": [
      "symbol-index phase 3",
      "optional-maintenance epoch 18",
      "provider ruleset fetch phase",
      "resumable upload chunk 12"
    ]
  }
}
