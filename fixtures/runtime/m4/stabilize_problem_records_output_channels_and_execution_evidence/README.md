# Stabilize Problem Records, Output Channels, and Execution Evidence Fixtures

These fixtures exercise the stable execution-evidence bundle owned by
`aureline-runtime`. Each case starts from the canonical stable bundle and applies
one named mutation so the test suite can prove that parser confidence,
raw-output backlinks, imported-provider truth, output chunks, and reopen lineage
stay serializable and export-safe.

