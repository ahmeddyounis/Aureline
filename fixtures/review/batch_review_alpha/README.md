# Batch Review Alpha Fixtures

These fixtures protect the review-lane consumer of the shared collection
contract.

- `local_review_batch_sheet.yaml` starts from a local review-workspace seed,
  selects diff anchors by stable identity, and proves the batch sheet separates
  included, excluded, blocked, and hidden members before a consequential action
  can continue.

