# Protected fixture manifest for the schema, endpoint-policy, and
# operational-signal inspector.

fixture_set_id: endpoint_policy_alpha
schema_version: 1
owner: "@ahmeddyounis"
description: >
  Exercises telemetry/support schema claims, endpoint-policy rows, and
  operational signal slices using the same freshness and redaction
  vocabulary across desktop inspector, support export, and runbook/help
  handoff projections.

cases:
  - case_id: telemetry_support_operational_signals
    fixture_ref: fixtures/inspectors/endpoint_policy_alpha/telemetry_support_operational_signals.yaml
    required_claim_refs:
      - telemetry.ux_product_event
      - support.bundle_manifest
      - schema_alpha:support_export.bundle_manifest
    required_signal_kinds:
      - logs
      - metrics
      - traces
      - incident_timeline
    required_freshness_classes:
      - live
      - buffering
      - cached
      - stale
      - partial
      - offline
    required_redaction_classes:
      - metadata_only
      - redacted_payload
      - by_reference_only
      - withheld_by_policy
