__fixture__:
  name: acting_identity_project_scoped_grant_qualification
  case_id: provider_qualification.acting_identity_project_scoped_grant_qualification.v1
  scenario: >-
    A project-scoped grant is the actor on a code-host pull-request
    draft authored offline. The badge renders as
    `project_scoped_grant_label`; the project scope (path, repo,
    project name) is visible on every listed label surface. The
    decision row carries `actor_class = project_scoped_grant` and
    `decision_class = local_draft_only`; offline capture admits a
    queue item rather than reporting a publish.
  exercises:
    drill_id: acting_identity_project_scoped_grant_qualification
    actor_class: project_scoped_grant
    label_class: project_scoped_grant_label
    label_surfaces:
      - desktop_provider_badge
      - desktop_surface_header
      - desktop_notification
      - audit_event_payload
      - support_export_row
cited_records:
  - kind: project_scoped_grant_record
    fixture_ref: fixtures/providers/connected_account_cases/project_scoped_grant_local_draft_only_offline.yaml
    note: >-
      Project-scoped grant, badge, and decision row;
      `actor_class = project_scoped_grant`,
      `decision_class = local_draft_only`.
acceptance_proof:
  - claim: project scope visible everywhere
    proof: >-
      Every label surface lists the project scope alongside the
      `project_scoped_grant_label`.
  - claim: local-draft-only posture preserved
    proof: >-
      Offline capture admits a publish-later queue item with the
      typed `mode_admission_reason = offline_capture`; silent
      promotion to `publish_now` is forbidden.
  - claim: support-export carries the project scope
    proof: >-
      The support-export row preserves the project-scope path and
      the local-draft-only posture verbatim.
