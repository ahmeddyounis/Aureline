__fixture__:
  name: acting_identity_human_account_qualification
  case_id: provider_qualification.acting_identity_human_account_qualification.v1
  scenario: >-
    A human account is the actor on a code-host pull-request review
    publish. The acting-identity badge renders as `you_label` on the
    desktop provider badge, the desktop surface header, the desktop
    notification, the audit-event payload, and the support-export
    row. The effective-scope decision row carries
    `actor_class = human_account`; no surface displays a generic
    "Connected" label.
  exercises:
    drill_id: acting_identity_human_account_qualification
    actor_class: human_account
    label_class: you_label
    label_surfaces:
      - desktop_provider_badge
      - desktop_surface_header
      - desktop_notification
      - audit_event_payload
      - support_export_row
cited_records:
  - kind: acting_identity_badge_record
    fixture_ref: fixtures/providers/connected_account_cases/human_account_link_allowed_review_publish.yaml
    note: >-
      Human-account link, badge, and decision row in one fixture;
      `actor_class = human_account` and `decision_class = allowed`.
acceptance_proof:
  - claim: badge-label-actor parity on every surface
    proof: >-
      Every label surface listed renders `you_label` with the human
      account's display label intact.
  - claim: decision-row actor parity
    proof: >-
      The cited `provider_scope_resolution_result_record` carries
      `actor_class = human_account`.
  - claim: support-export honesty
    proof: >-
      The support-export row preserves the human-account subject and
      label class verbatim; collapsing into "Connected" is rejected.
