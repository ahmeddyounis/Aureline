__fixture__:
  name: artifact_registry__package_version
  case_id: provider_qualification.artifact_registry__package_version.v1
  scenario: >-
    An artifact / package registry qualifies for the
    `artifact_registry_or_package_registry__package_version` matrix
    row. Package-version metadata inspects cleanly into a typed
    snapshot; publishing rides the release lane (no raw publish_now);
    yank, deprecate, and ownership-mutation actions route through the
    browser-handoff sheet with a typed `object_link_anchor` return.
  exercises:
    matrix_row_id: artifact_registry_or_package_registry__package_version
    provider_class: artifact_registry_or_package_registry
    object_class: package_version
    inspect_support_class: full_typed_snapshot
    in_product_mutate_support_class: rides_release_lane
    browser_only_posture_class: browser_only_for_some_actions
    local_draft_support_class: not_applicable
    publish_later_support_class: parked_publish_now_only
    callback_or_webhook_requirement_class: required_for_authoritative_state
    mirror_or_self_host_class: self_host_supported_with_host_match
cited_records:
  - kind: imported_provider_snapshot_record
    fixture_ref: fixtures/providers/provider_mode_cases/artifact_registry_inspect_only_snapshot.json
    note: >-
      Inspect-only snapshot grouped under an `import_session_record`
      with the typed source, freshness, actor scope, and trust
      posture.
acceptance_proof:
  - claim: per-object honesty
    proof: >-
      Package-version mutate posture is `rides_release_lane`; the
      sibling registry-entry row stays `not_supported` and is
      browser-only by contract.
  - claim: typed snapshot
    proof: >-
      The inspect-only result is wrapped in an
      `imported_provider_snapshot_record`; rendering it as locally
      authored is forbidden.
  - claim: release-lane discipline
    proof: >-
      Publishing a new package version rides the release lane with a
      provider_consequence_preview; an inline publish_now button on
      this surface is forbidden.
