__fixture__:
  name: managed_admin__admin_surface
  case_id: provider_qualification.managed_admin__admin_surface.v1
  scenario: >-
    A managed-admin provider qualifies for the
    `managed_admin_provider__admin_surface` matrix row. Admin
    surfaces inspect-only with the delegated-credential acting-
    identity badge; mutation is browser-only via the browser-handoff
    sheet with a typed `object_link_anchor` return; raw URL launches
    are forbidden.
  exercises:
    matrix_row_id: managed_admin_provider__admin_surface
    provider_class: managed_admin_provider
    object_class: admin_surface
    inspect_support_class: bounded_partial_snapshot
    in_product_mutate_support_class: not_supported
    browser_only_posture_class: browser_only_full_object
    local_draft_support_class: not_applicable
    publish_later_support_class: not_applicable
    callback_or_webhook_requirement_class: optional_freshness_only
    mirror_or_self_host_class: host_pinned_authoritative_only
cited_records:
  - kind: delegated_credential_binding_record
    fixture_ref: fixtures/providers/connected_account_cases/delegated_credential_browser_only_admin_surface.yaml
    note: >-
      Delegated-credential binding with the
      `delegated_label` acting-identity badge and a
      `browser_only` decision class.
acceptance_proof:
  - claim: per-object honesty
    proof: >-
      Admin-surface mutate posture is `not_supported`; sibling
      audit-entry row is `full_in_product_alternative` and is
      surfaced separately.
  - claim: browser-handoff sheet contract
    proof: >-
      Every admin-surface mutation routes through the typed
      browser-handoff sheet record with a return-anchor and a
      privacy note.
  - claim: acting-identity honesty
    proof: >-
      The delegated badge is rendered on the desktop header, the
      desktop notification, the audit-event payload, and the
      support-export row.
