__fixture__:
  name: issue_or_planning_tracker__issue_or_work_item
  case_id: provider_qualification.issue_or_planning_tracker__issue_or_work_item.v1
  scenario: >-
    An issue / planning tracker qualifies for the
    `issue_or_planning_tracker__issue_or_work_item` matrix row.
    Issue bodies and comments author locally with `local_draft`,
    publish through `publish_now` or `deferred_publish`, and offline
    capture admits a queue item with
    `mode_admission_reason = offline_capture` rather than reporting
    "done".
  exercises:
    matrix_row_id: issue_or_planning_tracker__issue_or_work_item
    provider_class: issue_or_planning_tracker
    object_class: issue_or_work_item
    inspect_support_class: full_typed_snapshot
    in_product_mutate_support_class: publish_now_supported
    browser_only_posture_class: browser_only_for_some_actions
    local_draft_support_class: supported
    publish_later_support_class: supported
    callback_or_webhook_requirement_class: required_for_authoritative_state
    mirror_or_self_host_class: self_host_supported_with_host_match
cited_records:
  - kind: publish_later_queue_item_record
    fixture_ref: fixtures/providers/provider_mode_cases/issue_tracker_local_draft_offline_capture.json
    note: >-
      Offline-capture publish-later queue item with the typed
      `mode_admission_reason = offline_capture` and an explicit
      reopen-on-connectivity-restored prerequisite.
acceptance_proof:
  - claim: local-draft visibility
    proof: >-
      The local-draft surface header shows `local_draft` mutation
      mode; the queued offline capture is visibly distinct from a
      published issue.
  - claim: typed mutation modes
    proof: >-
      Custom workflow transitions that the API does not expose route
      through the browser-handoff sheet rather than collapsing into
      `publish_now`.
  - claim: callback envelope round trip
    proof: >-
      The publish_now path is bound to a callback envelope returning
      to the `issue_draft_anchor` on the originating local draft.
