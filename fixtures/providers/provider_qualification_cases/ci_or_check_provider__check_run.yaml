__fixture__:
  name: ci_or_check_provider__check_run
  case_id: provider_qualification.ci_or_check_provider__check_run.v1
  scenario: >-
    A CI / check provider qualifies for the
    `ci_or_check_provider__check_run` matrix row. Rerun requests
    admit through the publish-later queue or route through the
    browser-handoff sheet; inline `publish_now` is forbidden so a
    check-run mutate never collapses into the broader code-host
    claim.
  exercises:
    matrix_row_id: ci_or_check_provider__check_run
    provider_class: ci_or_check_provider
    object_class: check_run
    inspect_support_class: full_typed_snapshot
    in_product_mutate_support_class: deferred_publish_only
    browser_only_posture_class: browser_only_for_some_actions
    local_draft_support_class: not_applicable
    publish_later_support_class: supported
    callback_or_webhook_requirement_class: required_for_authoritative_state
    mirror_or_self_host_class: self_host_supported_with_host_match
cited_records:
  - kind: provider_callback_envelope_record
    fixture_ref: fixtures/providers/provider_mode_cases/ci_rerun_open_in_provider.json
    note: >-
      Rerun request routed through `open_in_provider` with a typed
      return to the originating check-run row; alternative
      `deferred_publish` admission is allowed.
  - kind: provider_sync_health_view_record
    fixture_ref: fixtures/providers/provider_sync_health_cases/ci_checks_partial_mode_missing_pages.yaml
    note: >-
      Sync-health view in `partial` mode with
      `missing_page_detected`, illustrating the partial-import
      class this row must disclose honestly.
acceptance_proof:
  - claim: per-object honesty
    proof: >-
      The check-run row carries `deferred_publish_only` mutation
      support; the sibling code-host check-run row keeps mutate
      `not_supported` and routes here.
  - claim: typed mutation modes
    proof: >-
      Inline `publish_now` for a check-run rerun is forbidden by
      contract; the queue or the browser-handoff sheet is the only
      lane.
  - claim: degraded import disclosure
    proof: >-
      Sync-health partial mode is rendered with the typed
      `degraded_import_class = partial`, never as an authoritative
      check-run roster.
