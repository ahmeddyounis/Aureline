__fixture__:
  name: release_publisher__release_artifact
  case_id: provider_qualification.release_publisher__release_artifact.v1
  scenario: >-
    A release publisher qualifies for the
    `release_publisher_provider__release_artifact` matrix row.
    Publishing flows through release evidence with a typed
    `provider_consequence_preview_record` whose
    `irreversibility_class = irreversible_release_class`; an
    immediate publish_now button is forbidden by contract.
  exercises:
    matrix_row_id: release_publisher_provider__release_artifact
    provider_class: release_publisher_provider
    object_class: release_artifact
    inspect_support_class: full_typed_snapshot
    in_product_mutate_support_class: deferred_publish_only
    browser_only_posture_class: browser_only_for_some_actions
    local_draft_support_class: not_applicable
    publish_later_support_class: supported
    callback_or_webhook_requirement_class: required_for_authoritative_state
    mirror_or_self_host_class: self_host_supported_with_host_match
cited_records:
  - kind: publish_later_queue_item_record
    fixture_ref: fixtures/providers/provider_mode_cases/release_publisher_deferred_publish_preview.json
    note: >-
      Deferred-publish queue item with a typed
      provider_consequence_preview_record carrying
      `irreversibility_class = irreversible_release_class`.
  - kind: provider_callback_envelope_record
    fixture_ref: fixtures/providers/provider_mode_cases/webhook_dedup_redelivery.json
    note: >-
      Webhook redelivery is dedup-keyed; release callback re-delivery
      refreshes freshness only and does not double-publish.
acceptance_proof:
  - claim: typed mutation modes
    proof: >-
      Release publish always rides the deferred-publish lane with a
      preview; raw publish_now is forbidden.
  - claim: callback envelope round trip
    proof: >-
      The webhook redelivery cite proves dedup-key idempotence; a
      duplicate delivery refreshes freshness only.
  - claim: support export honesty
    proof: >-
      Audit packet cites the queue item id, the preview hash, and the
      callback envelope id without raw bodies.
