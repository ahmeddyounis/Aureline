__fixture__:
  name: docs_or_portal_provider__docs_page
  case_id: provider_qualification.docs_or_portal_provider__docs_page.v1
  scenario: >-
    A docs / portal provider qualifies for the
    `docs_or_portal_provider__docs_page` matrix row. Docs-page edits
    author locally and publish through `deferred_publish` after a
    review pass; an account switch between queue and drain transitions
    the queue item to `awaiting_account_reselection` rather than
    silently publishing under a new actor class.
  exercises:
    matrix_row_id: docs_or_portal_provider__docs_page
    provider_class: docs_or_portal_provider
    object_class: docs_page
    inspect_support_class: full_typed_snapshot
    in_product_mutate_support_class: deferred_publish_only
    browser_only_posture_class: browser_only_for_some_actions
    local_draft_support_class: supported
    publish_later_support_class: supported
    callback_or_webhook_requirement_class: optional_freshness_only
    mirror_or_self_host_class: self_host_supported_with_host_match
cited_records:
  - kind: publish_later_queue_item_record
    fixture_ref: fixtures/providers/provider_mode_cases/docs_portal_queue_item_account_switch.json
    note: >-
      Queue item with `mode_admission_reason = account_mapping_pending`
      and the queue state `awaiting_account_reselection`.
acceptance_proof:
  - claim: typed mutation modes
    proof: >-
      Inline `publish_now` is forbidden on docs surfaces; the
      deferred-publish queue is the only lane and account switches
      visibly hold the queue item.
  - claim: account-mapping continuity
    proof: >-
      The cited queue item carries an
      `account_mapping_binding_record` in
      `pending_user_selection`; drain refuses until the user
      reselects.
  - claim: support export honesty
    proof: >-
      A migration note for this row preserves the original local
      draft, the queue item id, and the account-mapping state without
      collapsing into a single "published" claim.
