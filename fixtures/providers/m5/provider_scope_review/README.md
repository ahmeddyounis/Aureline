# Provider Scope Review Fixtures

- `page.json` is the seeded provider scope-review page.
- The packet exercises human-account, installation-grant, delegated-credential,
  browser-only, denied host-mismatch, and invalidation flows.
- Consumer projections prove that desktop, CLI/headless, companion, and
  support/export surfaces all quote the same canonical decision object.
