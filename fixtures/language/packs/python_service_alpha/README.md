# Python Service Language-Pack Alpha Fixtures

These fixtures prove the Python service/data pack enables Python, Markdown, and
local Git review surfaces from one canonical artifact. They are intentionally
metadata-only: no partner source, raw diffs, terminal output, or private paths
are embedded.

Run:

```sh
cargo test -p aureline-language --test python_service_pack_alpha
```
