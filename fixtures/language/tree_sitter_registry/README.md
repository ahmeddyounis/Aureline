# Tree-sitter Registry Fixtures

These fixtures cover the launch-language syntax substrate. They prove that
launch grammars resolve through one registry, parser state is surfaced as a
parse-session record, and missing grammars degrade with explicit fallback
labels instead of hiding inside editor code.
