schema_version: 1
case_id: lang:layer_case:docs:claim_requires_layers:01

feature_family: graph_overlays
surface_family: docs

claim_rule:
  forbidden_copy_patterns:
    - "supports language"
    - "full support"
    - "works for"
  required_structure:
    - name_language_or_file_class
    - enumerate_supported_layer_ids
    - include_scope_limit_and_degraded_state_when_not_full
  examples:
    acceptable:
      - "Syntax/structure available (Layer 1). Compatibility providers may be installed separately (Layer 2). Semantic depth is not claimed unless graph-backed (Layer 3)."
    unacceptable:
      - "Supports this language."

