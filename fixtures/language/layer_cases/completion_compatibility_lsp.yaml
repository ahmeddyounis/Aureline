schema_version: 1
case_id: lang:layer_case:completion:compatibility_only:01

feature_family: completion
surface_family: editor

requested_layer_id: layer_3_aureline_semantic_depth
answer_layer_id: layer_2_compatibility_breadth

available_layers:
  - layer_1_syntax_structure
  - layer_2_compatibility_breadth
unavailable_deeper_layers:
  - layer_id: layer_3_aureline_semantic_depth
    reason_class: partial_scope
    summary: Graph is warm only for a subset of the workspace and cannot claim completion for this request.

downgrade:
  from_layer_id: layer_3_aureline_semantic_depth
  to_layer_id: layer_2_compatibility_breadth
  degraded_state_token: partial
  summary: Completion uses compatibility provider results while semantic depth is partial for this scope.

required_surface_disclosure:
  must_render:
    - answer_layer_id
    - provider_disclosure
    - deeper_layer_unavailable_reason
  must_include_copy_intent:
    - name_the_provider_family_and_scope_limit
  must_not_imply:
    - aureline_owned_semantic_proof

