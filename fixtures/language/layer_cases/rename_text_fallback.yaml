schema_version: 1
case_id: lang:layer_case:rename:text_fallback:01

feature_family: rename
surface_family: refactor

requested_layer_id: layer_3_aureline_semantic_depth
answer_layer_id: layer_1_syntax_structure

available_layers:
  - layer_1_syntax_structure
unavailable_deeper_layers:
  - layer_id: layer_2_compatibility_breadth
    reason_class: offline
    summary: Compatibility provider is offline for this workspace.
  - layer_id: layer_3_aureline_semantic_depth
    reason_class: unsupported
    summary: Semantic depth is unsupported for this language/file class.

downgrade:
  from_layer_id: layer_3_aureline_semantic_depth
  to_layer_id: layer_1_syntax_structure
  degraded_state_token: unsupported
  summary: Rename may proceed only as explicit text-only transform with strict scope warnings.

required_surface_disclosure:
  must_render:
    - answer_layer_id
    - explicit_scope_warning
    - preview_required
    - deeper_layer_unavailable_reason
  must_not_imply:
    - semantic_rename
    - workspace_wide_correctness

