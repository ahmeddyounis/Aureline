# Performance Capture Cases

These fixtures exercise the profiling, trace, replay, and regression
evidence contract. They are intentionally small and metadata-only; raw
trace chunks, memory payloads, command lines, paths outside the
repository, and environment values are represented by opaque refs.

The manifest lists which schema each file is expected to satisfy after a
YAML-to-JSON conversion.
