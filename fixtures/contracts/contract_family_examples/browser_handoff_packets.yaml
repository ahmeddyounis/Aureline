# yaml-language-server: $schema=../../../schemas/contracts/contract_family_registry.schema.json
$schema: "https://aureline.dev/schemas/contracts/contract_family_registry.schema.json"
record_kind: contract_family_example_record
contract_family_example_schema_version: 1
example_id: browser_handoff_packets_exported_as_evidence
family_id: browser_handoff_packets
title: Browser handoff is a typed packet, not a raw URL launch
scenario: >
  Provider handoff is expressed as a typed packet with destination class, reason
  code, privacy consequence disclosure, expiry, and return anchors so support
  and review flows can export and audit the transition safely.
surface_bindings:
  cli_headless_output_refs: []
  desktop_export_refs:
    - docs/adr/0010-connected-provider-browser-handoff-approval-ticket.md
  support_bundle_refs:
    - docs/support/support_bundle_contract.md
  release_packet_refs:
    - docs/release/release_evidence_packet_template.md
  provider_browser_handoff_refs:
    - schemas/integration/browser_handoff_packet.schema.json

