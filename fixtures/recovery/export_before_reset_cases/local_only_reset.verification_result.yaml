# yaml-language-server: $schema=../../../schemas/recovery/export_verification_result.schema.json
__fixture__:
  name: local_only_reset.verification_result
  scenario: >
    The verification-result record matching the local_only_reset
    checklist. Six rows verified; the credentials row carries
    result_class = unsupported_class with the
    skip_unsupported_class_acknowledged follow-up. The reset-
    authorization gate sits at authorized_for_reset because every
    non-verified row carries a typed acknowledgement_ref.
  doc_sections:
    - "§15 Record model — export-verification-result"
    - "§16 Reset-authorization-state vocabulary"
    - "§17 Honesty invariants — verification result"

record_kind: export_verification_result_record
export_verification_result_schema_version: 1
verification_id: evr-credential-store-local-only-0001
checklist_id_ref: ebr-credential-store-local-only-0001
generated_at: "mono:7012:11:42:01.0042"

overall_result_class: partial

verification_rows:
  - row_id_ref: ebr-row-workspace-state-0001
    result_class: verified
    summary: >
      Workspace bytes verified; checksum matches.
  - row_id_ref: ebr-row-profile-defaults-0001
    result_class: verified
    summary: >
      Profile defaults verified.
  - row_id_ref: ebr-row-layout-topology-0001
    result_class: verified
    summary: >
      Layout topology verified.
  - row_id_ref: ebr-row-evidence-packets-0001
    result_class: verified
    summary: >
      Evidence packets verified.
  - row_id_ref: ebr-row-local-history-lane-0001
    result_class: verified
    summary: >
      Local-history lane verified.
  - row_id_ref: ebr-row-autosave-journal-0001
    result_class: verified
    summary: >
      Autosave journal verified.
  - row_id_ref: ebr-row-credentials-0001
    result_class: unsupported_class
    follow_up_guidance_class: skip_unsupported_class_acknowledged
    acknowledgement_ref: acknowledgement:credentials.unsupported.skip.0001
    summary: >
      User acknowledged the unsupported credentials class; reset will
      delete OS-level credential entries.

reset_authorization_state_class: authorized_for_reset

decline_acknowledgement_ref: acknowledgement:no_undo.credentials.0001

summary: >
  Six classes verified; credentials acknowledged unsupported. Reset is
  authorized.

honesty_invariants:
  every_row_has_typed_result: true
  non_verified_row_has_typed_followup: true
  unsupported_or_policy_row_visible_in_overall: true
  reset_authorization_is_typed: true
  linkage_back_to_checklist_is_typed: true
