__fixture__:
  name: extension_quarantine_disable_quarantine
  scenario: >
    A suspect extension is implicated in a crash loop or severe regression.
    The repair isolates the extension without silently re-enabling it and
    without uninstalling unrelated assets.
  doc_sections:
    - "Repair-class matrix (semantic contract)"
    - "Rule: no hidden adjacent durable-state mutation"

record_kind: repair_class_example_record
repair_class_example_schema_version: 1
example_id: rcx-disable-quarantine-0001
generated_at: "mono:6100:02:10:00.0001"

repair_class_group_id: disable_quarantine

stable_support_mapping:
  repair_class_family: extension_isolation
  suggested_repair_class: quarantine_and_bisect_extension
  default_transaction_reversal_class: compensating

preview_minimum_expected:
  must_name_impacted_state_classes:
    - extension_quarantine_state
  must_assert_preserved_state_classes:
    - user_authored_files
    - workspace_trust_store
    - credential_store
    - installed_extension_marketplace_state
  must_include:
    - affected_extension_inventory
    - explicit_disabled_capabilities
    - explicit_reenable_path_requires_review

checkpoint_expectation:
  checkpoint_required: false
  reason: >
    Quarantine is a bounded capability-narrowing action; a checkpoint is
    optional unless coupled to other durable changes.

side_effect_scope_expectations:
  allowed_mutations:
    - mark_extension_quarantined
    - disable_auto_activation_for_quarantined_extension
  forbidden_mutations:
    - silent_extension_reinstall
    - uninstall_unrelated_extensions
    - mutate_user_authored_files

marketing_support_caveat_expectations:
  must_not_claim:
    - permanently_fixed_root_cause
  must_say:
    - disables_or_quarantines_specific_extension
    - reenable_requires_explicit_review_gate

