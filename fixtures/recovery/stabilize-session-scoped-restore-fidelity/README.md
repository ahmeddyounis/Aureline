# Session-Scoped Restore Fidelity Fixtures

These fixtures bind runtime-backed panes to the shared
`schemas/recovery/session-restore-fidelity.schema.json` contract.

The corpus proves that restored terminal, task, debug, notebook, preview, and
remote surfaces preserve layout and evidence without silently rerunning work,
reattaching privileged sessions, or reacquiring authority.

