# Recovery-evidence packet — monitor topology change, context
# restore with topology adjustment note.
#
# Packet shape frozen in:
#   artifacts/qa/restore_qualification_matrix.yaml#recovery_evidence_packet_shape
# Drill row qualified by this packet:
#   artifacts/qa/restore_qualification_matrix.yaml drill_id =
#   monitor_topology_change_layout_only_qualification

__fixture__:
  name: monitor_topology_change_layout_only_qualification
  scenario: >
    A reopen across a monitor topology change — a display was
    removed, added, or redocked between the prior session and this
    launch. Pane intent and split structure remain intact; raw
    pixel coordinates are best-effort metadata only. The chooser
    opens at `context_restore_with_placeholders` with selection
    criterion `compatible_translation_available`; the placeholder/
    downgrade note records the topology-adjustment. The stale
    window-topology snapshot is named on the discarded-or-deferred
    classes. The drill row is the qualification companion to the
    window-display matrix's `display_detach_dock_safe_bounds` and
    `mixed_dpi_cross_monitor_reflow` rows.
  drill_row_ref: drill:monitor_topology_change_layout_only_qualification
  matrix_ref: artifacts/qa/restore_qualification_matrix.yaml
  related_window_display_matrix_rows:
    - display_detach_dock_safe_bounds
    - mixed_dpi_cross_monitor_reflow

drill_id: monitor_topology_change_layout_only_qualification
drill_class: monitor_topology_change_drill
emitted_at: "mono:6806:14:55:01.0220"

chooser_id: chooser-monitor-topology-change-0001
chosen_recovery_level_class: context_restore_with_placeholders
selection_criterion_class: compatible_translation_available
risk_class: restore_no_rerun_no_reattach
reversibility_class: exact_undo

retained_artifact_classes:
  - restore_manifest
  - window_topology_snapshot
  - workspace_authority_checkpoint
  - restore_provenance_record

discarded_or_deferred_artifact_classes:
  - stale_window_topology_snapshot

action_chosen_id: restore_now
compare_before_discard_hook_class: placeholder_inspect_only
export_before_reset_linkage_class: not_applicable_no_destructive_action
placeholder_or_downgrade_note_class: monitor_topology_adjustment_note

claimed_profile_id: linux_ubuntu_24_04_gnome_wayland_x86_64

upstream_chooser_record_ref: chooser-monitor-topology-change-0001
upstream_checkpoint_inventory_record_ref: checkpoint_inventory:window_topology_snapshot.0001
upstream_export_before_reset_checklist_ref: null
upstream_export_verification_result_ref: null
upstream_recovery_scenario_card_ref: null
upstream_hydration_phase_event_refs:
  - hydration_phase_event:shell_skeleton_ready_index_warmup_pending.0001

rerun_expectation_class: rerun_required_claimed_profile_added

parity_expectations_proved:
  - chosen_recovery_level_matches_selection_criterion
  - retained_and_discarded_classes_named_no_silent_omission
  - placeholder_or_downgrade_note_visible_when_applicable
  - chooser_chooser_id_stable_across_surfaces
  - rerun_expectation_explicit_when_protected_surface_changes

fixture_ref: fixtures/recovery/qualification_cases/monitor_topology_change_layout_only_qualification.yaml

honesty_invariants:
  upstream_chooser_record_cited: true
  retained_classes_named_explicitly: true
  discarded_classes_named_explicitly: true
  export_before_reset_linkage_typed: true
  rerun_expectation_typed: true
