# Recovery-evidence packet — crash recovery, exact session restore.
#
# Packet shape frozen in:
#   artifacts/qa/restore_qualification_matrix.yaml#recovery_evidence_packet_shape
# Drill row qualified by this packet:
#   artifacts/qa/restore_qualification_matrix.yaml drill_id =
#   crash_recovery_exact_session_qualification

__fixture__:
  name: crash_recovery_exact_session_qualification
  scenario: >
    A clean reopen on a workspace whose runtime survived under the
    same compatible state contract — the chooser opens at
    `exact_session_restore`. No destructive action runs; the
    evidence packet records that nothing was discarded and that
    no export-before-reset checklist was minted because no reset
    is in scope. The same `chooser_id` is reused by Start Center,
    diagnostics, and any support-export preview the launch
    produces.
  drill_row_ref: drill:crash_recovery_exact_session_qualification
  matrix_ref: artifacts/qa/restore_qualification_matrix.yaml
  upstream_chooser_fixture_ref: fixtures/recovery/recovery_level_cases/exact_session_restore.yaml

drill_id: crash_recovery_exact_session_qualification
drill_class: crash_recovery_drill
emitted_at: "mono:6800:09:14:02.0001"

chooser_id: chooser-exact-session-restore-0001
chosen_recovery_level_class: exact_session_restore
selection_criterion_class: prior_runtime_survived_compatible_contract
risk_class: restore_no_rerun_no_reattach
reversibility_class: exact_undo

retained_artifact_classes:
  - restore_manifest
  - dirty_buffer_recovery_journal
  - local_history_journal
  - workspace_authority_checkpoint
  - restore_provenance_record

discarded_or_deferred_artifact_classes:
  - nothing_discarded_explicit

action_chosen_id: restore_now
compare_before_discard_hook_class: no_compare_required
export_before_reset_linkage_class: not_applicable_no_destructive_action
placeholder_or_downgrade_note_class: no_placeholder_or_downgrade

claimed_profile_id: macos_15_plus_universal

upstream_chooser_record_ref: chooser-exact-session-restore-0001
upstream_checkpoint_inventory_record_ref: checkpoint_inventory:exact_session_restore.0001
upstream_export_before_reset_checklist_ref: null
upstream_export_verification_result_ref: null
upstream_recovery_scenario_card_ref: null
upstream_hydration_phase_event_refs:
  - hydration_phase_event:shell_skeleton_ready_index_warmup_pending.0001
  - hydration_phase_event:workspace_authority_rebind_restore_complete.0001

rerun_expectation_class: rerun_required_chooser_vocabulary_extended

parity_expectations_proved:
  - chosen_recovery_level_matches_selection_criterion
  - retained_and_discarded_classes_named_no_silent_omission
  - compare_before_discard_hook_typed
  - chooser_chooser_id_stable_across_surfaces
  - crash_drill_class_distinct_from_update_drill_class
  - placeholder_or_downgrade_note_visible_when_applicable

fixture_ref: fixtures/recovery/qualification_cases/crash_recovery_exact_session_qualification.yaml

honesty_invariants:
  upstream_chooser_record_cited: true
  retained_classes_named_explicitly: true
  discarded_classes_named_explicitly: true
  export_before_reset_linkage_typed: true
  rerun_expectation_typed: true
