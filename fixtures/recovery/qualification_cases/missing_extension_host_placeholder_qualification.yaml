# Recovery-evidence packet — missing extension host, context restore
# with placeholder pane.
#
# Packet shape frozen in:
#   artifacts/qa/restore_qualification_matrix.yaml#recovery_evidence_packet_shape
# Drill row qualified by this packet:
#   artifacts/qa/restore_qualification_matrix.yaml drill_id =
#   missing_extension_host_placeholder_qualification

__fixture__:
  name: missing_extension_host_placeholder_qualification
  scenario: >
    A reopen where the extension host that owned a pane is gone —
    the extension was removed, the grant was revoked, or the host
    binary is missing. The pane slot, tab order, and surrounding
    split structure remain intact; only the unavailable pane
    degrades to a placeholder. The chooser opens at
    `context_restore_with_placeholders` with selection criterion
    `missing_dependency_layout_only`. Live authority is never
    silently rebound. The drill is the qualification companion to
    the window-display matrix's
    `restart_reopen_missing_dependency_placeholder` row.
  drill_row_ref: drill:missing_extension_host_placeholder_qualification
  matrix_ref: artifacts/qa/restore_qualification_matrix.yaml
  upstream_chooser_fixture_ref: fixtures/recovery/recovery_level_cases/context_restore_with_placeholders.yaml
  related_window_display_matrix_rows:
    - restart_reopen_missing_dependency_placeholder

drill_id: missing_extension_host_placeholder_qualification
drill_class: missing_extension_host_drill
emitted_at: "mono:6807:17:02:48.0061"

chooser_id: chooser-missing-extension-host-0001
chosen_recovery_level_class: context_restore_with_placeholders
selection_criterion_class: missing_dependency_layout_only
risk_class: restore_no_rerun_no_reattach
reversibility_class: exact_undo

retained_artifact_classes:
  - restore_manifest
  - window_topology_snapshot
  - placeholder_pane_slot
  - local_history_journal
  - restore_provenance_record

discarded_or_deferred_artifact_classes:
  - revoked_extension_grant

action_chosen_id: restore_now
compare_before_discard_hook_class: placeholder_inspect_only
export_before_reset_linkage_class: not_applicable_no_destructive_action
placeholder_or_downgrade_note_class: missing_extension_host_placeholder

claimed_profile_id: linux_ubuntu_24_04_gnome_x11_x86_64

upstream_chooser_record_ref: chooser-missing-extension-host-0001
upstream_checkpoint_inventory_record_ref: checkpoint_inventory:placeholder_pane_slot.0001
upstream_export_before_reset_checklist_ref: null
upstream_export_verification_result_ref: null
upstream_recovery_scenario_card_ref: null
upstream_hydration_phase_event_refs:
  - hydration_phase_event:placeholder_hydration_missing_extension.0001

rerun_expectation_class: rerun_required_protected_surface_changed

parity_expectations_proved:
  - chosen_recovery_level_matches_selection_criterion
  - retained_and_discarded_classes_named_no_silent_omission
  - placeholder_or_downgrade_note_visible_when_applicable
  - chooser_chooser_id_stable_across_surfaces

fixture_ref: fixtures/recovery/qualification_cases/missing_extension_host_placeholder_qualification.yaml

honesty_invariants:
  upstream_chooser_record_cited: true
  retained_classes_named_explicitly: true
  discarded_classes_named_explicitly: true
  export_before_reset_linkage_typed: true
  rerun_expectation_typed: true
