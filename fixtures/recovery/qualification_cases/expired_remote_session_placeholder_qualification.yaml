# Recovery-evidence packet — expired remote session, context restore
# with placeholder pane and required regrant.
#
# Packet shape frozen in:
#   artifacts/qa/restore_qualification_matrix.yaml#recovery_evidence_packet_shape
# Drill row qualified by this packet:
#   artifacts/qa/restore_qualification_matrix.yaml drill_id =
#   expired_remote_session_placeholder_qualification

__fixture__:
  name: expired_remote_session_placeholder_qualification
  scenario: >
    A reopen where the prior session held a remote target whose
    authority has expired between launches (token expired, grant
    revoked, or session window closed). The chooser opens at
    `context_restore_with_placeholders` with selection criterion
    `missing_dependency_layout_only`. The placeholder pane slot
    is preserved with a required-regrant action; any restored
    role badge is restored as local context only and never claims
    live authority. The expired remote authority and any revoked
    collaboration grant are named on the discarded-or-deferred
    classes; silent reauth is forbidden.
  drill_row_ref: drill:expired_remote_session_placeholder_qualification
  matrix_ref: artifacts/qa/restore_qualification_matrix.yaml
  upstream_collab_restore_fixture_ref: fixtures/recovery/collab_restore_cases/expired_collaboration_session_evidence_only.yaml

drill_id: expired_remote_session_placeholder_qualification
drill_class: expired_remote_session_drill
emitted_at: "mono:6808:19:33:11.0143"

chooser_id: chooser-expired-remote-session-0001
chosen_recovery_level_class: context_restore_with_placeholders
selection_criterion_class: missing_dependency_layout_only
risk_class: restore_no_rerun_no_reattach
reversibility_class: exact_undo

retained_artifact_classes:
  - restore_manifest
  - placeholder_pane_slot
  - role_badge_local_context
  - restore_provenance_record

discarded_or_deferred_artifact_classes:
  - expired_remote_authority
  - revoked_collaboration_grant

action_chosen_id: restore_now
compare_before_discard_hook_class: placeholder_inspect_only
export_before_reset_linkage_class: not_applicable_no_destructive_action
placeholder_or_downgrade_note_class: missing_remote_target_placeholder

claimed_profile_id: linux_fedora_current_gnome_wayland_x86_64

upstream_chooser_record_ref: chooser-expired-remote-session-0001
upstream_checkpoint_inventory_record_ref: checkpoint_inventory:remote_session_placeholder.0001
upstream_export_before_reset_checklist_ref: null
upstream_export_verification_result_ref: null
upstream_recovery_scenario_card_ref: null
upstream_hydration_phase_event_refs:
  - hydration_phase_event:downgraded_from_live_after_authority_expired.0001
  - hydration_phase_event:live_dependency_rebind_remote_target.0001

rerun_expectation_class: rerun_required_protected_surface_changed

parity_expectations_proved:
  - chosen_recovery_level_matches_selection_criterion
  - retained_and_discarded_classes_named_no_silent_omission
  - placeholder_or_downgrade_note_visible_when_applicable
  - chooser_chooser_id_stable_across_surfaces

fixture_ref: fixtures/recovery/qualification_cases/expired_remote_session_placeholder_qualification.yaml

honesty_invariants:
  upstream_chooser_record_cited: true
  retained_classes_named_explicitly: true
  discarded_classes_named_explicitly: true
  export_before_reset_linkage_typed: true
  rerun_expectation_typed: true
