schema_version: 1
record_kind: repair_preview_skeleton_record
skeleton_id: repair_preview_skeleton:disposable_state_rebuild.cache_index_repair
repair_transaction_ref: repair_transaction:disposable_state_rebuild.cache_index_repair
reversal_class: regenerate
blast_radius_class: single_object_class
compensation_class: regenerate_from_authoritative_source
checkpoint_disposition_class: durable_pre_apply_checkpoint
preview_disposition_class: cancellable_pending_review
affected_object_rows:
  - affected_object_class: derived_cache_object
    change_summary: Disposable derived cache for the loaded build is rebuilt from authoritative sources behind a reviewed checkpoint.
preserved_state_classes:
  - user_authored_files
  - open_buffer_selection
  - durable_workspace_indexes
  - workspace_trust_store
  - credential_store
  - managed_policy_overrides
  - session_restore_store
  - support_export_store
  - installed_extension_marketplace_state
  - managed_workspace_control_plane
doctor_finding_ref: doctor.finding.cache_schema_version_drift
checkpoint_ref: checkpoint:cache-reset:seed
support_packet_ref: support:repair-preview-beta:cache-index-rebuild
destructive_resets_present: false
explanation:
  change_summary: Disposable derived cache for the loaded build is rebuilt from authoritative sources.
  preserved_summary: Authored files, durable workspace indexes, trust posture, credentials, policy, session restore state, and the installed extension set all remain untouched.
  compensation_summary: Regenerates disposable derived state from authoritative sources; no semantic inverse is required.
  checkpoint_summary: A durable pre-apply checkpoint is available before mutation; apply may roll back to the checkpoint on failure.
  user_facing_next_step: Compare the candidate skeleton against the prior baseline, then cancel or authorize.
captured_at: "2026-05-15T08:30:00Z"
