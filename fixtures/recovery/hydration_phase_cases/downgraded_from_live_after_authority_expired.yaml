# yaml-language-server: $schema=../../../schemas/recovery/hydration_phase_event.schema.json
__fixture__:
  name: downgraded_from_live_after_authority_expired
  scenario: >
    Semantic search was previously emitted_live during a prior
    placeholder_hydration completion; the workspace-index
    authority has now expired and the cue regresses. The event
    rides downgraded_from_live with a typed partiality reason
    (expired_authority on the index target). Surfaces cannot
    quietly drop the cue — the regression is named.
  doc_sections:
    - "§4 Cue-transition vocabulary"
    - "§5 Partiality block"
    - "§7 Honesty invariants"
    - "§8 Surface rules"

record_kind: hydration_phase_event_record
hydration_phase_event_schema_version: 1
event_id: event-semantic-ready-downgrade-0001
emitted_at: "mono:6112:09:14:02.0902"

phase_class: placeholder_hydration
phase_state: stalled
surface_family: diagnostics_panel
startup_state_ref: "startup_state:partial_startup"
recovery_level_class_ref: context_restore_with_placeholders

cue_transitions:
  - cue: semantic_ready
    transition: downgraded_from_live
    target_ref: null
    prior_event_ref: event-semantic-ready-upgrade-0001
    summary: >
      Workspace-index authority expired; semantic search regresses
      to name-only candidates. The cue is downgraded, not
      withdrawn — the user is offered a reauthorize path.

partiality_block:
  partiality_classes:
    - expired_authority
    - pending_index_warmup
  stalled_rebind_target_refs: []
  missing_dependency_class_refs: []
  expired_authority_target_refs:
    - workspace_index_authority:0001
  pending_index_warmup_class: workspace_index_warmup
  progress_visible: true
  summary: >
    Workspace-index authority expired and a warmup pass is
    queued. Semantic search regressed to name-only candidates
    until the user reauthorizes.

packet_export_linkage:
  phase_session_id: hyd-session-warm-start-0001
  restore_chooser_state_ref: chooser-context-restore-0001
  restore_provenance_record_ref: provenance:warm-start-0001
  window_topology_snapshot_ref: window_topology_snapshot:warm-start-0001
  workspace_authority_checkpoint_ref: workspace_authority_checkpoint:warm-start-0001
  recovery_ladder_packet_ref: recovery_ladder_packet:default.0001
  support_bundle_candidate_ref: null
  benchmark_trace_session_ref: spike_timing_session:warm_start.0001
  release_evidence_packet_ref: null
  docs_help_label: >
    Semantic search regressed after workspace-index authority
    expired. The cue is downgraded, not silently withdrawn.

accessibility_contract:
  keyboard_reachable_actions: true
  focus_target_ref: focus:diagnostics.semantic_search.expired_authority
  screen_reader_summary: >
    Semantic search regressed to name-only after the workspace
    index authority expired. Reauthorize to restore index-backed
    results.

honesty_invariants:
  phase_vocabulary_is_closed: true
  ready_cues_are_distinct: true
  partiality_is_named: true
  no_silent_cue_redefinition: true
  privileged_surfaces_never_live_before_rebind: true
  linkage_is_typed: true
