# yaml-language-server: $schema=../../../schemas/recovery/hydration_phase_event.schema.json
__fixture__:
  name: shell_skeleton_ready_index_warmup_pending
  scenario: >
    Shell skeleton finishes painting; quick open, command entry,
    and the first editor become interactive. The local file index
    has not finished warming, so search_ready is emitted_degraded
    while the four core shell cues are emitted_live. No
    privileged surface is claimed live; rebind-complete cues are
    deferred to live_dependency_rebind.
  doc_sections:
    - "§2 Phase vocabulary"
    - "§3 Ready-cue vocabulary"
    - "§4 Cue-transition vocabulary"
    - "§5 Partiality block"

record_kind: hydration_phase_event_record
hydration_phase_event_schema_version: 1
event_id: event-shell-skeleton-ready-0001
emitted_at: "mono:6112:09:14:02.0103"

phase_class: shell_skeleton
phase_state: completed
surface_family: shell_chrome
startup_state_ref: "startup_state:reopen_with_pending_restore"
recovery_level_class_ref: context_restore_with_placeholders

cue_transitions:
  - cue: shell_ready
    transition: emitted_live
    target_ref: null
    prior_event_ref: null
    summary: >
      Shell skeleton paints; input focus accepted.
  - cue: quick_open_ready
    transition: emitted_live
    target_ref: null
    prior_event_ref: null
    summary: >
      Quick open serves recently-used and name-only candidates.
  - cue: command_entry_ready
    transition: emitted_live
    target_ref: null
    prior_event_ref: null
    summary: >
      Command palette serves the static command set; provider-
      contributed commands remain pending.
  - cue: first_editor_ready
    transition: emitted_live
    target_ref: null
    prior_event_ref: null
    summary: >
      First editor accepts input against local text source. Save
      may still be deferred until workspace authority rebinds.
  - cue: search_ready
    transition: emitted_degraded
    target_ref: null
    prior_event_ref: null
    summary: >
      Search returns name-only matches; lexical / index-backed
      candidates are pending index warmup.

partiality_block:
  partiality_classes:
    - pending_index_warmup
  stalled_rebind_target_refs: []
  missing_dependency_class_refs: []
  expired_authority_target_refs: []
  pending_index_warmup_class: workspace_index_warmup
  progress_visible: true
  summary: >
    Local file index is still warming; search returns name-only
    matches in the meantime.

packet_export_linkage:
  phase_session_id: hyd-session-warm-start-0001
  restore_chooser_state_ref: chooser-context-restore-0001
  restore_provenance_record_ref: provenance:warm-start-0001
  window_topology_snapshot_ref: window_topology_snapshot:warm-start-0001
  workspace_authority_checkpoint_ref: null
  recovery_ladder_packet_ref: recovery_ladder_packet:default.0001
  support_bundle_candidate_ref: null
  benchmark_trace_session_ref: spike_timing_session:warm_start.0001
  release_evidence_packet_ref: null
  docs_help_label: >
    Shell skeleton ready — quick open, command entry, and the
    first editor are interactive. Search degraded to name-only
    while the index warms.

accessibility_contract:
  keyboard_reachable_actions: true
  focus_target_ref: focus:shell.quick_open
  screen_reader_summary: >
    Shell ready. Quick open, command entry, and the first editor
    are interactive. Search returns name-only matches; the index
    is still warming.

honesty_invariants:
  phase_vocabulary_is_closed: true
  ready_cues_are_distinct: true
  partiality_is_named: true
  no_silent_cue_redefinition: true
  privileged_surfaces_never_live_before_rebind: true
  linkage_is_typed: true
