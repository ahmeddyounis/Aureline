# yaml-language-server: $schema=../../../schemas/recovery/recovery_route_decision.schema.json
__fixture__:
  name: device_replacement_routes_to_restore_chooser
  scenario: >
    A new device is hydrating state under device_replacement. The router
    routes to the restore chooser and hydration phases instead of
    presenting factory reset as an early recovery action.
  doc_sections:
    - "§2 Scenario router — record model"
    - "§3 Scenario router — closed route classes"
    - "§5 Scenario router — required scenario coverage"

record_kind: recovery_route_decision_record
recovery_route_decision_schema_version: 1
decision_id: rrd-device-replacement-0001
generated_at: "mono:8801:11:16:21.2201"

scenario_family_class: device_replacement
deployment_profile_scope_class: managed_tenant

recommended_route_class: restore_chooser_route
title: Device replacement — route to restore chooser
summary: >
  Device replacement routes to restore chooser + hydration phases so the
  user can rehydrate from authoritative sources and review verification
  posture before any destructive cleanup is proposed.

glossary:
  authoritative_backup_definition_const: true
  local_checkpoint_definition_const: true
  sync_replica_definition_const: true
  mirror_cache_definition_const: true
  convenience_export_definition_const: true
  externally_recoverable_artifact_definition_const: true

linkage:
  recovery_scenario_card_ref: card-device-replacement-0001
  restore_chooser_ref: restore-chooser-state-0001
  summary: Restore chooser and hydration posture for rehydration.

honesty_invariants:
  scenario_family_is_closed: true
  recommended_route_is_closed: true
  narrowest_safe_path_preferred: true
  factory_reset_is_not_first_candidate: true
  glossary_terms_frozen: true

