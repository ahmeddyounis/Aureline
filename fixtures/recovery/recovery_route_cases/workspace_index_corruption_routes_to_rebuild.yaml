# yaml-language-server: $schema=../../../schemas/recovery/recovery_route_decision.schema.json
__fixture__:
  name: workspace_index_corruption_routes_to_rebuild
  scenario: >
    A surface is asked to run a reset under workspace_index_corruption.
    The router prefers the narrowest disposable-state rebuild route and
    refuses to route the derived-index case into destructive reset
    flows.
  doc_sections:
    - "§2 Scenario router — record model"
    - "§4 Scenario router — selection rules"
    - "§5 Scenario router — required scenario coverage"

record_kind: recovery_route_decision_record
recovery_route_decision_schema_version: 1
decision_id: rrd-workspace-index-corruption-0001
generated_at: "mono:8801:11:05:19.1220"

scenario_family_class: workspace_index_corruption
deployment_profile_scope_class: individual_local

requested_route_class: export_before_reset_checklist_route
requested_reset_kind_class: workspace_reset

recommended_route_class: corruption_rescue_compare_route
title: Workspace index corruption — route to derived-state rebuild
summary: >
  Derived workspace index corruption routes to a disposable-state
  rebuild (corruption rescue compare sheet constrained to derived-only
  artifacts). Reset-gated destructive flows are forbidden for this
  scenario.

glossary:
  authoritative_backup_definition_const: true
  local_checkpoint_definition_const: true
  sync_replica_definition_const: true
  mirror_cache_definition_const: true
  convenience_export_definition_const: true
  externally_recoverable_artifact_definition_const: true

linkage:
  recovery_scenario_card_ref: card-workspace-index-corruption-0001
  corruption_rescue_sheet_ref: crs-derived-index-0001
  summary: Corruption rescue compare sheet for derived disposable index.

honesty_invariants:
  scenario_family_is_closed: true
  recommended_route_is_closed: true
  narrowest_safe_path_preferred: true
  factory_reset_is_not_first_candidate: true
  glossary_terms_frozen: true

