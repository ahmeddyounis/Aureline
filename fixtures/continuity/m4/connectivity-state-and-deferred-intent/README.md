# Connectivity State And Deferred Intent Fixture

This fixture is the stable replay and support-export corpus for the continuity
contract. It covers the six shared connectivity states, one queueable
idempotent provider intent, a non-queueable Git push declaration, and a
reconciliation review sheet that blocks replay after route, policy, auth,
tenant, region, endpoint, or target drift.
