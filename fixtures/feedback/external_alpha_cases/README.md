# External alpha feedback cases

These fixtures exercise the design-partner intake, task, feedback, known-limit,
and redaction states consumed by `ci/check_design_partner_alpha.py`.

The cases are metadata-only. They intentionally avoid partner names, repository
names, raw paths, raw logs, transcripts, screenshots, customer data, credentials,
and source content.

