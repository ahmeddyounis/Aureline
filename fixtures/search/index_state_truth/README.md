# Indexed State Truth Fixtures

These fixtures prove the shared indexed-state vocabulary for search, graph,
and docs lanes. Each case is materialized by `aureline-search` and consumed by
`aureline-shell` so status rows, result-pane notices, and support artifacts
quote the same `warming`, `partial`, `cached`, `stale`, or `paused` token.
