# Hot-Set Scheduler Alpha Fixtures

These fixtures protect the first-useful navigation path for search. They prove
that quick open can consume a bounded hot-set lexical index before full
indexing completes, and that fallback states remain explicit when no hot-set
input is available.

