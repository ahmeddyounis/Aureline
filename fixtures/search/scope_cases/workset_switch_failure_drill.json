{
  "record_kind": "search_scope_case",
  "schema_version": 1,
  "case_name": "workset_switch_failure_drill",
  "scenario": "Failure drill: the user is mid-search on the 'Hot path' workset (apps/web only) and switches to the 'API surface' workset (apps/api only). The scope chip MUST update to the new family + name, the in-scope file set MUST switch to the new workset's pattern result, and the previous workset's rows MUST NOT leak through as if they were repo-wide truth.",
  "workspace_id": "ws-search-fixture",
  "scope_kind": "workset_switch",
  "from_workset_artifact": {
    "workset_id": "wks:hot:0",
    "workset_name": "Hot path",
    "scope_class": "selected_workset",
    "root_refs": ["fs-r-0"],
    "patterns": [
      { "kind": "include", "pattern": "apps/web/**" }
    ],
    "membership_policy": "glob_pattern",
    "member_partial_truth": "loaded",
    "policy_limited": false,
    "partial_index_note": null
  },
  "to_workset_artifact": {
    "workset_id": "wks:api:0",
    "workset_name": "API surface",
    "scope_class": "selected_workset",
    "root_refs": ["fs-r-0"],
    "patterns": [
      { "kind": "include", "pattern": "apps/api/**" }
    ],
    "membership_policy": "glob_pattern",
    "member_partial_truth": "loaded",
    "policy_limited": false,
    "partial_index_note": null
  },
  "files": [
    "apps/web/main.tsx",
    "apps/api/handler.rs",
    "apps/api/router.rs",
    "src/main.rs"
  ],
  "expect": {
    "before_switch": {
      "scope_class_token": "selected_workset",
      "chip_label": "Selected workset · Hot path",
      "in_scope": ["apps/web/main.tsx"],
      "out_of_scope": [
        "apps/api/handler.rs",
        "apps/api/router.rs",
        "src/main.rs"
      ]
    },
    "after_switch": {
      "scope_class_token": "selected_workset",
      "chip_label": "Selected workset · API surface",
      "in_scope": [
        "apps/api/handler.rs",
        "apps/api/router.rs"
      ],
      "out_of_scope": [
        "apps/web/main.tsx",
        "src/main.rs"
      ]
    }
  }
}
