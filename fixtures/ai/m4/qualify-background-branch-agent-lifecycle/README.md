# Background branch-agent lifecycle fixtures

`lifecycle_packet.json` is the canonical M4 fixture for the governed stable
branch-agent lifecycle lane. It proves launch review, active run, checkpoint,
re-review, operator takeover, completion review, and cleanup rows stay
attributable by `stable_run_id` across UI, support export, and evidence refs.
