# M5 AI Workflow Matrix Fixtures

## held_branch_agent_matrix.json

A matrix fixture where the branch-or-worktree-agent lane is held pending upstream M5 provider graduation. Inline assist and patch review remain stable. Demonstrates that held lanes do not require evidence packets and use `not_applicable` rollback posture.
