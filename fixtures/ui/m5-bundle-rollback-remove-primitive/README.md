# M5 bundle rollback / remove primitive fixtures

Protected mirror of the checked-in support export and matrix CSV under
`artifacts/release/m5-bundle-rollback-remove-primitive-proof/`. Regenerated from
`seeded_m5_bundle_rollback_remove_packet()`.
