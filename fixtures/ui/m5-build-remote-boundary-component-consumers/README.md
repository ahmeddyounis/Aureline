# M5 build/remote-boundary component consumer fixtures

Mirror of `artifacts/release/m5-build-remote-boundary-component-consumer-proof/`. Proves
the run/test/debug, notebook, preview, AI tool-routing, companion-handoff, and
support/export + release-packet consumers all reuse the same frozen build/remote-boundary
component families and vocabulary.
Regenerate with `GEN_BUILD_REMOTE_BOUNDARY_CONSUMER_ARTIFACTS=1 cargo test -p aureline-remote generate_artifacts`.
