# M5 bundle drift / override primitive fixtures

Protected mirror of the checked-in support export and matrix CSV under
`artifacts/release/m5-bundle-drift-override-primitive-proof/`. Regenerated from
`seeded_m5_bundle_drift_override_packet()`.
