# M5 notebook-kernel-output component consumer fixtures

Mirror of `artifacts/release/m5-notebook-kernel-output-component-consumer-proof/`. Proves
the notebook editor, diff/review, debug, AI-context, CLI/headless, and support/export +
release-packet consumers all reuse the same frozen notebook document / kernel / output
component families and vocabulary.
Regenerate with `GEN_NOTEBOOK_KERNEL_OUTPUT_CONSUMER_ARTIFACTS=1 cargo test -p aureline-notebook generate_artifacts`.
