{
  "case_id": "surface-overclaims",
  "kind": "narrowing",
  "description": "A narrowed case whose profile still renders certified floors as an overclaim.",
  "base_case_id": "fallback:native-structured-language-problems:0001",
  "stale_window": false,
  "overrides": {
    "integrity.confidence_label_visible": false,
    "profiles[0].rendered_claim": "fallback_certified",
    "profiles[1].rendered_claim": "fallback_narrowed",
    "profiles[2].rendered_claim": "fallback_narrowed"
  },
  "expected": {
    "effective_claim": "fallback_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "surface_overclaims",
      "confidence_unlabeled"
    ]
  }
}
