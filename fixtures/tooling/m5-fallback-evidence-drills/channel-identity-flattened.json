{
  "case_id": "channel-identity-flattened",
  "kind": "narrowing",
  "description": "An output channel that loses its stable canonical id floors the case.",
  "base_case_id": "fallback:native-structured-language-problems:0001",
  "stale_window": false,
  "overrides": {
    "integrity.channel_identity_stable": false,
    "profiles[*].rendered_claim": "fallback_unreconstructable"
  },
  "expected": {
    "effective_claim": "fallback_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "channel_identity_flattened"
    ]
  }
}
