{
  "case_id": "partial-export-incomplete",
  "kind": "narrowing",
  "description": "A partial export that needs the originating UI to be reviewable floors the case.",
  "base_case_id": "fallback:partial-export-support-bundle:0001",
  "stale_window": false,
  "overrides": {
    "integrity.partial_export_self_contained": false,
    "profiles[*].rendered_claim": "fallback_unreconstructable"
  },
  "expected": {
    "effective_claim": "fallback_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "partial_export_incomplete"
    ]
  }
}
