{
  "case_id": "copy-export-unavailable",
  "kind": "narrowing",
  "description": "An output channel that loses copy/export narrows the case.",
  "base_case_id": "fallback:channel-virtualization-large-log:0001",
  "stale_window": false,
  "overrides": {
    "virtualization.copy_exportable": false,
    "profiles[*].rendered_claim": "fallback_narrowed"
  },
  "expected": {
    "effective_claim": "fallback_narrowed",
    "narrowed": true,
    "active_narrowing_reasons": [
      "copy_export_unavailable"
    ]
  }
}
