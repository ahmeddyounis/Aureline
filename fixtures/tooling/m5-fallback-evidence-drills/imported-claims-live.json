{
  "case_id": "imported-claims-live",
  "kind": "narrowing",
  "description": "An imported overlay claiming live local authority floors below the read-only overlay.",
  "base_case_id": "fallback:imported-provider-annotation:0001",
  "stale_window": false,
  "overrides": {
    "integrity.imported_evidence_read_only": false,
    "profiles[*].rendered_claim": "fallback_unreconstructable"
  },
  "expected": {
    "effective_claim": "fallback_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "imported_overlay_claims_live"
    ]
  }
}
