{
  "case_id": "profile-hides-heuristic-distinction",
  "kind": "narrowing",
  "description": "A single profile that hides the heuristic-versus-structured distinction floors.",
  "base_case_id": "fallback:heuristic-parse-terminal:0001",
  "stale_window": false,
  "overrides": {
    "profiles[0].fallback_visibly_distinct": false,
    "profiles[*].rendered_claim": "fallback_unreconstructable"
  },
  "expected": {
    "effective_claim": "fallback_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "heuristic_indistinct_from_structured"
    ]
  }
}
