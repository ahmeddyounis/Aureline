{
  "case_id": "imported-overlay-clean",
  "kind": "narrowing",
  "description": "An imported provider annotation reused read-only stays a read-only overlay, not narrowed.",
  "base_case_id": "fallback:imported-provider-annotation:0001",
  "stale_window": false,
  "overrides": {},
  "expected": {
    "effective_claim": "fallback_read_only_overlay",
    "narrowed": false,
    "active_narrowing_reasons": []
  }
}
