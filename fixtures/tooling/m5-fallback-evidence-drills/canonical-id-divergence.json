{
  "case_id": "canonical-id-divergence",
  "kind": "narrowing",
  "description": "A profile that points at a different run id breaks the single-canonical-id contract and floors.",
  "base_case_id": "fallback:native-structured-language-problems:0001",
  "stale_window": false,
  "overrides": {
    "profiles[1].bound_run_ref": "run.some.other.0009",
    "profiles[*].rendered_claim": "fallback_unreconstructable"
  },
  "expected": {
    "effective_claim": "fallback_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "canonical_id_divergence"
    ]
  }
}
