{
  "case_id": "source-kind-flattened",
  "kind": "narrowing",
  "description": "Flattening the problem-source class floors the case to a raw fallback.",
  "base_case_id": "fallback:native-structured-language-problems:0001",
  "stale_window": false,
  "overrides": {
    "integrity.preserves_source_kind": false,
    "profiles[*].rendered_claim": "fallback_unreconstructable"
  },
  "expected": {
    "effective_claim": "fallback_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "source_kind_flattened"
    ]
  }
}
