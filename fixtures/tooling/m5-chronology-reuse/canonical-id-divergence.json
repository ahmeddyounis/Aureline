{
  "case_id": "canonical-id-divergence",
  "kind": "narrowing",
  "description": "A support bundle that points at a different run id breaks the single-canonical-id contract and floors.",
  "base_entry_id": "chronology:run-failed-local-test:0001",
  "stale_window": false,
  "overrides": {
    "bindings[3].bound_run_ref": "run.some.other.0009",
    "bindings[*].rendered_claim": "chronology_unreconstructable"
  },
  "expected": {
    "effective_claim": "chronology_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "canonical_id_divergence"
    ]
  }
}
