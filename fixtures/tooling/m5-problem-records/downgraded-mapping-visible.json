{
  "case_id": "downgraded-mapping-visible",
  "kind": "narrowing",
  "description": "A downgraded provenance mapping stays visibly classified rather than silently upgraded.",
  "base_record_id": "problem:local-downgraded-mapping:0001",
  "stale_window": false,
  "overrides": {},
  "expected": {
    "effective_status": "narrowed_actionable",
    "narrowed": true,
    "active_downgrade_reasons": [
      "downgraded_mapping"
    ]
  }
}
