{
  "case_id": "overlay-any-gap-floors",
  "kind": "narrowing",
  "description": "An overlay with any non-floor gap drops below the read-only overlay rather than holding it.",
  "base_projection_id": "projection:pipeline-annotation-provider:0001",
  "stale_window": false,
  "overrides": {
    "revision_remap.remap_quality_labeled": false,
    "renderings[*].rendered_claim": "projection_unreconstructable"
  },
  "expected": {
    "effective_claim": "projection_unreconstructable",
    "narrowed": true,
    "active_narrowing_reasons": [
      "remap_quality_unlabeled"
    ]
  }
}
