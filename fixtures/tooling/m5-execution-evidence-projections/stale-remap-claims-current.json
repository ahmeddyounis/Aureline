{
  "case_id": "stale-remap-claims-current",
  "kind": "narrowing",
  "description": "A stale/unmapped anchor that still claims the current revision narrows.",
  "base_projection_id": "projection:coverage-local-test:0001",
  "stale_window": false,
  "overrides": {
    "revision_remap.quality": "stale_unmapped",
    "revision_remap.anchored_to_current_revision": true,
    "renderings[*].rendered_claim": "projection_narrowed"
  },
  "expected": {
    "effective_claim": "projection_narrowed",
    "narrowed": true,
    "active_narrowing_reasons": [
      "stale_remap_unlabeled"
    ]
  }
}
