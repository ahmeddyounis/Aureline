# Semantic graph object model and query contract fixtures

`baseline_stable.json` pins the canonical stable packet, required object class
tokens, closed query-family vocabulary, required graph-backed consumer surfaces,
and support-export safety expectations.
