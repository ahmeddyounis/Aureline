# yaml-language-server: $schema=../../../schemas/security/text_representation_action.schema.json
__fixture__:
  name: copy_raw_editor_bidi_controls
  scenario: >
    An editor token contains a bidi control. Exact source text is still
    available, so Copy raw is the primary action and Copy escaped must
    remain reachable on the same surface.
  covers:
    - copy_raw
    - bidi_controls
    - cross_surface_label_parity

record_kind: text_representation_action_record
text_representation_action_schema_version: 1
action_row_id: security:text_action:editor:copy_raw:bidi_control:01
policy_transfer_record_ref: case:editor:bidi_identifier#copy_raw
source_surface_family: editor
source_trust_class: RawText
action_id: copy_raw
canonical_label: Copy raw
surface_label: Copy raw token
representation_class: raw
availability_state: available
availability_conditions:
  - exact_source_available
  - suspicious_content_present
  - assistive_label_available
defaulting_rule:
  default_state: primary_default
  reason: Exact source text is available and the editor surface is a raw-text source of truth.
payload_posture:
  body_posture: exact_source_bytes
  raw_source_required: true
  active_content_removed: false
  includes_body: true
required_peer_actions:
  - copy_escaped
required_disclosure_fields:
  - representation_label
  - source_surface
  - trust_class
  - warning_state
unavailable_reason: none
parity_surface_refs:
  - editor
  - diff
  - install_review
  - terminal_transcript
  - security_review
review_note: The surface label may add a target noun, but the canonical stem remains Copy raw.
minted_at: "2026-04-29T16:15:00Z"
