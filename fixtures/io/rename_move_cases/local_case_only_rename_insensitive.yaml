# yaml-language-server: $schema=../../../schemas/io/rename_move_plan.schema.json
fixture_metadata:
  name: local_case_only_rename_insensitive
  fixture_class: local_case_folding
  scenario: Local insensitive-preserving root receives a case-only rename that preserves the canonical object but still requires preview because path and case-fold semantics can diverge.
  expected_next_safe_action: rename_in_place
record_kind: rename_move_plan_record
io_rename_move_plan_schema_version: 1
plan_id: rename.plan.local.case_only.0001
emitted_at: "2026-04-29T13:00:00Z"
initiating_surface: file_tree
operation_intent: rename
current_presentation_path:
  uri: "file:///workspace/src/readme.md"
  display_label: readme.md
  root_badge: local
proposed_presentation_path:
  uri: "file:///workspace/src/README.md"
  display_label: README.md
  root_badge: local
logical_workspace_identity:
  workspace_id: workspace.local.demo
  root_id: root.local.casefold
  logical_uri: "workspace://local-demo/src/readme.md"
  trust_state: trusted
  policy_scope: local_user
current_canonical_object:
  canonical_uri: "file:///workspace/src/readme.md"
  root_class: local_posix_like
  normalization_form: nfc
  object_identity_token:
    kind: device_inode_generation
    value: "dev:1/ino:812/gen:5"
    strength: exact
  fallback_identity_tokens:
    - kind: content_hash_only
      value: "sha256:readme-before"
      strength: heuristic
  alias_set:
    - alias_uri: "file:///workspace/src/README.md"
      alias_kind: case_only_variant
      resolution_chain:
        - "opened file:///workspace/src/readme.md"
        - "case-folded peer file:///workspace/src/README.md"
proposed_canonical_effect:
  effect_class: preserves_canonical_object
  object_relation_after: same_object_new_presentation
  canonical_uri_after: "file:///workspace/src/README.md"
  identity_token_after:
    kind: device_inode_generation
    value: "dev:1/ino:812/gen:5"
    strength: exact
  implication_summary: The rename changes presentation casing only; the canonical object remains the same inode after root capability checks.
capability_context:
  source_root:
    root_id: root.local.casefold
    root_class: local_posix_like
    case_sensitivity: insensitive_preserving
    unicode_normalization: nfc
    supports_atomic_rename: true
    supports_in_place_rename: true
    supports_copy_delete_transfer: true
    supports_conditional_remote_move: false
    supports_alias_detach: false
    supports_generated_relation_update: false
    identity_token_strength: exact
    watcher_state: exact
    read_only: false
    policy_constrained: false
    symlink_escape_policy: warn
  target_root:
    root_id: root.local.casefold
    root_class: local_posix_like
    case_sensitivity: insensitive_preserving
    unicode_normalization: nfc
    supports_atomic_rename: true
    supports_in_place_rename: true
    supports_copy_delete_transfer: true
    supports_conditional_remote_move: false
    supports_alias_detach: false
    supports_generated_relation_update: false
    identity_token_strength: exact
    watcher_state: exact
    read_only: false
    policy_constrained: false
    symlink_escape_policy: warn
  same_root: true
  cross_root_transfer_class: same_root
  required_capability_checks:
    - case_fold_capability
    - canonical_identity_refresh
    - rollback_checkpoint_available
    - policy_epoch
  recompute_required_before_apply: false
review_gate:
  review_required: true
  reviewer_blocking: true
  review_surface_class: rename_move_review_sheet
  review_reason_codes:
    - case_only_rename_on_insensitive_root
    - alias_set_non_empty
  preview_ref: review.rename.case_only.readme.preview
  review_ref: review.rename.case_only.readme
  stale_plan_blocks_apply: true
policy_trust_boundary_notes:
  - boundary_class: none
    summary: The case-only rename stays inside the same trusted local root and policy scope.
    policy_decision_ref: null
rollback_posture:
  checkpoint_class: local_history_checkpoint
  rollback_class: exact_restore
  checkpoint_ref: checkpoint.rename.case_only.readme.before
  journal_ref: mutation.rename.case_only.readme.0001
  rollback_summary: Local history can restore the prior presentation spelling while preserving the same canonical object identity.
next_safe_action:
  action: rename_in_place
  mutation_authority: writes_after_review
  requires_user_confirmation: true
  action_summary: Rename in place after the review confirms the case-folded target is the same canonical object and no colliding alias exists.
  followup_refs:
    - review.rename.case_only.readme
silent_best_effort_forbidden: true
parity_requirements:
  vocabulary_version: 1
  surfaces:
    - file_tree
    - breadcrumb
    - refactor_rename
    - cli
    - support_export
  required_shared_fields:
    - plan_id
    - operation_intent
    - current_presentation_path
    - proposed_presentation_path
    - logical_workspace_identity
    - current_canonical_object
    - proposed_canonical_effect
    - capability_context
    - review_gate
    - rollback_posture
    - next_safe_action
    - silent_best_effort_forbidden
    - parity_requirements
    - redaction_policy
  one_vocabulary_for_all_surfaces: true
  no_surface_local_fallbacks: true
support_export:
  packet_family: io.rename_move_plan
  redaction_policy: support_redact_paths_default
  diagnostic_summary: Case-only local rename is reviewable before mutation because the root folds case while preserving the visible spelling.
  saved_packet_fields:
    - plan_id
    - operation_intent
    - current_presentation_path
    - proposed_presentation_path
    - logical_workspace_identity
    - current_canonical_object
    - proposed_canonical_effect
    - capability_context
    - review_gate
    - rollback_posture
    - next_safe_action
    - silent_best_effort_forbidden
    - parity_requirements
    - redaction_policy
  parity_signature: "sha256:rename-move-local-case-only"
