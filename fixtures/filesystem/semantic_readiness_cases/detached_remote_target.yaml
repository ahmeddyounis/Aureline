# yaml-language-server: $schema=../../../schemas/filesystem/semantic_readiness_view.schema.json
__fixture__:
  name: detached_remote_target
  scenario: >
    A remote target is detached or unreachable. Remote-backed producers cannot
    serve claims; surfaces must preserve the unavailable state and route users
    to offline details or support exports without blocking local-core editing.
  doc_section: Semantic-readiness projection — detached remote target
  exercises:
    semantic_readiness.state: unavailable
    not_ready_reason: offline_unreachable
    why_not_ready.blocking_subsystem: remote_target
    why_not_ready.freshness_source: remote_reachability
record_kind: semantic_readiness_view_record
semantic_readiness_view_schema_version: 1
view_id: readiness.view.detached_remote_target.0001
readiness_record:
  record_kind: semantic_readiness_record
  filesystem_identity_schema_version: 1
  subject:
    kind: producer
    producer_id: remote_target_bridge
  semantic_readiness:
    state: unavailable
    not_ready_reason: offline_unreachable
    safe_next_action: open_offline_details
    producer_id: remote_target_bridge
    producer_version: remote-bridge-0.0.0
    observed_at: "mono:readiness:detached_remote_target:0001"
    explainer: Remote target unreachable; remote-backed claims unavailable until reconnect.
    readiness_chip:
      state: unavailable
      detail_target: aureline.remote.showAttachDetails
    not_ready_explainer:
      title: Remote target disconnected
      body: >
        The producer depends on a remote target that is currently unreachable.
        Local-core editing continues; remote-backed claims remain unavailable until
        the target reconnects.
      what_still_works:
        - Opening, editing, and saving local files
        - Viewing cached results with unavailable labels
    safe_next_actions:
      - action: open_offline_details
        command_id: aureline.remote.openOfflineDetails
        consequence: Opens the offline/reconnect detail surface for the target.
      - action: open_support_bundle
        command_id: aureline.support.openBundlePreview
        consequence: Opens a preview of the readiness packet that would be exported to support.
    support_export:
      packet_family: semantic_readiness
      redaction_policy: default_filesystem_redaction_v1
why_not_ready:
  blocking_subsystem: remote_target
  freshness_source: remote_reachability
  confidence_class: unknown
  not_ready_explainer_copy_id: filesystem.readiness.explainer.detached_remote_target
  safe_next_actions:
    - action: open_offline_details
      command_id: aureline.remote.openOfflineDetails
      consequence: Opens the offline/reconnect detail surface for the target.
    - action: open_support_bundle
      command_id: aureline.support.openBundlePreview
      consequence: Opens a preview of the readiness packet that would be exported to support.
  escalation_target:
    kind: command_id
    ref: aureline.support.openBundlePreview
    label: Export readiness evidence
recorded_at: "mono:readiness:detached_remote_target:view:0001"

