record_kind: docs_symbol_linked_refs_alpha_manifest
schema_version: 1
fixture_suite_id: fixture-suite:docs-symbol-linked-refs-alpha
fixtures:
  - fixture_ref: fixtures/docs/symbol_linked_refs_alpha/project_symbol_and_stale_example.json
    exercised_states:
      - docs_search_planned_result_id
      - exact_anchor_disclosed
      - project_docs_precedence
      - missing_anchor_downgrade
      - citation_drawer_hook
      - stale_example_signal
      - docs_suggestion_publish_boundary
required_checks:
  - Docs search uses the shared search planner and result ID shape.
  - Symbol-linked rows carry exact anchor, doc kind, source class, version, locality, freshness, and citation state.
  - Missing exact anchors downgrade visibly instead of silently pretending the fallback guide is exact.
  - Stale examples preserve trigger provenance, validation freshness, open-failing-source action, and publish-boundary state.
  - Docs suggestions disclose target branch/channel, local authoring posture, evidence state, validation freshness, and review-diff or evidence actions.
