# M5 docs handoff-banner / shared-consumer fixtures

Narrowed variants of the canonical M5 docs handoff-banner / shared-consumer packet. Both
keep every consumer visible; only one consumer's qualification is narrowed.

- `onboarding_tour_beta_narrowed.json` — the onboarding tour is held at Beta because a slice
  of onboarding surfaces do not yet render the source/version context badge on every
  profile.
- `ai_evidence_preview_narrowed.json` — the AI-evidence follow link is narrowed to Preview
  pending privacy-consequence parity proof across every AI-evidence export path.

Minted only by the headless emitter
`aureline_docs_handoff_banner_shared_consumers`
(`fixture-onboarding-tour-beta-narrowed` / `fixture-ai-evidence-preview-narrowed`). Do not
edit by hand.
