record_kind: docs_help_alpha_fixture_manifest
schema_version: 1
fixture_suite_id: fixture-suite:docs-help-alpha
catalog_fixture: fixtures/docs/help_search_alpha/catalog.yaml
support_export_fixture: fixtures/docs/docs_help_alpha/support_export_reconstruction.yaml
required_checks:
  - fixture catalog parses as the Rust help alpha catalog payload.
  - Command-backed rows use the canonical command registry command id and help anchor.
  - Result cards expose source, version, freshness, support class, client scope, and citation state.
  - Offline rows retain locale fallback and local-only continuity limits.
search_queries:
  - query_text: open folder
    expected_result_ids:
      - fixture:help-search:canonical-open-folder
    expected_command_id: cmd:workspace.open_folder
    expected_help_anchor_id: docs:anchor:workspace:open_folder_overview
badge_axes:
  source_class:
    - project_docs
    - support_runbook
  freshness_class:
    - authoritative_live
    - stale
  support_class:
    - supported
    - limited
  client_scope:
    - desktop_product
    - cli
  contract_state:
    - ready
    - local_only
  locale_availability:
    - requested_locale_authoritative
    - requested_locale_missing_fallback_to_primary
