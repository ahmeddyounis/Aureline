fixture_id: fixture:learning-tour-alpha.local-only-contextual
scenario: Local-only package renders a command-backed guided step with citations and exact reopen preserved.
expected:
  package_ref: learning-pack:aureline.entry.local
  step_ref: step:aureline.entry.open-folder
  command_id: cmd:workspace.open_folder
  package_degradation_class: local_only_disclosed
  target_kind: command_id
  exact_reopen_required: true
  hidden_mutation_allowed: false
