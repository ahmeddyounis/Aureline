# OS theme flip ignored under manual override with explicit disclosure.
__fixture__:
  name: macos_theme_flip_manual_override_disclosed
  scenario: >
    The OS theme preference flips while Aureline is pinned to a manual theme
    override. The effective appearance does not change; Aureline discloses why.
  exercised_axes:
    desktop_profile_id: macos_15_plus_universal
    os_signal_class: os_theme_signal
    axis: mode_theme_class
    follow_system_posture: manual_override
  contract_sections:
    - "§4 Follow-system vs override transitions"

record_kind: os_appearance_transition_case_record
os_appearance_transition_case_schema_version: 1
case_id: os_appearance_transition_case:macos_15_plus_universal:theme_flip_manual_override:01
desktop_profile_id: macos_15_plus_universal

prior_posture:
  follow_system_posture: manual_override
  preview_state: not_previewing

os_signal_event:
  os_signal_class: os_theme_signal
  changed_axes:
    - mode_theme_class
  new_value_summary: prefers_light

expected_resolution:
  session_revision_event: null
  notes: >
    The OS signal is observed but does not mutate the effective appearance
    because the session is in manual override posture. The disclosure path
    explains the held override and offers re-entering follow-system posture.
  disclosure_expectations:
    - disclosure_class: local_override_held
      disclosure_scope: global_shell
      disclosure_summary: System appearance changed; Aureline is using a local override.
      action_offered: reenter_follow_system
