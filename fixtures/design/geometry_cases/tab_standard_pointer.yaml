# yaml-language-server: $schema=../../../schemas/design/geometry_token.schema.json
#
# Tabs overflow/collapse before hit targets shrink or labels become meaningless.
__fixture__:
  name: tab_standard_pointer
  scenario: >
    Standard-density tabs use the shared tab-height token. Close buttons and
    pinned affordances preserve the minimum hit target; overflow occurs before
    labels become meaningless.
  exercised_axes:
    component_family: tab
    density_class: standard
    input_mode: pointer
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§3.1 Minimum interactive hit target (floor)"
    - "§5 Collapse-before-truncate and safe overflow (cramped shells)"

record_kind: geometry_case_record
geometry_token_schema_version: 1
case_id: geometry_case:tab:standard:pointer:01
component_family_class: tab
density_class: standard
input_mode_class: pointer
effective_zoom_percent: 100
effective_text_scale_percent: 100
token_expectations:
  - token_ref: size.hit.min
    expected_px: 28
  - token_ref: size.tab
    expected_px: 28
  - token_ref: stroke.focus.ring
    expected_px: 2
overflow_guardrails:
  - collapse_secondary_before_truncate_primary
  - move_optional_actions_to_overflow_before_shrinking_hit_targets
minted_at: "2026-05-05T00:00:00Z"

