# yaml-language-server: $schema=../../../schemas/design/geometry_token.schema.json
#
# Tables keep row height and hit targets stable for keyboard traversal.
__fixture__:
  name: table_standard_keyboard
  scenario: >
    Standard-density tables use the shared row-height token; keyboard traversal,
    selection, and inline actions remain operable under density and zoom changes
    because optional columns collapse before identity/provenance columns do.
  exercised_axes:
    component_family: table
    density_class: standard
    input_mode: keyboard
    effective_zoom_percent: 125
    effective_text_scale_percent: 100
  contract_sections:
    - "§3 Density mapping (compact/standard/comfortable)"
    - "§4 Zoom and text-scale behavior"
    - "§5 Collapse-before-truncate and safe overflow (cramped shells)"

record_kind: geometry_case_record
geometry_token_schema_version: 1
case_id: geometry_case:table:standard:keyboard:01
component_family_class: table
density_class: standard
input_mode_class: keyboard
effective_zoom_percent: 125
effective_text_scale_percent: 100
token_expectations:
  - token_ref: size.hit.min
    expected_px: 28
  - token_ref: size.row.standard
    expected_px: 28
  - token_ref: stroke.focus.ring
    expected_px: 2
overflow_guardrails:
  - collapse_secondary_before_truncate_primary
  - move_optional_actions_to_overflow_before_shrinking_hit_targets
  - preserve_focus_ring_clearance_under_zoom
minted_at: "2026-05-05T00:00:00Z"

