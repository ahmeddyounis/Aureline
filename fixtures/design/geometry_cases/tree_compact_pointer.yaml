# yaml-language-server: $schema=../../../schemas/design/geometry_token.schema.json
#
# Tree rows preserve stable disclosure affordances and indentation rhythm.
__fixture__:
  name: tree_compact_pointer
  scenario: >
    Compact-density tree rows use the shared row-height token and keep a stable
    disclosure affordance: a 12 px glyph inside a 28 px hit target. Indentation
    uses the shared 16 px step per level.
  exercised_axes:
    component_family: tree
    density_class: compact
    input_mode: pointer
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§3 Density mapping (compact/standard/comfortable)"
    - "§3.3 Tree indentation and disclosure affordances"
    - "§5 Collapse-before-truncate and safe overflow (cramped shells)"

record_kind: geometry_case_record
geometry_token_schema_version: 1
case_id: geometry_case:tree:compact:pointer:01
component_family_class: tree
density_class: compact
input_mode_class: pointer
effective_zoom_percent: 100
effective_text_scale_percent: 100
token_expectations:
  - token_ref: size.hit.min
    expected_px: 28
    notes: Disclosure hit target must not drop below this floor.
  - token_ref: size.row.compact
    expected_px: 24
  - token_ref: size.indent.step
    expected_px: 16
  - token_ref: size.disclosure.glyph
    expected_px: 12
overflow_guardrails:
  - preserve_disclosure_hit_target_under_cramped_width
  - collapse_secondary_before_truncate_primary
  - move_optional_actions_to_overflow_before_shrinking_hit_targets
minted_at: "2026-05-05T00:00:00Z"

