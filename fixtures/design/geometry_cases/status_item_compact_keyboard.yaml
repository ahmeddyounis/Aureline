# yaml-language-server: $schema=../../../schemas/design/geometry_token.schema.json
#
# Status items are dense but remain inspectable and keyboard reachable.
__fixture__:
  name: status_item_compact_keyboard
  scenario: >
    Compact-density status items preserve the minimum hit target and use the
    shared subtle opacity token for low-emphasis metadata. Secondary detail
    yields space first when the status bar becomes crowded.
  exercised_axes:
    component_family: status_item
    density_class: compact
    input_mode: keyboard
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§3 Density mapping (compact/standard/comfortable)"
    - "§3.1 Minimum interactive hit target (floor)"
    - "§5 Collapse-before-truncate and safe overflow (cramped shells)"

record_kind: geometry_case_record
geometry_token_schema_version: 1
case_id: geometry_case:status_item:compact:keyboard:01
component_family_class: status_item
density_class: compact
input_mode_class: keyboard
effective_zoom_percent: 100
effective_text_scale_percent: 100
token_expectations:
  - token_ref: size.hit.min
    expected_px: 28
  - token_ref: size.statusbar
    expected_px: 24
  - token_ref: opacity.subtle
    expected_alpha: 0.72
overflow_guardrails:
  - collapse_secondary_before_truncate_primary
  - preserve_focus_ring_clearance_under_zoom
minted_at: "2026-05-05T00:00:00Z"

