# yaml-language-server: $schema=../../../schemas/design/geometry_token.schema.json
#
# Banners prioritize readable primary statements over dense inline actions.
__fixture__:
  name: banner_standard_pointer
  scenario: >
    Standard-density banners use shared spacing tokens and move optional actions
    into overflow before truncating the primary statement or shrinking hit
    targets.
  exercised_axes:
    component_family: banner
    density_class: standard
    input_mode: pointer
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§2 Geometry token ledger (values of record)"
    - "§5 Collapse-before-truncate and safe overflow (cramped shells)"

record_kind: geometry_case_record
geometry_token_schema_version: 1
case_id: geometry_case:banner:standard:pointer:01
component_family_class: banner
density_class: standard
input_mode_class: pointer
effective_zoom_percent: 100
effective_text_scale_percent: 100
token_expectations:
  - token_ref: size.hit.min
    expected_px: 28
  - token_ref: space.3
    expected_px: 12
  - token_ref: space.4
    expected_px: 16
overflow_guardrails:
  - collapse_secondary_before_truncate_primary
  - move_optional_actions_to_overflow_before_shrinking_hit_targets
minted_at: "2026-05-05T00:00:00Z"

