# yaml-language-server: $schema=../../../schemas/design/appearance_session.schema.json
#
# OS contrast signal flipped to high-contrast. The resolving live
# policy applied the change live but minted a revertable checkpoint so
# the user can roll back if the OS signal misfires.
__fixture__:
  name: os_contrast_signal_revertable_checkpoint_event
  scenario: >
    The OS reported a high-contrast signal change. The session moved
    from `contrast_standard` to `contrast_high` and minted a revertable
    appearance checkpoint. No user confirm was required because the
    contrast axis is `live_apply_with_revertable_checkpoint` in the
    cited live policy.
  exercised_axes:
    cause_class: os_signal_change
    applied_under_live_class: live_apply_with_revertable_checkpoint
    changed_axes:
      - contrast_mode
  contract_sections:
    - "§3 Live follow-system policy"
    - "§5 Versioning and revision events"

record_kind: appearance_session_revision_event_record
appearance_session_schema_version: 1
event_id: appearance_session_event:profile.default:os_contrast:01
appearance_session_ref: appearance_session:profile.default:steady:01
prior_session_revision: 12
resulting_session_revision: 13
cause_class: os_signal_change
os_signal_ref: os_signal:appearance:contrast_high:01
confirm_action_ref: null
checkpoint_ref: appearance_checkpoint:profile.default:os_contrast:01
rollback_ref: appearance_rollback:profile.default:os_contrast:01
changed_axes:
  - contrast_mode
applied_under_live_class: live_apply_with_revertable_checkpoint
notes: >
  Live contrast escalation. Rollback ref restores the prior contrast
  posture without restart.
policy_context:
  policy_epoch: pe:2026-04-29:01
  trust_state: trusted
redaction_class: metadata_safe_default
recorded_at: "2026-04-29T09:00:30Z"
