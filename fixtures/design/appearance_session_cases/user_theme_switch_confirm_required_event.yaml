# yaml-language-server: $schema=../../../schemas/design/appearance_session.schema.json
#
# User-driven theme-package switch held under `confirm_review_required`
# until the user confirmed the switch in a review sheet. The event
# records the confirm action and the resulting revision.
__fixture__:
  name: user_theme_switch_confirm_required_event
  scenario: >
    The user selected a different theme package from the settings UI.
    The cited live policy treats `mode_theme_class` switching as
    confirm-required when not paired with an `os_theme_signal`, so the
    settings UI presented a confirm sheet. After confirm, the session
    moved to the new theme with a checkpoint and rollback ref recorded
    on the event.
  exercised_axes:
    cause_class: user_explicit_setting
    applied_under_live_class: confirm_review_required
    changed_axes:
      - mode_theme_class
      - active_theme_package_ref
  contract_sections:
    - "§3 Live follow-system policy"
    - "§5 Versioning and revision events"

record_kind: appearance_session_revision_event_record
appearance_session_schema_version: 1
event_id: appearance_session_event:profile.default:user_theme_switch:01
appearance_session_ref: appearance_session:profile.default:steady:01
prior_session_revision: 13
resulting_session_revision: 14
cause_class: user_explicit_setting
os_signal_ref: null
confirm_action_ref: action:appearance.confirm_theme_switch
checkpoint_ref: appearance_checkpoint:profile.default:user_theme_switch:01
rollback_ref: appearance_rollback:profile.default:user_theme_switch:01
changed_axes:
  - mode_theme_class
applied_under_live_class: confirm_review_required
notes: >
  User confirmed the theme switch. Rollback ref restores the prior
  active_theme_package_ref and mode_theme_class atomically.
policy_context:
  policy_epoch: pe:2026-04-29:01
  trust_state: trusted
redaction_class: metadata_safe_default
recorded_at: "2026-04-29T09:01:30Z"
