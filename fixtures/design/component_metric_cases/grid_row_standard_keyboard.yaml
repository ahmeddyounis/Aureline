# yaml-language-server: $schema=../../../schemas/design/component_metrics.schema.json
#
# Grid/table rows freeze a shared row-height metric so density changes remain
# mechanical across compare and inspect surfaces.
__fixture__:
  name: grid_row_standard_keyboard
  scenario: >
    Standard-density grid rows use the shared row-height metric and preserve the
    minimum hit target for row actions under keyboard traversal.
  exercised_axes:
    component_surface_class: grid_row
    density_class: standard
    input_mode: keyboard
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§2 One metric ledger (values of record)"

record_kind: component_metric_case_record
component_metrics_schema_version: 1
case_id: component_metric_case:grid_row:standard:keyboard:01
component_surface_class: grid_row
density_class: standard
input_mode_class: keyboard
effective_zoom_percent: 100
effective_text_scale_percent: 100
metric_expectations:
  - metric_id: metric.grid_row.height.standard
    expected_px: 28
  - metric_id: metric.row.inset_x.standard
    expected_px: 12
  - metric_id: metric.hit_target.min
    expected_px: 28
screenshot_baseline_refs:
  - design-screenshot:grid_row.standard.focus_visible.default:01
accessibility_review_refs:
  - a11y-hit-target:grid_row.inline_action.standard.keyboard:01
component_packet_refs:
  - component-packet:grid_row.compare:seed:01
minted_at: "2026-05-05T00:00:00Z"

