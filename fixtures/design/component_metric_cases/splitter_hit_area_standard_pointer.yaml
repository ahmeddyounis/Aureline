# yaml-language-server: $schema=../../../schemas/design/component_metrics.schema.json
#
# Splitter hit areas stay operable under density and zoom. The visible line may
# be thin; the hit area is governed by the shared floor.
__fixture__:
  name: splitter_hit_area_standard_pointer
  scenario: >
    Splitter hit area preserves the minimum and preferred hit area values and
    remains operable when the shell is dense or compact.
  exercised_axes:
    component_surface_class: splitter
    density_class: standard
    input_mode: pointer
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§2 One metric ledger (values of record)"

record_kind: component_metric_case_record
component_metrics_schema_version: 1
case_id: component_metric_case:splitter:standard:pointer:01
component_surface_class: splitter
density_class: standard
input_mode_class: pointer
effective_zoom_percent: 100
effective_text_scale_percent: 100
metric_expectations:
  - metric_id: metric.splitter.hit_area
    expected_minimum_px: 4
    expected_recommended_default_min_px: 6
    expected_recommended_default_max_px: 8
screenshot_baseline_refs:
  - design-screenshot:splitter.hit_area.hover_focus.standard:01
accessibility_review_refs:
  - a11y-hit-target:splitter.drag.standard.pointer:01
component_packet_refs:
  - component-packet:splitter.shell:seed:01
minted_at: "2026-05-05T00:00:00Z"

