# yaml-language-server: $schema=../../../schemas/design/component_metrics.schema.json
#
# List rows bind row height and default insets under compact density.
__fixture__:
  name: list_row_compact_keyboard
  scenario: >
    Compact-density list rows use the shared row-height metric and preserve the
    minimum hit target for inline row actions under keyboard focus.
  exercised_axes:
    component_surface_class: list_row
    density_class: compact
    input_mode: keyboard
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§2 One metric ledger (values of record)"

record_kind: component_metric_case_record
component_metrics_schema_version: 1
case_id: component_metric_case:list_row:compact:keyboard:01
component_surface_class: list_row
density_class: compact
input_mode_class: keyboard
effective_zoom_percent: 100
effective_text_scale_percent: 100
metric_expectations:
  - metric_id: metric.list_row.height.compact
    expected_px: 24
  - metric_id: metric.row.inset_x.compact
    expected_px: 8
  - metric_id: metric.hit_target.min
    expected_px: 28
screenshot_baseline_refs:
  - design-screenshot:list_row.compact.focus_visible.default:01
accessibility_review_refs:
  - a11y-hit-target:list_row.inline_action.compact.keyboard:01
component_packet_refs:
  - component-packet:list_row.identity:seed:01
minted_at: "2026-05-05T00:00:00Z"

