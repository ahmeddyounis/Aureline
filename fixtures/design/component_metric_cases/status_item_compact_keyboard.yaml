# yaml-language-server: $schema=../../../schemas/design/component_metrics.schema.json
#
# Status items are dense but must remain operable, focus-visible, and searchable
# under compact shells.
__fixture__:
  name: status_item_compact_keyboard
  scenario: >
    Compact-density status items stay inside the status bar height budget while
    preserving the minimum hit target and stable padding rhythm. Overflow and
    extension budgets are covered by the status bar contract; this fixture
    freezes geometry only.
  exercised_axes:
    component_surface_class: status_item
    density_class: compact
    input_mode: keyboard
    effective_zoom_percent: 100
    effective_text_scale_percent: 100
  contract_sections:
    - "§2 One metric ledger (values of record)"

record_kind: component_metric_case_record
component_metrics_schema_version: 1
case_id: component_metric_case:status_item:compact:keyboard:01
component_surface_class: status_item
density_class: compact
input_mode_class: keyboard
effective_zoom_percent: 100
effective_text_scale_percent: 100
metric_expectations:
  - metric_id: metric.statusbar.height
    expected_px: 24
  - metric_id: metric.status_item.inset_x.compact
    expected_px: 4
  - metric_id: metric.hit_target.min
    expected_px: 28
screenshot_baseline_refs:
  - design-screenshot:status_item.compact.focus_visible.default:01
accessibility_review_refs:
  - a11y-hit-target:status_item.compact.keyboard:01
component_packet_refs:
  - component-packet:status_bar.item:seed:01
minted_at: "2026-05-05T00:00:00Z"

