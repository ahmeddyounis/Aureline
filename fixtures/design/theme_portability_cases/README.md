# Theme portability fixtures

This directory contains worked `theme_package_portability_record` fixtures
validated against:

- `schemas/design/theme_portability_record.schema.json`

The cases exercise:

- built-in theme portability (no import lineage),
- imported-translated portability (provenance + mapping-summary + slot notes),
- metadata-only support export projection with an explicit downgrade event.

