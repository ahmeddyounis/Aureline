{
  "$schema": "../../../schemas/efficiency/publication-ingestion.schema.json",
  "__fixture__": {
    "fixture_id": "fixture.efficiency_publication_ingestion.schema_missing_field",
    "expect_schema_valid": false,
    "expect_admitted": false,
    "rejected_by": "schema_required_field",
    "scenario": "A binding missing a required field fails the schema before any admission rule runs."
  },
  "record_kind": "efficiency_publication_ingestion_binding",
  "schema_version": 1,
  "binding_id": "fixture.ingest.docs.schema_missing_field",
  "surface": "docs",
  "surface_locator": "docs/efficiency/publication-ingestion.md#notebooks",
  "consumes_entry_id": "eff.notebooks.thermal",
  "renders_verbatim_from_entry": true,
  "disclosed_fields": ["entry_id", "posture", "effective_posture", "certification_state", "claim_support"],
  "notes": "Negative case: rendered_projection is omitted, so the record fails its schema."
}
