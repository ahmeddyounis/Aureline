# Rejected-pattern examples (fixtures)

These fixtures demonstrate how to anchor a rejected proposal to:

1. a stable rejected-pattern id (`rp.*`),
2. the governing ADR/contract refs, and
3. a concrete revisit trigger + required evidence packet to reopen.

The canonical ledger is `artifacts/architecture/rejected_pattern_rows.yaml`.

