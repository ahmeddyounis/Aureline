# Dependency Advisory And Lockfile Review Fixtures

These fixtures protect dependency graph, advisory truth, lockfile mutation
preview, and release-debt packet parity for package-security review. They use
refs, controlled enum values, count buckets, and workspace-relative paths only;
they do not carry raw registry URLs, raw manifests, raw lockfiles, package
bodies, scanner payloads, tokens, or certificate material.
