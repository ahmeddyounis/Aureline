// Small clean text fixture for the large-file path prototype.
//
// This file is meant to land in NORMAL mode under default policy
// (no large-file trigger fires): it is short, has no NUL bytes,
// no long single line, no pack-suffix, and no operator override.
//
// The harness reads the start of the file through the paged
// reader and exercises the capability table to confirm every
// capability is allowed in normal mode.
let answer = 42;
let greeting = "hello, world";
let mode = "normal";
