// Slightly-larger-than-tight-threshold text fixture.
//
// The matching scenario configures `large_file_size_threshold` to
// 256 bytes so this fixture lands above the threshold and the
// classifier fires `trigger = size_threshold` first, even though
// the sniff is otherwise clean.
//
// The text below pads the file past the configured threshold
// without making it big in absolute terms — the prototype's whole
// point is that the threshold is the trigger; the production
// build replaces the 256-byte threshold with the workspace
// policy's value (default 100 MiB).
let answer = 42;
let line_one = "the buffer enters limited mode at this size";
let line_two = "the paged reader walks pages without loading the whole file";
let line_three = "save with whole-file participants is denied";
let line_four = "save with range-only participants remains";
let line_five = "viewport multi-cursor is allowed; whole-file multi-cursor is denied";
let line_six = "indexing and background analysis are denied for this buffer";
