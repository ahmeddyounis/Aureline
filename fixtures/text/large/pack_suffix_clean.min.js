// Small clean JS file whose name matches a "large-file pack" suffix.
//
// Sniff is clean (no NULs, short lines), the file is tiny, and no
// operator override applies — but the path suffix `.min.js` is
// listed in the policy's pack-suffix table, so classification
// trips by the policy-pack rule rather than by sniff content.
function id(x) { return x; }
function noop() {}
