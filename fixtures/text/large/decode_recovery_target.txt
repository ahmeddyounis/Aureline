// Clean text that the decode-recovery surface routed to large-file mode.
//
// Sniff is clean and the file is small, so under default policy
// no trigger fires. The matching scenario sets
// `decode_recovery_chose_large_file = true` to model the user
// choosing "open in large-file mode" from the decode-recovery
// surface after a previous decode failed.
let recovered_with = "user_chose_large_file_mode";
let trigger = "decode_posture";
