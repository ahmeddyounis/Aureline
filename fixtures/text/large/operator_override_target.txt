// Clean small text that the operator chose to open in large-file mode.
//
// No size, classification, decode-posture, or pack-suffix trigger
// fires for this file under the default policy. The harness flips
// the operator-override flag to exercise the last switch
// condition and confirm the prototype routes the file to limited
// mode with `trigger = operator_override` rather than promoting
// any earlier trigger.
let mode = "operator_override";
let allowed_in_normal_mode = true;
let routed_to_limited_mode_by = "user_choice";
