# yaml-language-server: $schema=../../../schemas/security/secret_class_rows.schema.json
__fixture__:
  name: enterprise_vault_unlocked
  scenario: >
    An enterprise vault adapter is available and unlocked. Secret-bearing
    material is still consumed through handles and declared projection modes; an
    unlocked vault does not imply raw reveal or exportability.
  adr_section: "Trust-store classes (frozen)"
  exercised_classes_summary:
    secret_classes:
      - ai_provider_token
      - code_host_token
      - package_registry_token
      - database_credential
      - provider_session
    trust_store_classes:
      - enterprise_vault_adapter
    unlock_states:
      - unlocked
    projection_modes: []
    denial_reasons: []
    audit_event_ids: []

record_kind: trust_store_posture_record
secret_schema_version: 1
active_trust_store_class: enterprise_vault_adapter
unlock_state: unlocked
session_memory_cache_in_use: false
degraded_since: "mono-20260505-000010"
affected_secret_classes:
  - ai_provider_token
  - code_host_token
  - package_registry_token
  - database_credential
  - provider_session
notes:
  - >
    Storage authority affects where material lives, not whether raw material may
    be revealed or exported. Export rules remain governed by secret class +
    surface redaction class.

