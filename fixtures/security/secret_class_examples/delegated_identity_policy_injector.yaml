# yaml-language-server: $schema=../../../schemas/security/secret_class_rows.schema.json
__fixture__:
  name: delegated_identity_policy_injector
  scenario: >
    A managed policy injector materialises delegated credentials at call time.
    The broker still reports secret class and storage authority, but the source
    is policy-owned and ephemeral: exports remain metadata-only and raw reveal
    remains denied unless an explicit policy exception exists.
  adr_section: "Trust-store classes (frozen) - managed_policy_injector"
  exercised_classes_summary:
    secret_classes:
      - provider_session
      - ephemeral_operation_token
    trust_store_classes:
      - managed_policy_injector
    unlock_states:
      - unlocked
    projection_modes: []
    denial_reasons: []
    audit_event_ids: []

record_kind: trust_store_posture_record
secret_schema_version: 1
active_trust_store_class: managed_policy_injector
unlock_state: unlocked
session_memory_cache_in_use: false
degraded_since: "mono-20260505-000020"
affected_secret_classes:
  - provider_session
  - ephemeral_operation_token
notes:
  - >
    Delegated/policy-injected authority is still class-labelled and audited, but
    it does not become a portable secret source. Exports carry class + alias +
    posture metadata only.

