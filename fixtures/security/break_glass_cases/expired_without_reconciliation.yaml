# yaml-language-server: $schema=../../../schemas/security/break_glass_event.schema.json
__fixture__:
  name: expired_without_reconciliation
  scenario: >
    Worked break_glass_event_record for an update-pause invocation
    where the 24 h close-out envelope elapsed without a retrospective
    cosign and without a superseding signed action. The event closes
    in expired_without_reconciliation; the surface raises a correction
    signal, the reconciliation_plan_note names the follow-up path, and
    a post-incident review record is required before the same scope
    resumes.
  contract_sections:
    - "Break-glass admissibility"
    - "Expired without reconciliation"
    - "Cross-surface referencing rule"

break_glass_event_schema_version: 1
record_kind: break_glass_event_record

break_glass_event_id: break_glass_event.AURELINE-BG-2026-0003
signing_quorum_action_ref: emergency_policy_bundle_publish
break_glass_profile_ref: audited_single_responder_containment

title: >
  Emergency update-pause invocation expired without retrospective cosign
summary: >
  The stable-channel emergency update pause was invoked under
  audited_single_responder_containment. The 24 h close-out envelope
  elapsed before a cosigning release-council member was available and
  before a superseding signed action landed; the event closed in
  expired_without_reconciliation and surfaces the correction signal.

invoking_actor:
  actor_class: security_operator
  actor_role_class: backup_maintainer
  actor_account_ref: operator_account.security_trust_review.backup
  actor_note: >
    Backup security operator invoked the update pause while the
    primary was unreachable; no release-council cosigner was available
    inside the envelope.

reason_class: urgent_channel_freeze_needed
urgency_class: immediate

scope:
  affected_subjects:
    - subject_kind: release_channel
      subject_id: release-channel:stable
      note: Stable update fetch paused pending review.
    - subject_kind: policy_bundle
      subject_id: policy_bundle.emergency.update_pause.stable
      note: Emergency update-pause policy bundle in effect until superseded.
  affected_install_profile_card_refs:
    - card.linux.x86_64.per_user_installed.stable
    - card.linux.x86_64.managed_deployed.stable
  affected_exact_build_identity_refs: []
  affected_channel_classes:
    - stable
  affected_deployment_profile_scope:
    - individual_local
    - self_hosted
    - enterprise_online
  scope_note: >
    Update pause narrowed stable update fetch across individual, self
    hosted, and enterprise-online profiles; air-gapped profiles were
    out of scope for this invocation.

related_emergency_action_refs:
  - emergency_action.AURELINE-ACT-2026-0003
related_revocation_refs: []
related_advisory_refs: []
related_private_triage_packet_ref: private_triage_workspace_packet.update_channel_anomaly_triage

invoked_at: "2026-04-19T04:10:00Z"
must_close_by: "2026-04-20T04:10:00Z"
closed_at: "2026-04-20T04:10:00Z"

lifecycle_state: expired_without_reconciliation

retrospective_cosign: null

temporary_mitigation_note: >
  Stable update fetch and mirror promote paths were blocked via the
  emergency policy bundle while the invocation was open; the produced
  emergency-action record continues under its own expiry rules.
reconciliation_plan_note: >
  Correction plan: open a release-council review by 2026-04-20T16:00Z
  to either mint a superseding cosigned emergency-action record or
  withdraw the update pause. The produced emergency-action record is
  pinned to pending reconciliation until a post-incident review row
  closes the resume decision.

history_links:
  release_evidence_packet_refs:
    - release_evidence_packet.update_channel_anomaly_triage
  support_export_packet_refs:
    - support_bundle.update_channel_anomaly_triage
  admin_export_refs:
    - admin_export.update_pause.stable.2026-04-19
  incident_workspace_packet_refs:
    - incident_workspace_packet.update_channel_anomaly
  post_incident_review_refs:
    - post_incident_review.update_channel_anomaly.2026-04-21
  decision_history_refs:
    - signing_quorum_yaml#actions.emergency_policy_bundle_publish.invocation.break_glass_event.AURELINE-BG-2026-0003

redaction_class: support_export_only
minted_at: "2026-04-19T04:10:00Z"
