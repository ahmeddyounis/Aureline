# yaml-language-server: $schema=../../../schemas/security/local_continuity_card.schema.json
__fixture__:
  name: local_continuity_card_admin_override
  scenario: >
    Worked local_continuity_card_record explaining a forced disablement originating from an admin override,
    with an explicit next-safe recovery path to clear the override.
  contract_sections:
    - "Local continuity card record"
  exercised_axes:
    trigger_source_class: admin_override

record_kind: local_continuity_card_record
local_continuity_card_schema_version: 1
local_continuity_card_id: continuity_card.provider_route.managed_ai_gateway.admin_override.0001

target:
  target_class: provider_route
  target_ref: provider_route.managed_ai_gateway
  target_label: Managed AI gateway route

source:
  trigger_source_class: admin_override
  source_ref: admin_override.provider_route.disable.0001
  source_distribution: null
  note: >
    The route is disabled by an admin override. This override may narrow further than signed policy but cannot widen beyond signed ceilings.

local_continuity:
  posture_class: local_continuity_narrowed
  note: >
    Local editing remains available; provider routing through the managed gateway is disabled by admin override.
  retained_capabilities:
    - Local editing and save
    - Local-only inference routes where configured
  blocked_capabilities:
    - Managed AI gateway dispatch through provider_route.managed_ai_gateway

next_safe_paths:
  - path_class: clear_local_override
    path_ref: admin_policy_handoff_export.provider_route.disable.0001
    note: Clear the admin override or replace it with a signed policy or emergency bundle if the restriction must persist.
  - path_class: contact_admin_or_owner
    path_ref: null
    note: Contact the organization administrator for review and follow-up.

rendered_at: "2026-04-28T16:15:00Z"

