# yaml-language-server: $schema=../../../schemas/security/private_triage_workspace_packet.schema.json
__fixture__:
  name: offline_airgapped_expired_bundle_rekey
  scenario: >
    Worked, pre-implementation private-triage packet for an offline /
    air-gapped distribution finding. An internal reviewer discovers
    that the seeded offline signed bundle for the stable air-gapped
    target is signed with a key whose rotation was not mirrored onto
    existing bundles; once the current bundle expires, air-gapped
    operators would have no signed recovery path. The monitored
    contact path acknowledges immediately on
    vendor_self_initiated_intake, opens a vendor_only triage at
    operational_emergency severity, reserves an advisory id plus a
    CVE alias, invokes a pending-reconciliation break-glass trust-
    root rotation action to mint a recovery offline bundle, ships the
    recovery bundle to a subset of target sites (mitigation_shipped_
    partial), and holds the packet at public-on-advisory posture
    while the remaining sites mirror the rotated bundle. Raw signing
    material, raw offline bundle bytes, and raw site identifiers do
    not appear; redaction_posture stays at private_triage_only because
    break-glass reconciliation and the rotated signing-material audit
    are still circulating inside the vendor_only scope.
  distribution_lane: offline_air_gapped
  exercised_classes_summary:
    severity_class: security_severity.operational_emergency
    triage_status_class: triage_in_progress
    private_triage_workspace_scope: vendor_only
    intake_route_class: vendor_self_initiated_intake
    current_mitigation_status_class: mitigation_shipped_partial
    compensating_control_state_class: compensating_control_partial
    break_glass_state_class: break_glass_invoked_pending_reconciliation
    disclosure_posture_class: disclosure_posture_public_on_advisory
    redaction_posture_class: private_triage_only
    impacted_deployment_profile_scope:
      - air_gapped
    affected_publication_posture_classes:
      - offline_signed_bundle

private_triage_workspace_packet_schema_version: 1
record_kind: private_triage_workspace_packet_record

triage_packet_id: triage_packet.offline_airgapped_expired_bundle_rekey

title: >
  Offline air-gapped signing-material rekey on the stable offline bundle
summary: >
  Private-triage packet for the offline / air-gapped rekey finding:
  the shipped offline signed bundle is signed with a key whose rotation
  was not mirrored to existing bundles, leaving air-gapped sites with no
  signed recovery path once the current bundle expires. Reserved
  AURELINE-ADV-2026-0005 with a CVE alias; break-glass trust-root
  rotation invoked and pending release-council reconciliation; recovery
  bundle shipped to a subset of sites; held at public-on-advisory
  posture.

severity_class: security_severity.operational_emergency

triage_status: triage_in_progress

private_triage_workspace_scope: vendor_only

monitored_contact_ref: security_md_monitored_contact

intake_route: vendor_self_initiated_intake

intake_received_at: "2026-04-18T02:12:00Z"
intake_acknowledged_at: "2026-04-18T02:18:00Z"

advisory_alias_reservation:
  aureline_advisory_id: AURELINE-ADV-2026-0005
  cve_id: CVE-2026-10005
  ghsa_id: null
  additional_alias_refs: []

affected_install_linkage:
  affected_exact_build_identity_refs:
    - build-id:aureline:stable:0.7.4:platform_native_any:offline_bundle:release:70ee6318afa2
  affected_artifact_refs:
    - subject_kind: signing_material
      subject_id: signing_material.release_hardware_key_sign.aureline.stable
      note: Release hardware-key signing material the offline bundle resolves against.
    - subject_kind: install_profile_card
      subject_id: card.air_gap_bundle_target.platform_native_any.offline_bundle.stable
      note: Stable air-gap offline-bundle install-profile card affected by the rekey.
    - subject_kind: build_binary
      subject_id: build_binary.aureline.stable.offline_bundle.recovery_path
      note: Recovery-path binary family re-signed against the rotated key for the interim bundle.
  affected_install_profile_card_refs:
    - card.air_gap_bundle_target.platform_native_any.offline_bundle.stable
    - card.windows.x86_64.offline_bundle.stable
    - card.linux.x86_64.offline_bundle.stable
  impacted_deployment_profile_scope:
    - air_gapped
  affected_channel_classes:
    - stable
  affected_publication_posture_classes:
    - offline_signed_bundle
  mirror_freshness_class: offline_expired
  offline_expiration_at: "2026-05-01T00:00:00Z"
  local_continuity_note: >
    Online-vendor, customer-managed-mirror, and managed-cloud lanes are
    not affected; air-gapped sites keep editor continuity on the prior
    signed bundle until offline_expiration_at; sites that have mirrored
    the rotated recovery bundle regain a signed recovery path
    immediately; remaining sites MUST mirror the rotated bundle before
    offline_expiration_at to avoid a forced fail-closed state.

current_mitigation_status: mitigation_shipped_partial

compensating_control_state: compensating_control_partial

compensating_control_refs:
  - control_ref_kind: emergency_action_record
    control_ref_id: emergency_action_record.offline_airgapped_expired_bundle_rekey.trust_root_rotation
    note: >
      Trust-root rotation emergency action: mint a recovery offline
      bundle signed with the rotated key and mirror it to air-gapped
      sites before offline_expiration_at.
  - control_ref_kind: configuration_note
    control_ref_id: configuration_note.offline_airgapped_expired_bundle_rekey.site_rekey_playbook
    note: >
      Site-operator playbook for verifying the rotated pack key
      fingerprint against the published rotation notice before
      trusting the recovery bundle.

break_glass_state: break_glass_invoked_pending_reconciliation

break_glass_refs:
  - audit_row_ref: signing_quorum_audit.offline_airgapped_expired_bundle_rekey.trust_root_rotation
    action_ref_kind: trust_root_rotation_action
    action_ref_id: emergency_action_record.offline_airgapped_expired_bundle_rekey.trust_root_rotation
    note: >
      Break-glass trust-root rotation invoked under single-responder
      authorization; release-council co-sign scheduled within the
      emergency triage target; reconciliation will move the state to
      break_glass_invoked_reconciled.

disclosure_posture: disclosure_posture_public_on_advisory

redaction_posture: private_triage_only

linked_advisory_refs:
  - advisory_record.AURELINE-ADV-2026-0005

linked_incident_workspace_packet_ref: incident_workspace_packet.offline_airgapped_expired_bundle_rekey

linked_emergency_action_refs:
  - emergency_action_record.offline_airgapped_expired_bundle_rekey.trust_root_rotation

linked_revocation_refs: []

linked_postmortem_refs: []

policy_context:
  policy_epoch: policy_epoch.security_response_pre_implementation.2026.04
  trust_state: managed_admin
  execution_context_id: execution_context.security_trust_review.vendor_only

opened_at: "2026-04-18T02:18:30Z"
last_updated_at: "2026-04-23T21:15:00Z"
