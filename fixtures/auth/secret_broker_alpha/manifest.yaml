fixture_set: secret_broker_alpha
contract_ref: docs/security/secret_broker_alpha.md
owner_crate: crates/aureline-auth
record_kinds:
  - secret_broker_alpha_packet_record
coverage:
  - Credential-store handle rows keep raw material out of settings and support export.
  - Session-only rows stay visibly degraded and process-scoped.
  - Delegated rows name actor, scope, expiry, and revoke path without exporting delegated token bodies.
  - Locked, unavailable, and trust-store-changed continuity results name exact affected capability classes.
  - Support export includes metadata-only broker state and never exports raw secret values or raw handle ids.
guardrails:
  - plaintext_persistence_allowed must remain false.
  - silent_in_memory_promotion_allowed must remain false.
  - stale_ticket_reuse_allowed must remain false.
  - raw_secret_material_present must remain false.
minted_at: "2026-05-13T23:58:00Z"
