# yaml-language-server: $schema=../../../schemas/auth/credential_state.schema.json
__fixture__:
  name: browser_device_code_handoff
  scenario: >
    A connector auth flow is waiting on a system-browser or device-code
    handoff. The product shows provider/org, handoff expiry, local
    continuity, and handle-only destination. Raw device codes and
    callback payloads are not serialized in this row.
  contract_sections:
    - Secret-access prompt contract
    - Storage mode and origin vocabulary
    - Export, support, and automation rules
  exercised_axes:
    state_class: handle_only
    store_source_class: browser_device_code_handoff
    safe_actions:
      - start_browser_handoff
      - use_device_code_handoff

record_kind: credential_state_record
credential_state_schema_version: 1
credential_state_id: credential-state:connector:browser-device-handoff
state_class: handle_only
display_label: Browser handoff pending
subject:
  target_class: browser_device_handoff
  target_ref: browser-handoff:connector-auth
  target_label: Connector sign-in handoff
  secret_class: provider_session
  scope_ref: tenant:self-hosted:connector
  scope_label: Self-hosted connector tenant
  audience_label: Connector provider session
authority:
  storage_mode_class: handle_only
  store_source_class: browser_device_code_handoff
  credential_origin_class: browser_or_device_code_handoff
  authority_ref: credential-authority:connector-browser-handoff
  alias_ref: credential-alias:connector:pending
  handle_ref: null
  source_label: System-browser or device-code handoff
  delegated_credential_ref: null
  policy_owner_ref: policy-owner:connector-platform
  browser_handoff_ref: browser-handoff:connector-auth
  raw_secret_material_present: false
storage:
  trust_store_class: null
  unlock_state: null
  store_capability_ref: store-capability:browser-device-code-handoff
  store_verification_state: reference_only_unverified
  session_only_downgrade_visible: false
  supports_persistent_long_lived: false
  plaintext_fallback_allowed: false
  storage_note: Handoff will return a broker handle or alias; raw device codes and callback query strings are not stored here.
lifetime:
  lifetime_class: operation_scoped
  issued_at: "2026-04-29T09:30:00Z"
  expires_at: "2026-04-29T09:40:00Z"
  refresh_after: null
  revocation_path_label: Cancel connector handoff
  lifetime_note: The handoff is operation-scoped and expires if the browser or device-code flow does not complete.
projection_mode: token_exchange
capability_implications:
  can_read_secret: false
  can_write_secret: false
  can_rebind_handle: false
  can_import_credential: true
  can_unlock_store: false
  can_use_existing_handle: false
  can_project_raw_material: false
  local_work_continues: true
  retained_capabilities:
    - Continue editing local files.
    - Cancel the handoff without losing local workspace state.
    - Export handoff metadata for support.
  blocked_capabilities:
    - Use the connector provider session before handoff completion.
  capability_note: Browser/device-code flow acquires a handle; it is not an embedded password or raw-token prompt.
prompt_policy:
  prompt_required: true
  prompt_action_classes:
    - import_credential
  prompt_ref: secret-prompt:connector-browser-device-handoff
  prompt_reason: The connector needs a provider-session handle returned by browser or device-code handoff.
  handle_only_path_available: true
export_rules:
  handles_aliases_source_labels_may_travel: true
  raw_secret_material_may_travel: false
  private_keys_may_travel: false
  refresh_tokens_may_travel: false
  ambient_delegated_credentials_may_travel: false
  support_export_posture: metadata_only
  automation_export_posture: denied_until_prompt
  export_note: Export may include the handoff ref, alias, expiry, and provider class; raw device codes and callback payloads are excluded.
safe_actions:
  primary_action: start_browser_handoff
  fallback_actions:
    - use_device_code_handoff
    - continue_without_credential
    - export_support_metadata
  escalation_action: contact_admin
  safe_retry_allowed: true
  escalation_note: Retry the browser handoff or use the device-code fallback where provider and policy allow.
diagnostics:
  denial_reason: null
  failure_label: handoff_pending
  audit_event_refs:
    - auth-event:browser-handoff-started:connector
  last_checked_at: "2026-04-29T09:30:05Z"
  support_summary: Connector auth is waiting on browser/device-code handoff; local work remains available and raw handoff values are excluded.
policy_context:
  identity_mode: self_hosted_org
  policy_epoch: policy-epoch:connector:handoff
  trust_state: trusted
  execution_context_id: execution-context:connector-auth
surface_redaction_class: metadata_safe_default
secret_redaction_class_refs:
  - support_bundle
  - logs_local
  - profile_export
minted_at: "2026-04-29T09:30:10Z"
