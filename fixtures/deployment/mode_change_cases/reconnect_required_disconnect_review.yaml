# yaml-language-server: $schema=../../../schemas/deployment/mode_change_review.schema.json
$schema: "https://aureline.dev/schemas/deployment/mode_change_review.schema.json"
__fixture__:
  name: reconnect_required_disconnect_review
  scenario: >
    Disconnect review paired with the reconnect_required_after_grace
    mode-change record. The grace window closed; the auth identity
    and policy services require reconnect; mirrored docs pack and
    policy bundle artifacts have entered the extended freshness
    window. The reconnect path is manual_reconnect.
  contract_sections:
    - "§4 The disconnect review record"
    - "§4.6 paused_capabilities and stale_artifacts"
    - "§4.8 freshness_label"
    - "§5.2 Plane separation never collapses on disconnect"
record_kind: disconnect_review_record
disconnect_review_schema_version: 1
review_id: "review.disconnect.reconnect_required_after_grace"
emitted_at: "2026-04-29T00:30:00Z"
disconnect_event_class: reconnect_required
affected_service_classes:
  - sync_service
  - registry_service
  - relay_service
  - ai_broker_service
  - auth_identity_service
  - policy_service
  - docs_pack_service
  - catalog_service
affected_capability_classes: []
local_continuity_statement:
  available_local_safe_capability_classes:
    - local_editing
    - local_save
    - local_search
    - local_git
    - local_tasks
    - local_docs_inspect
    - local_export
    - local_diagnostics
  local_safe_label: "Two unsaved local buffers remain editable, saveable, and undoable. Local edit, save, search, Git, tasks, docs inspection, export, and diagnostics continue on device while reconnect is pending."
  no_silent_loss_guarantee: true
  unsaved_local_artifacts_safe: true
paused_capabilities:
  - "Managed AI broker calls."
  - "Managed settings sync."
  - "Browser-companion handoff."
  - "Managed catalog browse."
stale_artifacts:
  - artifact_class: docs_pack
    mirror_freshness_class: mirror_within_extended_window
    last_refresh_at: "2026-04-25T00:00:00Z"
    freshness_label: "Docs pack is within the extended freshness window."
  - artifact_class: policy_bundle
    mirror_freshness_class: mirror_within_extended_window
    last_refresh_at: "2026-04-25T00:00:00Z"
    freshness_label: "Policy bundle is within the extended freshness window."
reconnect_path_class: manual_reconnect
reconnect_label: "Sign in again to refresh the managed control plane and resume managed actions."
requires_user_action: true
freshness_label: "Docs pack and policy bundle are within the extended freshness window pending reconnect."
mode_change_review_ref: "review.mode_change.reconnect_required_after_grace"
linked_continuity_packet_ref: "continuity.managed_cloud.reconnect_required"
redaction_class: metadata_safe_default
export_safe: true
notes: >
  Reconnect requires user action. Local capabilities are unaffected;
  stale artifacts remain reviewable with explicit freshness labels.
