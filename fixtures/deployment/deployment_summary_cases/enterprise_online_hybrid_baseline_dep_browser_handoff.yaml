# yaml-language-server: $schema=../../../schemas/deployment/deployment_summary_card.schema.json
$schema: "https://aureline.dev/schemas/deployment/deployment_summary_card.schema.json"
__fixture__:
  name: enterprise_online_hybrid_baseline_dep_browser_handoff
  scenario: >
    Browser handoff dependency for the hybrid enterprise-online
    baseline. Posture is required; vendor_or_public_dependence is
    true; when unreachable, cross-device handoff narrows to cached
    last-known-good state with explicit freshness labelling.
  contract_sections:
    - "§4 The residual-dependency row record"
    - "§4.2 Posture / impact pairing rules"
record_kind: residual_dependency_row_record
residual_dependency_row_schema_version: 1
row_id: "row.dep.enterprise_online_hybrid_baseline.browser_handoff"
card_id_ref: "card.deployment.enterprise_online_hybrid_baseline"
dependency_class: browser_handoff
posture_class: required
vendor_or_public_dependence: true
dependent_feature_label: "Cross-device approval handoff, hosted-review browser open, and managed-cloud companion sign-in."
dependent_feature_refs:
  - "feature.handoff.approval"
  - "feature.handoff.hosted_review"
  - "feature.handoff.managed_companion"
unreachable_impact_class: narrows_to_cached_last_known_good
unreachable_impact_label: "Companion handoffs render the last cached state with explicit freshness labelling; new handoffs wait for the channel to recover."
continuity_fallback_class: resume_after_reconnect
ledger_row_ref: "ledger.dep.browser_handoff:enterprise_online"
freshness_label: "Last successful handoff observed within the current session."
redaction_class: metadata_safe_default
export_safe: true
notes: >
  Vendor-bound by ledger; surfaces cannot render this dependency as
  customer-operated.
