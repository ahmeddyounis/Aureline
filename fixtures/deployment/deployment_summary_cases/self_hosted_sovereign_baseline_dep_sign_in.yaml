# yaml-language-server: $schema=../../../schemas/deployment/deployment_summary_card.schema.json
$schema: "https://aureline.dev/schemas/deployment/deployment_summary_card.schema.json"
__fixture__:
  name: self_hosted_sovereign_baseline_dep_sign_in
  scenario: >
    Sign-in dependency row for the self-hosted sovereign baseline. The
    customer IdP session is required for managed-only flows; absence
    blocks pending reconnect while local-core capabilities continue.
    vendor_or_public_dependence is false because sign-in resolves
    against the customer's IdP, not a vendor IdP.
  contract_sections:
    - "§4 The residual-dependency row record"
    - "§4.2 Posture / impact pairing rules"
record_kind: residual_dependency_row_record
residual_dependency_row_schema_version: 1
row_id: "row.dep.self_hosted_sovereign_baseline.sign_in"
card_id_ref: "card.deployment.self_hosted_sovereign_baseline"
dependency_class: sign_in
posture_class: required
vendor_or_public_dependence: false
dependent_feature_label: "Customer-tenant sign-in for managed-only flows (publish, hosted review, policy refresh)."
dependent_feature_refs:
  - "feature.self_hosted.publish"
  - "feature.self_hosted.hosted_review"
  - "feature.self_hosted.policy_refresh"
unreachable_impact_class: blocked_pending_reconnect
unreachable_impact_label: "Managed-only publish, hosted review, and policy refresh wait for reconnect; local edit, save, search, Git, tasks, export, and diagnostics continue."
continuity_fallback_class: resume_after_reconnect
ledger_row_ref: "ledger.dep.sign_in:self_hosted"
redaction_class: metadata_safe_default
export_safe: true
notes: >
  Customer IdP. The dependency is not vendor-bound; the sign-in row in
  /artifacts/governance/residual_dependencies.yaml pairs self_hosted
  with resume_after_reconnect.
