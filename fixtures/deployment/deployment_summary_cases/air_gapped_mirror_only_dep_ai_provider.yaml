# yaml-language-server: $schema=../../../schemas/deployment/deployment_summary_card.schema.json
$schema: "https://aureline.dev/schemas/deployment/deployment_summary_card.schema.json"
__fixture__:
  name: air_gapped_mirror_only_dep_ai_provider
  scenario: >
    AI provider dependency for the air-gapped baseline. Posture is
    forbidden; vendor_or_public_dependence is false because the
    dependency is excluded by construction. Surfaces that attempt to
    surface AI-assisted workflows on this profile fail closed and cite
    this row.
  contract_sections:
    - "§4 The residual-dependency row record"
    - "§4.2 Posture / impact pairing rules"
record_kind: residual_dependency_row_record
residual_dependency_row_schema_version: 1
row_id: "row.dep.air_gapped_mirror_only.ai_provider"
card_id_ref: "card.deployment.air_gapped_mirror_only"
dependency_class: ai_provider
posture_class: forbidden
vendor_or_public_dependence: false
dependent_feature_label: "AI-assisted code completion, refactor preview, and chat panel."
dependent_feature_refs:
  - "feature.ai.completion"
  - "feature.ai.refactor_preview"
  - "feature.ai.chat_panel"
unreachable_impact_class: fail_closed_forbidden_in_profile
unreachable_impact_label: "AI-assisted features are forbidden on the air-gapped profile; surfaces that attempt to render them fail closed."
continuity_fallback_class: fail_closed_no_fallback
ledger_row_ref: "ledger.dep.ai_provider:air_gapped"
redaction_class: metadata_safe_default
export_safe: true
notes: >
  Excluded by construction. The air-gapped profile forbids hosted AI
  inference unless a local-only inference path is named separately.
