# yaml-language-server: $schema=../../../schemas/deployment/deployment_summary_card.schema.json
$schema: "https://aureline.dev/schemas/deployment/deployment_summary_card.schema.json"
__fixture__:
  name: air_gapped_mirror_only_dep_docs_pack
  scenario: >
    Docs pack dependency for the air-gapped baseline. Posture is
    mirrored; vendor_or_public_dependence is true (the docs pack is a
    vendor-published artifact resolved through a customer-operated
    mirror). When unreachable, docs inspection narrows to mirror-
    backed read-only with explicit freshness labelling.
  contract_sections:
    - "§4 The residual-dependency row record"
    - "§4.2 Posture / impact pairing rules"
record_kind: residual_dependency_row_record
residual_dependency_row_schema_version: 1
row_id: "row.dep.air_gapped_mirror_only.docs_pack"
card_id_ref: "card.deployment.air_gapped_mirror_only"
dependency_class: docs_pack
posture_class: mirrored
vendor_or_public_dependence: true
dependent_feature_label: "Offline docs inspection, help index search, and signed help-pack open."
dependent_feature_refs:
  - "feature.docs.inspection"
  - "feature.docs.help_index"
  - "feature.docs.signed_help_pack_open"
unreachable_impact_class: narrows_to_mirror_backed_read_only
unreachable_impact_label: "Docs inspection narrows to mirror-backed read-only; freshness label states the mirror snapshot's age."
continuity_fallback_class: mirror_snapshot_import
ledger_row_ref: "ledger.dep.docs_pack:air_gapped"
freshness_label: "Mirror snapshot is within the extended freshness window; refresh from the next signed offline bundle import."
redaction_class: metadata_safe_default
export_safe: true
notes: >
  Mirror-backed by construction. The freshness label is required because
  the dependency posture is mirrored.
