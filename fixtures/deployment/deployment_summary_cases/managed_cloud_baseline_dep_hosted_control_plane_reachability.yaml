# yaml-language-server: $schema=../../../schemas/deployment/deployment_summary_card.schema.json
$schema: "https://aureline.dev/schemas/deployment/deployment_summary_card.schema.json"
__fixture__:
  name: managed_cloud_baseline_dep_hosted_control_plane_reachability
  scenario: >
    Hosted control-plane reachability dependency for the managed-cloud
    baseline. Required and vendor-bound; when unreachable, managed-
    only flows block pending reconnect while local-core capabilities
    continue. This is the dependency a managed-cloud claim cannot
    silently shed.
  contract_sections:
    - "§4 The residual-dependency row record"
    - "§6.5 Vendor-bound dependencies cannot be customer-operated"
record_kind: residual_dependency_row_record
residual_dependency_row_schema_version: 1
row_id: "row.dep.managed_cloud_baseline.hosted_control_plane_reachability"
card_id_ref: "card.deployment.managed_cloud_baseline"
dependency_class: hosted_control_plane_reachability
posture_class: required
vendor_or_public_dependence: true
dependent_feature_label: "Vendor-operated managed control plane reachability for sync, registry, hosted review, AI broker, and policy refresh."
dependent_feature_refs:
  - "feature.managed.sync"
  - "feature.managed.registry"
  - "feature.managed.hosted_review"
  - "feature.managed.policy_refresh"
unreachable_impact_class: blocked_pending_reconnect
unreachable_impact_label: "Managed-only flows wait for reconnect; local-core editing, save, search, Git, tasks, docs inspection, export, and diagnostics continue."
continuity_fallback_class: resume_after_reconnect
ledger_row_ref: "ledger.dep.hosted_control_plane_reachability:managed_cloud"
redaction_class: metadata_safe_default
export_safe: true
