Protected fixture corpus for artifact save truth.

Each JSON file binds one admitted artifact family to one worked scenario proving:

- metadata-sensitive cue visibility
- fallback disclosure and action availability
- mutator drift posture
- one or more required evidence classes

The fixture file name matches `expected_row_id` from the canonical packet in
`artifacts/state/artifact_save_truth.json`.
