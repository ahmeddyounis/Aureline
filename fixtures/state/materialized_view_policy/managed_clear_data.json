{
  "consumer_ref": "crates/aureline-support/src/m5_clear_data_review/mod.rs",
  "expected_disposition": "local_replica_revoked_reconcile_later",
  "fixture_id": "fixture:mv_policy:managed_clear_data",
  "notes": "Clearing data on a managed replicated view revokes the local replica; it reconciles from the managed source on reconnect.",
  "operation": "clear_data",
  "record_kind": "materialized_view_policy_fixture_record",
  "schema_version": 1,
  "view_class": "managed_replicated_view"
}
