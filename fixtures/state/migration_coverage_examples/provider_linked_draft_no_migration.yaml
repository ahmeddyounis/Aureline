# Recovery evidence packet — provider-linked draft has no local migration.
#
# Validates against:
#   schemas/state/recovery_evidence_packet.schema.json

__fixture__:
  name: provider_linked_draft_no_migration
  scenario: >
    A local draft references provider-owned state. The product does not
    migrate provider-owned bodies; it preserves references, records a
    manual follow-up, and directs the user to refresh or re-authenticate
    through the provider authority path.

record_kind: recovery_evidence_packet_record
recovery_evidence_packet_schema_version: 1
packet_id: recovery-evidence-provider-linked-draft-0001
packet_label: Provider-linked draft — authority refresh required
subject_family_ref: provider_family:linked_drafts

before:
  build:
    producer_name: aureline
    producer_version: 0.0.0-dev
    producer_channel: experimental
    producer_platform_class: windows
    producer_instance_handle: producer-instance-provider-0001
  schema_version: provider-draft-ref-v1
  artifact_ref: provider-linked-draft-ref-0001

after:
  build:
    producer_name: aureline
    producer_version: 0.0.0-dev
    producer_channel: experimental
    producer_platform_class: windows
    producer_instance_handle: producer-instance-provider-0001
  schema_version: provider-draft-ref-v1
  artifact_ref: provider-linked-draft-ref-0001

backup_reference:
  backup_present: false
  backup_ref: null
  rollback_checkpoint_ref: null
  notes: Provider-owned bodies are not backed up by this lane; references only.

dry_run_diff:
  dry_run_performed: false
  diff_summary_ref: null
  diff_summary_text: null

retained_artifacts:
  - artifact_ref: provider-linked-draft-ref-0001
    role: other_retained_artifact
    notes: Preserved provider reference; no local migration of provider body.

warnings:
  - warning_class: provider_authority_required
    summary: Provider authority is required to refresh the linked draft.
    related_artifact_refs:
      - provider-linked-draft-ref-0001

manual_follow_ups:
  - follow_up_class: refresh_provider_authority
    summary: Re-authenticate or refresh provider session, then retry the operation.
    related_artifact_refs:
      - provider-linked-draft-ref-0001

rollback_result:
  outcome: not_applicable
  rollback_checkpoint_ref: null
  rolled_back_artifact_ref: null
  notes: Authority refresh; no rollback in this lane.

emitted_at: "mono:7000:01:08:11.0001"

