# Nested source tree fixture

This fixture contains a deeper-than-trivial directory layout with a mix of
file types. It exists to exercise:

- explorer tree expansion and path rendering
- quick-open ranking on deep paths
- edit/save on a non-root file

Primary edit target: `app/routes.ts`

