# yaml-language-server: $schema=../../../schemas/admin/device_rebind_event.schema.json
__fixture__:
  name: device_rebind_event_account_recovery
  scenario: >
    Device rebind is recorded as part of account recovery. The event
    captures actor class, scope, reason, and whether local-work/export
    guidance was shown.
  exercised_contract_sections:
    - device_rebind_audit_event_agreement
    - export_safe_audit_event

record_kind: device_rebind_event_record
device_rebind_event_schema_version: 1
event_id: device-rebind-event:0110
occurred_at: "2026-05-05T10:22:11Z"
actor:
  actor_class: end_user
  actor_ref: actor-subject:managed-user:0110
  actor_label: Managed user
scope:
  tenant_or_org_ref: tenant-or-org:managed:example:0110
  seat_id_ref: seat-id:recovered:0110
  previous_device_id_ref: device-id:prior-binding:0110
  device_id_ref: device-id:rebound:0110
  managed_session_state_ref: managed-session:recovered:0110
rebind_reason_class: account_recovery
guidance_capture:
  local_work_preservation_notice_shown: true
  export_guidance_shown: true
  local_artifacts_safety_class: local_artifacts_safe_export_guidance_shown
  handoff_record_ref: handoff:recovery-complete:0110
  note: The UI stated that local work remained intact and offered local export/support export actions.
reviewable_summary: Device rebind completed after account recovery; local work preserved and export guidance shown.
related_object_refs:
  - handoff:recovery-complete:0110
  - managed-session:recovered:0110
  - entitlement-snapshot:recovered:0110
  - support-bundle:recovery-complete:0110
redaction_summary:
  redaction_class: metadata_safe_default
  included_data_classes:
    - actor.class
    - tenant.scope
    - device.binding_rotation
    - guidance.flags
  omitted_data_classes:
    - device.fingerprint_raw
    - user.identifier_raw
    - idp.payload_raw
    - token.raw
  redaction_note: The event identifies devices by opaque id only; raw fingerprints and IdP payloads are omitted.

