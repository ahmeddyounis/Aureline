# yaml-language-server: $schema=../../../schemas/admin/audit_event_filter.schema.json
__fixture__:
  name: partial_history_view
  scenario: >
    The audit explorer is rendering a view whose source streams
    depend on a customer-managed mirror that has gone past its grace
    window. The completeness record labels the view as
    mirrored_stale_past_grace and names the next safe action
    (refresh_mirror) so the explorer never silently presents an
    implied full history.
  exercised_contract_sections:
    - partial_history_labelling
    - mirrored_stale_past_grace_posture
    - next_safe_action

record_kind: audit_event_history_completeness_record
audit_event_explorer_schema_version: 1
completeness_id_ref: audit-event:completeness:mirrored-stale-past-grace:0001
view_record_ref: audit-event:view:enterprise-managed:0042
completeness_class: mirrored_stale_past_grace
mirror_posture_class: customer_managed_mirror_stale
last_successful_sync_at: "2026-04-22T03:14:00Z"
partial_history_reason_class: mirror_stale_past_grace
acknowledged_gap_waiver_refs: []
next_safe_action_class: refresh_mirror
completeness_summary: The customer-managed mirror is past its grace window; the explorer renders the visible window only, marks counts as truncated, and recommends refreshing the mirror before claiming completeness.
