schema_version: 1
record_kind: release_rehearsal_fixture_manifest
manifest_id: release_rehearsal.beta.inputs
packet_ref: artifacts/release/m3/rehearsals/packet.json
support_projection_ref: artifacts/release/m3/rehearsals/support_export_projection.json
validator_ref: tools/ci/m3/rehearsals
cases:
  - case_id: release_rehearsal_case:publish_design_partner_feed
    flow_class: publish
    case_ref: fixtures/release/m3/rehearsal_inputs/publish_design_partner_feed.json
    expected_result_state: passed_with_limits
    expected_decision_class: hold_channel
  - case_id: release_rehearsal_case:rollback_coordinated_graph
    flow_class: rollback
    case_ref: fixtures/release/m3/rehearsal_inputs/rollback_coordinated_graph.json
    expected_result_state: passed
    expected_decision_class: no_gap
  - case_id: release_rehearsal_case:revocation_mirror_metadata
    flow_class: revocation
    case_ref: fixtures/release/m3/rehearsal_inputs/revocation_mirror_metadata.json
    expected_result_state: blocked_with_decision
    expected_decision_class: promotion_blocker
  - case_id: release_rehearsal_case:advisory_affected_build
    flow_class: advisory
    case_ref: fixtures/release/m3/rehearsal_inputs/advisory_affected_build.json
    expected_result_state: passed_with_downgrade
    expected_decision_class: claim_downgrade
