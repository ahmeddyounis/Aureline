{
  "_comment": "Deterministic failing scenario for tools/check_source_anchors.py. Removes a requirement row so the register reports an orphaned requirement id.",
  "remove_anchor_ids": [
    "req.CERT-WS-001"
  ]
}
