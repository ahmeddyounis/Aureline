{
  "_comment": "Deterministic failing scenario for tools/check_source_anchors.py. Drops the canonical anchors from one requirement row so the linter emits a missing_canonical_anchor finding without editing the real map.",
  "override_row": {
    "anchor_id": "req.ARCH-PACK-901",
    "canonical_anchors": []
  }
}
