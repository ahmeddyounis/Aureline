{
  "scenario_id": "missing_deployment_profile",
  "description": "Mutates one compatibility row so it claims a deployment profile id that does not exist in the shared vocabulary.",
  "mutations": [
    {
      "target": "artifacts/compat/qualification_matrix_seed.yaml",
      "path": ["qualification_rows", 0, "claimed_deployment_profiles", 0],
      "value": "missing_profile_for_demo"
    }
  ]
}
