scenario_id: schema_bump_without_example_review
description: >
  Treat a protected schema file as changed without updating the contract-example
  source map in the same diff.
changed_files:
  - schemas/commands/command_descriptor.schema.json

