record_kind: bundle_lifecycle_visibility_case_record
bundle_lifecycle_visibility_case_schema_version: 1
case_id: workflow.bundle_lifecycle.removed_extension_missing_artifact_visible
minted_at: "2026-05-06T20:55:00Z"

target_bundle:
  bundle_id: launch_bundle:typescript_web_app.seed
  bundle_revision: 1
  bundle_class: launch_bundle
  bundle_source_class: certified
  bundle_status_class: certified_retest_pending
  support_class: officially_supported

drift_rows:
  - drift_state_class: missing_artifact
    drift_axis: extension_set
    drift_severity_class: narrowing_review_recommended
    claim_narrowing_class: narrows_certification_target_pending_retest
    successor_bundle_suggestion_class: no_successor_drift_review
    notes: >
      A bundle-owned extension was removed/uninstalled locally. The missing
      artifact family remains visible as drift, narrows certification posture,
      and does not disappear behind a successor banner.

required_visibility_surfaces:
  - start_center_bundle_card
  - bundle_detail_page
  - project_doctor_recommended_action
  - support_export_packet

__fixture__:
  name: removed_extension_missing_artifact_visible
  scenario: >
    A previously applied bundle now has a missing extension artifact. Drift
    surfaces (and support export) must keep the missing artifact visible and
    reflect the narrowing of certification posture rather than presenting a
    clean Certified state.
  exercises:
    - removed_artifact_family_projects_as_drift_and_narrowing

