{
  "drill_id": "missing_pair_preview",
  "drill_description": "When the required pair (code_host, human_account) is absent from the descriptor snapshot, the page narrows to 'preview' because the required-pairs coverage condition is not met.",
  "expected_qualification_token": "preview",
  "expected_narrow_reason_token": "required_pair_missing",
  "trigger": "Remove the descriptor for (code_host, human_account) from the snapshot — only 8 of 9 required pairs are covered."
}
