# yaml-language-server: $schema=../../../schemas/testing/test_item_identity.schema.json
__fixture__:
  name: renamed_test_file
  scenario: >
    Exact remap after a test file rename. The source anchor and
    framework stable id still match, so the canonical test-item id is
    preserved and affected file selectors resolve through the remap
    record rather than broadening to a suite.
  exercised_classes_summary:
    record_kind: test_item_identity_remap_record
    remap_event_class: source_file_renamed
    remap_confidence_class: exact_source_anchor
    drift_behavior_class: keep_canonical_id

record_kind: test_item_identity_remap_record
test_item_identity_schema_version: 1
test_item_identity_remap_record_id: testing.identity_remap.rename.auth_totals.0001
previous_identity_ref: testing.identity.native.auth_totals.pre_rename.0001
previous_canonical_test_item_id: testing.canonical.auth_totals.case.valid_invoice
current_identity_ref: testing.identity.native.auth_totals.0001
current_canonical_test_item_id: testing.canonical.auth_totals.case.valid_invoice
remap_event_class: source_file_renamed
remap_confidence_class: exact_source_anchor
drift_behavior_class: keep_canonical_id
target_compatibility_class: same_target_exact
remap_evidence_refs:
  - navigation.rename_event.auth_totals_tests.0001
  - vfs.source_anchor.auth_totals.valid_invoice.post_rename
  - testing.identity.native.auth_totals.0001
affected_selector_refs:
  - testing.selector.file.auth_totals.pre_rename
  - testing.selector.id.auth_totals.valid_invoice
imported_provider_result_refs: []
explainability_notes:
  - Source anchor and framework stable id matched after rename; canonical id is preserved.
audit_event_refs:
  - audit_event.test_item_identity_remap_record.published
denial_reason_class: null
captured_at: "2026-04-29T08:20:00Z"
redaction_class: metadata_and_hashes_only
approval_ticket_ref: null
notes: >
  File selectors follow the remap record; rerun/debug actions retain
  exact scope and target identity.
