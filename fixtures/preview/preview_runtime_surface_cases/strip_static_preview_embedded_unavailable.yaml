# yaml-language-server: $schema=../../../schemas/preview/preview_runtime_strip.schema.json
__fixture__:
  name: strip_static_preview_embedded_unavailable
  scenario: >
    Embedded-lane static-preview strip rendered by the extension host. No
    live runtime, hot reload unavailable, mapping unavailable. The strip
    cannot claim live_runtime or mapped_edit_offered.

record_kind: preview_runtime_strip_record
preview_runtime_strip_schema_version: 1
strip_id: preview.strip.embedded.static.0001
captured_at: "2026-04-30T18:32:00Z"

preview_snapshot_record_ref: preview.snapshot.embedded.static.0001
hot_reload_state_record_ref: null
device_target_descriptor_ref: preview.device_target.viewport.preset.desktop.0001

preview_lane_class: embedded_preview_lane
preview_mode_class: static_preview

runtime_source_class: extension_provided_preview_runtime
target_kind_class: extension_host_preview_runtime

trust_state_class: TrustedLocalActive
policy_state_class: policy_in_force
connectivity_state_class: local_not_applicable

degraded_note:
  degraded_reason_trigger: preview_runtime_unavailable
  degraded_note_summary: >
    Static preview only. No live runtime is attached; export is the only
    outbound path.

source_revision_chip:
  source_sync_state_class: source_in_sync
  source_revision_class: vcs_commit_pinned
  source_revision_ref: revision.commit.workspace.primary.head
  drifted_paths_count: 0

theme_identity:
  theme_identity_class: light_default
  theme_revision_ref: null

device_identity_summary:
  device_target_class: viewport_preset_only
  viewport_preset_class: desktop_default_preset
  device_handle_ref: null
  redacted_device_label: null

session_freshness_class: fresh_within_session
mapping_quality_class: unavailable
hot_reload_state_class: unavailable
recovery_route_class: no_recovery_required_static_preview_unavailable

live_or_editable_claim_class: static_snapshot_only

share_sheet_handle_ref: null
approval_ticket_ref: null

summary: >
  Static preview rendered in the embedded extension host. Hot reload
  unavailable by construction; static export is the only outbound path.
notes: null
