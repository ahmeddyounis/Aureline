# yaml-language-server: $schema=../../../schemas/preview/preview_runtime_strip.schema.json
__fixture__:
  name: strip_stale_mapping_inspect_only
  scenario: >
    Browser-lane strip whose source revision drifted after the runtime was
    built. Mapping projects to unavailable; hot reload is restart_required.
    The strip surfaces a typed degraded note plus the restart_runtime
    recovery route. The claim drops to live_runtime_inspect_only.

record_kind: preview_runtime_strip_record
preview_runtime_strip_schema_version: 1
strip_id: preview.strip.browser.stale_mapping.0001
captured_at: "2026-04-30T18:33:30Z"

preview_snapshot_record_ref: preview.snapshot.browser.stale_mapping.0001
hot_reload_state_record_ref: preview.hot_reload.browser.restart_required.0001
device_target_descriptor_ref: preview.device_target.browser.window.0001

preview_lane_class: browser_preview_lane
preview_mode_class: live_preview

runtime_source_class: framework_dev_server
target_kind_class: local_dev_server

trust_state_class: TrustedLocalActive
policy_state_class: policy_in_force
connectivity_state_class: connected_verified

degraded_note:
  degraded_reason_trigger: source_revision_drifted
  degraded_note_summary: >
    Source revision moved after the runtime was built; restart the dev
    server to refresh the source-to-runtime mapping.

source_revision_chip:
  source_sync_state_class: source_drifted_since_render
  source_revision_class: working_tree_with_pending_edits
  source_revision_ref: revision.working_tree.fingerprint.0002
  drifted_paths_count: 4

theme_identity:
  theme_identity_class: light_default
  theme_revision_ref: null

device_identity_summary:
  device_target_class: browser_window_target
  viewport_preset_class: desktop_default_preset
  device_handle_ref: device.handle.browser.window.0001
  redacted_device_label: "Local Chromium window (desktop default)"

session_freshness_class: fresh_within_minutes
mapping_quality_class: unavailable
hot_reload_state_class: restart_required
recovery_route_class: restart_runtime_recovery

live_or_editable_claim_class: live_runtime_inspect_only

share_sheet_handle_ref: null
approval_ticket_ref: null

summary: >
  Source drifted since render; mapping is stale and the dev server must
  restart before mapped edits are admissible again.
notes: >
  blocked_actions on the underlying snapshot already strip apply_mapped_edit
  and direct_write_to_canonical_source from effective_allowed_actions; the
  strip carries the typed reason here.
