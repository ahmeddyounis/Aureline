# yaml-language-server: $schema=../../../schemas/preview/preview_runtime_strip.schema.json
__fixture__:
  name: strip_visual_edit_mapped_edit_offered
  scenario: >
    Browser-lane visual-edit strip whose underlying snapshot satisfies every
    visual-edit gate (exact mapping + applied hot reload + transform manifest).
    The strip claims mapped_edit_offered and points at the underlying
    snapshot through preview_snapshot_record_ref.

record_kind: preview_runtime_strip_record
preview_runtime_strip_schema_version: 1
strip_id: preview.strip.browser.visual_edit.0001
captured_at: "2026-04-30T18:31:15Z"

preview_snapshot_record_ref: preview.snapshot.browser.visual_edit.0001
hot_reload_state_record_ref: preview.hot_reload.browser.applied.visual_edit.0001
device_target_descriptor_ref: preview.device_target.browser.window.0001

preview_lane_class: browser_preview_lane
preview_mode_class: visual_edit

runtime_source_class: framework_dev_server
target_kind_class: local_dev_server

trust_state_class: TrustedLocalActive
policy_state_class: policy_in_force
connectivity_state_class: connected_verified

degraded_note: null

source_revision_chip:
  source_sync_state_class: source_in_sync
  source_revision_class: working_tree_with_pending_edits
  source_revision_ref: revision.working_tree.fingerprint.0001
  drifted_paths_count: 0

theme_identity:
  theme_identity_class: dark_default
  theme_revision_ref: null

device_identity_summary:
  device_target_class: browser_window_target
  viewport_preset_class: desktop_default_preset
  device_handle_ref: device.handle.browser.window.0001
  redacted_device_label: "Local Chromium window (desktop default)"

session_freshness_class: fresh_within_session
mapping_quality_class: exact
hot_reload_state_class: applied
recovery_route_class: no_recovery_required_applied

live_or_editable_claim_class: mapped_edit_offered

share_sheet_handle_ref: null
approval_ticket_ref: null

summary: >
  Visual-edit strip on browser lane. Exact mapping plus applied hot reload
  plus transform manifest with rollback checkpoint admit mapped edits.
notes: >
  Mapped-edit gate is enforced upstream by the snapshot record and the
  hot-reload state record; the strip surfaces the resolved claim only.
