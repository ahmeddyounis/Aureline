# yaml-language-server: $schema=../../../schemas/preview/device_target_descriptor.schema.json
__fixture__:
  name: device_target_managed_pool_reservation
  scenario: >
    Managed-device-pool descriptor with availability_class = requires_approval.
    The descriptor carries a non-null managed_pool_ref and a non-null
    approval_ticket_ref so the picker can route the user through the
    integration approval flow before the device is reserved.

record_kind: device_target_descriptor_record
device_target_descriptor_schema_version: 1
descriptor_id: preview.device_target.managed_pool.acme.0001
captured_at: "2026-04-30T18:40:25Z"

picker_group_class: managed_device_pool_group
device_target_class: managed_device_pool
viewport_preset_class: tablet_landscape_preset

is_real_runtime_target: true
device_handle_ref: device.handle.managed_pool.acme.tablet.0001

redacted_label: "Acme Corp managed device pool: tablet class"

viewport_pixel_width: 1180
viewport_pixel_height: 820
device_pixel_ratio_class: two_x

availability_class: requires_approval
availability_observed_at: "2026-04-30T18:40:25Z"
pairing_requirement_class: managed_pool_reservation_required
approval_ticket_ref: integration.approval.ticket.acme.device_pool.0001

managed_pool_ref: managed.device.pool.acme.tablet.0001
external_handoff_target_ref: null
bound_preview_snapshot_record_ref: null

summary: >
  Managed device pool reservation gated on an integration approval ticket.
  Pool reservation must complete before the runtime can bind.
