# yaml-language-server: $schema=../../../schemas/preview/preview_runtime_strip.schema.json
__fixture__:
  name: strip_runtime_unavailable_export_only
  scenario: >
    Browser-lane strip whose dev server is unreachable. Connectivity has
    dropped to disconnected_static_snapshot, mapping is unavailable, hot
    reload has failed. The strip routes to runtime_unavailable_export_only
    with export_metadata_only_recovery and a non-null degraded note.

record_kind: preview_runtime_strip_record
preview_runtime_strip_schema_version: 1
strip_id: preview.strip.browser.runtime_unavailable.0001
captured_at: "2026-04-30T18:35:00Z"

preview_snapshot_record_ref: preview.snapshot.browser.runtime_unavailable.0001
hot_reload_state_record_ref: preview.hot_reload.browser.failed.0001
device_target_descriptor_ref: preview.device_target.browser.window.0001

preview_lane_class: browser_preview_lane
preview_mode_class: live_preview

runtime_source_class: framework_dev_server
target_kind_class: local_dev_server

trust_state_class: TrustedLocalActive
policy_state_class: policy_in_force
connectivity_state_class: disconnected_static_snapshot

degraded_note:
  degraded_reason_trigger: preview_runtime_unavailable
  degraded_note_summary: >
    Dev server is unreachable. Export the metadata-only packet; the
    canonical source remains the source of truth.

source_revision_chip:
  source_sync_state_class: source_revision_unknown
  source_revision_class: unknown_revision
  source_revision_ref: null
  drifted_paths_count: null

theme_identity:
  theme_identity_class: light_default
  theme_revision_ref: null

device_identity_summary:
  device_target_class: browser_window_target
  viewport_preset_class: desktop_default_preset
  device_handle_ref: device.handle.browser.window.0001
  redacted_device_label: "Local Chromium window (desktop default)"

session_freshness_class: stale_unknown_age
mapping_quality_class: unavailable
hot_reload_state_class: failed
recovery_route_class: export_metadata_only_recovery

live_or_editable_claim_class: runtime_unavailable_export_only

share_sheet_handle_ref: null
approval_ticket_ref: null

summary: >
  Browser-lane preview whose dev server cannot be reached. The strip is
  export-only and routes the user to a metadata-only packet.
notes: null
