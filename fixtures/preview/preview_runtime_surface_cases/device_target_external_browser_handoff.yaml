# yaml-language-server: $schema=../../../schemas/preview/device_target_descriptor.schema.json
__fixture__:
  name: device_target_external_browser_handoff
  scenario: >
    External-handoff descriptor for an external browser handoff target.
    Carries a non-null external_handoff_target_ref and an
    external_handoff_handshake_required pairing class.

record_kind: device_target_descriptor_record
device_target_descriptor_schema_version: 1
descriptor_id: preview.device_target.external.browser_handoff.0001
captured_at: "2026-04-30T18:40:30Z"

picker_group_class: external_handoff_group
device_target_class: remote_attached_device
viewport_preset_class: desktop_default_preset

is_real_runtime_target: true
device_handle_ref: device.handle.external.browser_handoff.0001

redacted_label: "External browser handoff target"

viewport_pixel_width: 1440
viewport_pixel_height: 900
device_pixel_ratio_class: two_x

availability_class: available_remote_only
availability_observed_at: "2026-04-30T18:40:30Z"
pairing_requirement_class: external_handoff_handshake_required
approval_ticket_ref: null

managed_pool_ref: null
external_handoff_target_ref: external.handoff.target.browser.0001
bound_preview_snapshot_record_ref: null

summary: >
  External-handoff target reachable through a brokered handshake. The
  picker isolates external handoffs from attached browsers so users can
  see they are leaving the local workstation.
