report: id=migration:m5_depth_import:v1:default, rows=6, classes=6/6
preview_before_apply=true every_apply_reversible=true no_overclaimed_parity=true
Notebook handoff import [notebook_handoff]
  outcome=imported fidelity=translated scope=native
  mutates_durable_state=true reversible=true
  restore: Restore from the pre-apply notebook checkpoint to revert the imported document.
  deviation: Live kernel state and execution counts are not part of the handoff; the imported document opens without a running kernel.
Request and schema bundle import [request_schema_bundle]
  outcome=mapped fidelity=partial scope=partial_mapping
  mutates_durable_state=true reversible=true
  restore: Restore from the pre-apply request-workspace checkpoint to remove the mapped collection.
  compatibility_note: Saved requests and the API schema map into a request workspace; environment secrets and pre-request scripts are not imported and must be re-entered.
  deviation: Environment secrets and pre-request scripting do not cross the boundary.
Database query and session export [database_query_session_export]
  outcome=mapped fidelity=partial scope=partial_mapping
  mutates_durable_state=true reversible=true
  restore: Restore from the pre-apply data-workspace checkpoint to remove the imported query library.
  compatibility_note: Saved queries and session metadata map into a query library; live connection credentials are never imported and must be reconnected explicitly.
  deviation: Live connection credentials and open result-set state do not cross the boundary.
Profiler or trace capture import [profiler_trace_capture]
  outcome=manual_review fidelity=partial scope=inspect_only
  mutates_durable_state=false reversible=true
  compatibility_note: Captured traces open in an inspect-only viewer; Aureline does not re-run or re-symbolicate the capture, so the import does not mutate durable state.
  deviation: Re-symbolication and re-capture are not performed; symbols already present in the capture are shown as-is.
Signed template or scaffold manifest import [template_scaffold_manifest]
  outcome=bridge_required fidelity=shimmed scope=bridge
  mutates_durable_state=false reversible=true
  compatibility_note: Signed scaffold manifests run through a compatibility bridge rather than as a native scaffold; the bridge must be present before the scaffold can execute.
  deviation: There is no native scaffold engine for this manifest format; continuity depends on the bridge remaining available.
Companion or export packet import [companion_export_packet]
  outcome=unsupported fidelity=unsupported scope=export_bundle
  mutates_durable_state=false reversible=true
  compatibility_note: Companion export packets are preserved as a re-exportable bundle for offline review; Aureline does not import them as a live companion session, so the bundle is the supported continuity path.
  deviation: Live companion device sessions are not reconstructed; only the export bundle is retained.
