# Experiment Provenance and Result Comparison Fixtures

These fixtures exercise the checked-in packet at
`artifacts/data/qualify-experiment-provenance-and-result-comparison.json`.

The cases cover code revision drift, dataset snapshot drift, environment drift,
missing retained lineage, and a raw-preview-blocked dataset that still preserves
metadata-only export and comparison evidence.
