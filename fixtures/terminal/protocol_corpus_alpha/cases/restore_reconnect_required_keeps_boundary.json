{
  "record_kind": "terminal_protocol_corpus_alpha_case",
  "schema_version": 1,
  "case_id": "restore_reconnect_required_keeps_boundary",
  "case_kind": "restore_conformance",
  "protected_launch_wedge": true,
  "scenario": "A remote terminal loses transport. Restore conformance reports reconnect_required, keeps the remote boundary visible, and forbids command replay while a reconnect decision is pending.",
  "coverage_tags": [
    "reconnect_required",
    "remote_boundary_visible",
    "no_auto_rerun"
  ],
  "input": {
    "input_kind": "restore_conformance",
    "driver": "reconnect_required"
  },
  "expect": {
    "expect_kind": "restore_conformance",
    "state": "reconnect_required",
    "state_token": "reconnect_required",
    "reconnect_required": true,
    "transcript_restored": false,
    "ended_requires_fresh_session": false,
    "auto_rerun_forbidden": true,
    "fresh_session_required": false,
    "open_fresh_session_command_visible": false,
    "boundary_cue_visible": true
  }
}
