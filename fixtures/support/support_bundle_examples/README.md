# Support bundle examples

These fixtures are worked support-bundle packets that exercise the
governed bundle contract in
[`/schemas/support/support_bundle.schema.json`](../../../schemas/support/support_bundle.schema.json).

## Index

| Fixture | Key behaviors shown |
|---|---|
| [`recovery_ladder_remote_connector_loss.json`](./recovery_ladder_remote_connector_loss.json) | exact-build embedding, by-reference route truth, local-only retained trace, optional-upload crash dump, intentionally excluded shell history, continuity/fault-domain/recovery linkage |
