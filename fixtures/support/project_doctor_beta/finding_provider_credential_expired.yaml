# yaml-language-server: $schema=../../../schemas/support/project_doctor.schema.json
# Beta finding: provider credential expired and reauthentication required.

schema_version: 1
record_kind: project_doctor_finding_record
finding_id: finding:project_doctor.beta.provider_auth.credential_expired.example
finding_code: doctor.finding.provider_auth.credential_expired
probe_pack_ref: project_doctor.pack.provider_auth.v1
probe_pack_class: provider_auth
probe_pack_version: pack.provider_auth.v1

severity_class: blocking
confidence_class: observed_authoritative
diagnosis_posture: proving_diagnosis
recovery_handoff_class: handoff_only
support_context_class: support_guided

render_surfaces:
  - ui_finding_card
  - cli_finding_row
  - support_export_row

attribution_refs:
  - attribution_kind: probe_pack_ref
    ref: project_doctor.pack.provider_auth.v1
  - attribution_kind: doctor_rule_ref
    ref: doctor.rule.provider_auth.credential_expired
  - attribution_kind: evidence_ref
    ref: evidence:provider.credential_state

evidence_refs:
  - evidence:provider.credential_state
  - evidence:provider.scope_request

summary: >
  The provider credential is expired; the support-guided session can mint a
  reauthentication handoff that excludes raw provider payload and secret
  material.

captured_at: "2026-05-15T12:00:00Z"
redaction_class: metadata_safe_default
raw_private_material_excluded: true
