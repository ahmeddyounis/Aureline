schema_version: 1
record_kind: project_doctor_alpha_probe_scenario
scenario_id: project_doctor.alpha.provider_credential_expired
title: Connected provider credential handle expired
family: provider_auth
probe_id: doctor.probe.provider.auth_state
probe_version: probe.provider.auth_state.v1
support_context: remote_managed

scope:
  scope_class: session
  scope_ref: session:provider.ai_gateway

observed_state:
  problem_class: provider_credential_expired
  expected_ref: credential-state:provider.handle_current
  observed_ref: credential-state:provider.handle_expired
  expected_key: doctor.provider_auth.expected.handle_current
  observed_key: doctor.provider_auth.observed.handle_expired

evidence:
  - evidence_ref: evidence:credential_state.expired_handle
    evidence_role: primary
    source_class: existing_manifest
    signal_class: manifest
    data_class: metadata_only
    redaction_class: metadata_safe_default
    support_pack_inclusion_class: included_by_default_metadata
    replayability_class: replayable_from_refs
  - evidence_ref: evidence:provider.registry.descriptor
    evidence_role: corroborating
    source_class: existing_manifest
    signal_class: manifest
    data_class: metadata_only
    redaction_class: metadata_safe_default
    support_pack_inclusion_class: included_by_reference
    replayability_class: replayable_from_refs

safety:
  read_only_by_default: true
  allowed_side_effects:
    - read_existing_metadata
  forbidden_side_effects:
    - activate_third_party_extension
    - collect_high_risk_payload
    - execute_repo_owned_code
    - external_service_mutation
    - mutate_cache_or_index
    - mutate_target_or_route
    - mutate_trust_policy_or_credentials
    - mutate_user_files
  later_repair_flow_required_for_mutation: true

recovery:
  recovery_action_id: action.provider_auth.reauthenticate
  recovery_action_class: reauthenticate_provider
  repair_availability_class: reviewed_repair_available
  repair_candidate_ids:
    - repair.provider_auth.renew_handle.preview
  recovery_path_ref: repair.preview.provider_auth.renew_handle
  runbook_ref: docs/support/runbook_execution_contract.md#provider-auth
  support_bundle_ref: support.bundle.project_doctor.provider_auth
  escalation_packet_ref: escalation.packet.project_doctor.provider_auth

expected:
  finding_id: finding:project_doctor:alpha:provider_credential_expired
  finding_code: doctor.finding.provider_auth.credential_expired
  recovery_action_class: reauthenticate_provider
