# Hardened recovery-ladder flow: targeted reset
#
# Resets exactly one targeted disposable state class.

schema_version: 1
record_kind: hardened_recovery_ladder_flow_for_cache_rebuild_settings_repair_state_migration_repair_and_targeted_resets_record
flow_id: hardened_recovery_flow:targeted_reset.alpha_v1
flow_class: targeted_reset
rung_class: targeted_reset
blast_radius_class: single_disposable_state_class
reversal_class: exact
checkpoint_class: durable_pre_apply
checkpoint_ref: checkpoint:targeted-reset:seed
doctor_finding_ref: doctor.finding.targeted_disposable_state_corruption
preserved_state_classes:
  - user_authored_files
  - open_buffer_selection
  - durable_workspace_indexes
  - workspace_trust_store
  - credential_store
  - session_restore_store
  - support_export_store
impacted_state_classes:
  - targeted_disposable_state
requires_user_consent: true
requires_admin_consent: false
support_guidance: >
  Resets one specific disposable state class (for example a watcher
  backlog or derived cache) to its default state. A durable checkpoint
  was captured before the reset, so you can roll back if needed. Your
  files, open buffers, and trust settings are not affected.
flow_summary: >
  Reset exactly one targeted disposable state class behind a durable
  checkpoint. Blast radius is strictly single-object. All durable and
  user-authored state is preserved.
evidence_refs:
  - evidence:targeted_reset:doctor_finding_targeted_disposable_state_corruption
  - evidence:targeted_reset:checkpoint_ref_seed
redaction_class: metadata_safe_default
captured_at: "2026-06-02T13:00:00Z"
