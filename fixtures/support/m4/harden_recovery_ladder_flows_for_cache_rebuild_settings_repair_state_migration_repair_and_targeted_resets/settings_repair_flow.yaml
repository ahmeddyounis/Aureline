# Hardened recovery-ladder flow: settings repair
#
# Repairs settings or profile state from a captured backup or authoritative
# source.

schema_version: 1
record_kind: hardened_recovery_ladder_flow_for_cache_rebuild_settings_repair_state_migration_repair_and_targeted_resets_record
flow_id: hardened_recovery_flow:settings_repair.alpha_v1
flow_class: settings_repair
rung_class: settings_repair
blast_radius_class: same_family_state_classes
reversal_class: compensating
checkpoint_class: durable_pre_apply
checkpoint_ref: checkpoint:settings-repair:seed
doctor_finding_ref: doctor.finding.settings_integrity_failure
preserved_state_classes:
  - user_authored_files
  - open_buffer_selection
  - durable_workspace_indexes
  - workspace_trust_store
  - credential_store
  - session_restore_store
  - support_export_store
  - settings_profile_state
impacted_state_classes:
  - settings_profile_state
requires_user_consent: true
requires_admin_consent: false
support_guidance: >
  Repairs your workspace settings from the last known-good backup.
  Open files, trust decisions, and credentials are preserved. Review
  the settings diff before confirming; you can roll back to the
  checkpoint if anything looks wrong.
flow_summary: >
  Repair settings/profile state from backup behind a durable checkpoint.
  Preserves all user-authored and durable state; blast radius is limited
  to same-family settings state.
evidence_refs:
  - evidence:settings_repair:doctor_finding_settings_integrity_failure
  - evidence:settings_repair:checkpoint_ref_seed
redaction_class: metadata_safe_default
captured_at: "2026-06-02T13:00:00Z"
