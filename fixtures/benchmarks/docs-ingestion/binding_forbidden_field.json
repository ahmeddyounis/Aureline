{
  "$schema": "../../../schemas/benchmarks/publication-ingestion.schema.json",
  "__fixture__": {
    "fixture_id": "fixture.publication_ingestion.forbidden_field",
    "expect_schema_valid": true,
    "expect_admitted": false,
    "rejected_by": "disclosed_field_forbidden",
    "scenario": "A binding that discloses a forbidden raw field is rejected, so raw run logs and other sensitive material never cross into a surface."
  },
  "record_kind": "benchmark_publication_ingestion_binding",
  "schema_version": 1,
  "binding_id": "fixture.ingest.support_export.forbidden_field",
  "surface": "support_export",
  "surface_locator": "support_export.benchmark_context.buffer_operations",
  "consumes_entry_id": "publication_pack.aureline_only.buffer_operations",
  "repro_pack_ref": "repro_pack.aureline_only.buffer_operations",
  "renders_verbatim_from_entry": true,
  "rendered_projection": {
    "posture": "aureline_only_claim",
    "effective_claim": "aureline_only_claim",
    "freshness_state": "current",
    "downgrade_label": "Aureline-only claim backed by a current reference capture.",
    "metric_refs": ["ff.buffer_operations"]
  },
  "disclosed_fields": ["entry_id", "effective_claim", "freshness_state", "downgrade_label", "metric_refs", "raw_run_log"],
  "notes": "Negative case: raw_run_log is on the forbidden denylist and must never be disclosed."
}
