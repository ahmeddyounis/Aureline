{
  "$schema": "../../../schemas/benchmarks/publication-ingestion.schema.json",
  "__fixture__": {
    "fixture_id": "fixture.publication_ingestion.schema_missing_field",
    "expect_schema_valid": false,
    "expect_admitted": false,
    "rejected_by": "schema_required_field",
    "scenario": "A binding record that omits the required rendered_projection fails closed at the schema boundary before any admission rule runs."
  },
  "record_kind": "benchmark_publication_ingestion_binding",
  "schema_version": 1,
  "binding_id": "fixture.ingest.docs.schema_missing_field",
  "surface": "docs",
  "surface_locator": "docs/benchmarks/public-comparison-packs.md#buffer-operations",
  "consumes_entry_id": "publication_pack.aureline_only.buffer_operations",
  "repro_pack_ref": "repro_pack.aureline_only.buffer_operations",
  "renders_verbatim_from_entry": true,
  "disclosed_fields": ["entry_id", "effective_claim", "freshness_state", "downgrade_label", "metric_refs", "repro_pack_ref"],
  "notes": "Negative case: the required rendered_projection field is missing."
}
