{
  "$schema": "../../../schemas/benchmarks/publication-ingestion.schema.json",
  "__fixture__": {
    "fixture_id": "fixture.publication_ingestion.not_verbatim",
    "expect_schema_valid": true,
    "expect_admitted": false,
    "rejected_by": "not_rendered_verbatim",
    "scenario": "A binding that admits it does not render verbatim from the entry is rejected, so hand-written benchmark prose cannot be registered as a binding."
  },
  "record_kind": "benchmark_publication_ingestion_binding",
  "schema_version": 1,
  "binding_id": "fixture.ingest.docs.not_verbatim",
  "surface": "docs",
  "surface_locator": "docs/benchmarks/public-comparison-packs.md#buffer-operations",
  "consumes_entry_id": "publication_pack.aureline_only.buffer_operations",
  "repro_pack_ref": "repro_pack.aureline_only.buffer_operations",
  "renders_verbatim_from_entry": false,
  "rendered_projection": {
    "posture": "aureline_only_claim",
    "effective_claim": "aureline_only_claim",
    "freshness_state": "current",
    "downgrade_label": "Aureline-only claim backed by a current reference capture.",
    "metric_refs": ["ff.buffer_operations"]
  },
  "disclosed_fields": ["entry_id", "effective_claim", "freshness_state", "downgrade_label", "metric_refs", "repro_pack_ref"],
  "notes": "Negative case: renders_verbatim_from_entry is false, so the binding does not promise to render the canonical entry."
}
