# Help search and keymap bridge projection cases

Worked cases for:

- [`docs/ux/help_search_and_keymap_bridge_projection.md`](../../../docs/ux/help_search_and_keymap_bridge_projection.md)
- [`schemas/ux/discoverability_projection.schema.json`](../../../schemas/ux/discoverability_projection.schema.json)

The corpus covers:

- VS Code-style shortcut import projected into help search and migration lanes.
- Unsupported imported command that remains visible without promising parity.
- Multi-command bridge where one source command maps to multiple candidates.
- Leader-sequence bridge where the shortcut shape changes honestly (`translated`).
- Contextual-tip lane reusing the same command projection reference.
- Source-language fallback disclosure that preserves canonical command identity.

