# yaml-language-server: $schema=../../../schemas/ux/learning_progress_retention.schema.json
__fixture__:
  name: delete_review_preserves_workspace
  scenario: >
    A user-initiated delete review scoped to a single
    don't-show-again retention row. delete_class is
    deletable_per_profile; delete_origin_class is
    user_initiated_delete; preserves_unrelated_workspace_content is
    const-true (delete does not touch source files, workspace
    manifests, caches, recent-work metadata, or credentials).
    Exercises §7 delete review rules 1 and 2, and §9 invariant 11.
  exercised_axes:
    record_kind: learning_artifact_delete_review_record
    delete_class: deletable_per_profile
    delete_origin_class: user_initiated_delete
    preserves_unrelated_workspace_content: true
  contract_sections:
    - "§7 delete review"
    - "§9 manifest invariants"

record_kind: learning_artifact_delete_review_record
learning_progress_retention_schema_version: 1
review_id: "ux:learn:retention:delete_review:user_dsa_clear:01"
learning_profile_ref: "ux:learn:profile:default_individual_learner:01"
row_refs:
  - "ux:learn:retention:dsa:per_profile:01"
delete_class: deletable_per_profile
delete_origin_class: user_initiated_delete
preserves_unrelated_workspace_content: true
deployment_profile_refs:
  - individual_local
  - self_hosted
  - enterprise_online
policy_context:
  policy_epoch: "pe:2026-05-02:01"
  trust_state: trusted
redaction_class: metadata_safe_default
presentation_label: "Clear don't-show-again toggle (workspace preserved)"
denial_reason_on_deny: null
minted_at: "2026-05-02T19:40:09Z"
