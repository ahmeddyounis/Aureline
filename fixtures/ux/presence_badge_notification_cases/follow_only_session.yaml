__fixture__:
  name: follow_only_session
  case_category: follow_view_authority_projection
  scenario: >
    A guest joins a collaboration session in view-first mode and enables follow.
    The native presence/status projection may show follow mode and freshness,
    but must not imply shared terminal/debug control or recording.
  exercised_axes:
    follow_mode_class: cursor_follow_exact
    control_grant_present: false
    recording_admitted: false
  contract_sections:
    - "Core invariants"
    - "Status-item / tray / menu-bar presence indicator"

record_kind: presence_badge_notification_parity_case_record
presence_badge_notification_parity_schema_version: 1
case_id: case:presence_badge_notification_parity:follow-only:01
case_category: follow_view_authority_projection

matrix_row_refs:
  - presence_row:follow_only_view_authority

canonical_artifact_refs:
  - docs/ux/native_presence_notification_parity.md
  - artifacts/ux/presence_surface_matrix.yaml
  - schemas/collaboration/follow_and_presenter_state.schema.json
  - schemas/collaboration/control_grant.schema.json

upstream_fixture_refs:
  join_view_only_follow: fixtures/collaboration/join_follow_cases/join_approved_view_only_follow_and_debug_view.yaml

expected_os_projection:
  status_item:
    renders: true
    must_include:
      - follow_mode_class
      - follow_target_kind_class
      - freshness_class
    must_not_imply:
      - shared_terminal_control
      - shared_debug_control
      - recording_active
  os_notification:
    renders: false
  os_badge_app_icon:
    renders: false

reviewer_checks:
  - id: follow_is_view_authority_only
    description: >
      Follow mode projects view authority only and never implies any control
      grant; absence of control_grant_record keeps control posture off.
    pass: true

minted_at: "2026-05-07T00:00:00Z"

