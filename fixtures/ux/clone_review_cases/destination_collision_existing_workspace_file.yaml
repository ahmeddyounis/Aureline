# yaml-language-server: $schema=../../../schemas/ux/clone_review.schema.json
__fixture__:
  name: destination_collision_existing_workspace_file
  scenario: >
    The chosen destination contains an Aureline workspace manifest. The
    destination-collision sheet routes the activation through
    add_existing_to_workspace as the safe default, with reveal available.
    Demonstrates §4.2 destination_collision_class
    (existing_workspace_file_at_path) and §4.4 rule 6 (workspace-file
    match prefers add-existing).
  record_kind_exercised: destination_collision_sheet_record
  doc_sections:
    - "§4.2 destination_collision_class"
    - "§4.3 safe_action_class"
    - "§4.4 Acceptance rules (collision-sheet level)"

record_kind: destination_collision_sheet_record
destination_collision_schema_version: 1
destination_collision_id: dest-collision-existing-workspace-file-0001
clone_review_ref: clone-review-https-default-branch-0003
destination_path_label: "~/Code/acme-monorepo"
destination_collision_class: existing_workspace_file_at_path
safe_actions:
  - add_existing_to_workspace
  - reveal_in_filesystem
  - cancel_no_change
  - inspect_only
temp_directory_disclosed: false
explicit_overwrite_required: false
existing_target_descriptor_ref: filesystem-identity-acme-monorepo-workspace-0001
presentation_label: "Existing workspace manifest at destination"
summary: "The destination contains an Aureline workspace manifest. Add it to the active workspace via add_root or reveal it for inspection. No bytes are overwritten."
minted_at: "mono:0000:00:00:00.0000"
