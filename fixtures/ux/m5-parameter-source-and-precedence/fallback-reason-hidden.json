{
  "case_id": "fallback-reason-hidden",
  "kind": "narrowing",
  "description": "A fall back to a default whose reason is not disclosed floors.",
  "base_field_id": "field:package-install-config:0001",
  "stale_window": false,
  "overrides": {
    "inspector.fallback.fallback_reason_disclosed": false,
    "renderings[*].rendered_claim": "parameter_unsafe"
  },
  "expected": {
    "effective_claim": "parameter_unsafe",
    "narrowed": true,
    "active_narrowing_reasons": [
      "fallback_reason_hidden"
    ]
  }
}
