{
  "case_id": "policy-lock-hidden",
  "kind": "narrowing",
  "description": "A policy-locked field that does not surface the lock floors.",
  "base_field_id": "field:source-registration:0001",
  "stale_window": false,
  "overrides": {
    "inspector.policy_lock.lock_surfaced": false,
    "renderings[*].rendered_claim": "parameter_unsafe"
  },
  "expected": {
    "effective_claim": "parameter_unsafe",
    "narrowed": true,
    "active_narrowing_reasons": [
      "policy_lock_hidden"
    ]
  }
}
