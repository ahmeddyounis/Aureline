{
  "case_id": "policy-lock-not-enforced",
  "kind": "narrowing",
  "description": "A policy-locked field that still allows a silent user override floors.",
  "base_field_id": "field:source-registration:0001",
  "stale_window": false,
  "overrides": {
    "inspector.policy_lock.override_allowed_despite_lock": true,
    "renderings[*].rendered_claim": "parameter_unsafe"
  },
  "expected": {
    "effective_claim": "parameter_unsafe",
    "narrowed": true,
    "active_narrowing_reasons": [
      "policy_lock_not_enforced"
    ]
  }
}
