__fixture__:
  name: palette_route_parity_across_entries
  case_category: identical_route_multi_entry_surfaces
  scenario: >
    Multiple protected entry points are invoked from the command palette.
    Each entry's protected_entry_badge_record carries target_route_class =
    palette_invocation_route. Route wording, ordering position, and tooltip
    field sources are consistent even though the targets differ (task runner,
    debug adapter, external provider mutation, browser handoff).
  exercised_axes:
    target_route_class: palette_invocation_route
    entry_surfaces:
      - task_launcher
      - debug_launcher
      - provider_tile_entry
      - browser_handoff_sheet
  contract_sections:
    - "Ordering and tooltip discipline"
    - "Surfaces under audit"

record_kind: protected_entry_badge_parity_case_record
protected_entry_badge_parity_schema_version: 1
case_id: case:protected_entry_badge_parity:route:palette:01
case_category: identical_route_multi_entry_surfaces

matrix_row_refs:
  - pe_badge_row:tasks:local_task_runner:launch
  - pe_badge_row:debug:local_debug_adapter:launch
  - pe_badge_row:provider_entry:external_provider:mutation
  - pe_badge_row:browser_handoff:external_site:consent

canonical_artifact_refs:
  - artifacts/ux/protected_entry_badge_matrix.yaml
  - docs/ux/protected_entry_badge_parity.md
  - schemas/commands/diagnostic_projection.schema.json
  - docs/runtime/origin_target_route_taxonomy.md

surface_audit_rows:
  - entry_surface: task_launcher
    badge_fixture_ref: fixtures/commands/diagnostic_rows/badge_tasks_entry_launch.json
    expected_target_route_class: palette_invocation_route
    parity_finding_class: parity_complete
  - entry_surface: debug_launcher
    badge_fixture_ref: fixtures/commands/diagnostic_rows/badge_debug_entry_launch.json
    expected_target_route_class: palette_invocation_route
    parity_finding_class: parity_complete
  - entry_surface: provider_tile_entry
    badge_fixture_ref: fixtures/commands/diagnostic_rows/badge_provider_bearing_publish.json
    expected_target_route_class: palette_invocation_route
    parity_finding_class: parity_complete
  - entry_surface: browser_handoff_sheet
    badge_fixture_ref: fixtures/commands/diagnostic_rows/badge_browser_handoff.json
    expected_target_route_class: palette_invocation_route
    parity_finding_class: parity_complete

notes: >
  This case intentionally varies target_kind_class and target_trust_class across
  surfaces; only the route token is held constant. Reviewers should be able to
  compare screenshots/logs and see the same route label derived from the same
  controlled vocabulary, not from surface-local prose.
minted_at: "2026-05-07T13:00:00Z"

