__fixture__:
  name: policy_limited_entry_badge_retained
  case_category: restricted_or_policy_limited_entry
  scenario: >
    A protected entry is policy-limited (disabled with a typed reason). The
    entry continues to render its protected-entry badge so users and support
    exports retain target/origin truth; only labels narrow by export_safe_wording
    class. Removing the badge as a side-effect of disablement is non-conforming.
  exercised_axes:
    disabled_reason_code: policy_blocked_in_context
    protected_entry_badge_required: true
  contract_sections:
    - "Export-safe wording class"
    - "Client-scope and policy-limited entry"

record_kind: protected_entry_badge_parity_case_record
protected_entry_badge_parity_schema_version: 1
case_id: case:protected_entry_badge_parity:policy_limited_entry:01
case_category: restricted_or_policy_limited_entry

canonical_artifact_refs:
  - fixtures/commands/diagnostic_rows/diagnostic_row_policy_blocked.json
  - fixtures/commands/diagnostic_rows/badge_provider_bearing_push.json
  - docs/ux/command_diagnostics_contract.md
  - schemas/commands/diagnostic_projection.schema.json

surface_audit_rows:
  - entry_surface: provider_tile_entry
    governing_fixture_ref: fixtures/commands/diagnostic_rows/diagnostic_row_policy_blocked.json
    expected_behavior: disabled_row_retains_badge_ref
    parity_finding_class: parity_complete
  - entry_surface: support_export_receipt
    expected_behavior: badge_export_safe_wording_applied
    parity_finding_class: parity_complete

notes: >
  This case is intentionally policy-limited: the badge remains inspectable, but
  the action is denied with typed reason and remediation. Exports narrow labels
  according to export_safe_wording_class; they do not remove target/origin
  disclosure.
minted_at: "2026-05-07T13:04:00Z"

