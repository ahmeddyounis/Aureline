# yaml-language-server: $schema=../../../schemas/ux/job_row_retention.schema.json
$schema: ../../../schemas/ux/job_row_retention.schema.json
__fixture__:
  name: transport_reconnect
  scenario: >
    Sync transport is reconnecting after a network drop. The
    contextual banner discloses degraded state; the attention
    item is the canonical durable identity. OS notification and
    badge activations land on the same attention item.
  exercised_axes:
    state_class: attention_required
    activity_partition: needs_attention
    canonical_reopen_target_kind: attention_item_exact
    release_trigger_class: user_resolve
    dedupe_key_scheme: subsystem_plus_object_plus_phase
  contract_sections:
    - "Per-row drills"
    - "Drill rules"
record_kind: job_row_retention_packet_record
job_row_retention_schema_version: 1
retention_packet_id: dur:retention:transport_reconnect:needs_attention
corpus_row_id_ref: dur:row:transport_reconnect
canonical_event_id_ref: ux:event:transport:remote-mirror:reconnecting
durable_object_target_ref: obj:transport-session:remote-mirror
job_row_id_ref: ux:job-row:transport:remote-mirror:reconnecting
state_class: attention_required
activity_partition: needs_attention
partition_rule_ref: dur:partition-rule:needs_attention
retention_class: keep_until_user_resolve
retention_window_class: retained_with_unread_until_resolved
dismissibility_class: dismissible_to_history_after_resolution
release_trigger_class: user_resolve
transient_chrome_classes_dismissible_without_clearing_row:
  - os_badge_app_icon
  - os_notification
  - lock_screen_summary
  - status_item
canonical_reopen_target_ref: ux:reopen:transport:remote-mirror:attention
canonical_reopen_target_kind: attention_item_exact
expected_reopen_target_kinds:
  - attention_item_exact
  - durable_job_row_exact
  - activity_center_item
reopen_window_label: >
  The reconnect attention item reopens from contextual banner,
  status item, or OS-level mirror while transport is degraded.
audit_handling_class: full_audit_trail_required
audit_trail_refs:
  - audit:durable_job_created:transport:remote-mirror
  - audit:durable_job_phase_changed:transport:remote-mirror:reconnecting
support_export_inclusion_class: always_included_full_lineage
raw_private_material_excluded: true
stays_reviewable_label: >
  The transport row stays in needs_attention until the user
  acknowledges recovery or dismisses to history; last-known-good
  state and last failure reason remain on the row.
moves_to_partition_on_release: completed
durable_history_invariants:
  preserves_canonical_event_id: true
  preserves_durable_object_target_ref: true
  preserves_durable_history: true
  preserves_audit_trail: true
  preserves_critical_safety_tier: true
  transient_chrome_dismissal_does_not_clear_row: true
policy_context:
  policy_epoch: pe:2026-04-30:01
  trust_state: trusted
  execution_context_id: exec-context:sync_mirror:remote
source_doc_refs:
  - docs/ux/exact_reopen_drill_map.md#row-transport-reconnect
  - docs/ux/degraded_mode_pattern.md
  - artifacts/ux/durable_attention_corpus.yaml
minted_at: "2026-05-01T00:00:00Z"
