# yaml-language-server: $schema=../../../schemas/ux/job_row_retention.schema.json
$schema: ../../../schemas/ux/job_row_retention.schema.json
__fixture__:
  name: transient_dismissal_audit
  scenario: >
    The user dismisses the toast that mirrored a running build.
    The retention contract emits a
    transient_chrome_dismissed_durable_row_preserved audit event
    so the dismissal is auditable; the durable row stays in
    current_work and the canonical event id is preserved.
  contract_sections:
    - "Per-row drills"
    - "Drill rules"
    - "Activity-center partition retention summary"
record_kind: job_row_retention_audit_event_record
job_row_retention_schema_version: 1
audit_event_id: audit:retention:build_run:toast_dismissed:01
audit_event_kind: transient_chrome_dismissed_durable_row_preserved
retention_packet_ref: dur:retention:build_run:current_work
canonical_event_id_ref: ux:event:build:debug-profile:running
from_activity_partition: current_work
to_activity_partition: current_work
transient_chrome_class: toast
preserves_durable_history: true
preserves_canonical_event_id: true
summary_label: Toast dismissed; build row preserved.
occurred_at: "2026-05-01T00:05:12Z"
