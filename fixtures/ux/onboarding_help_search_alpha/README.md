# Onboarding Help Search Alpha Fixtures

These fixtures protect help-search and keymap-bridge projection on the
first-run onboarding wedge. The rows require stable command IDs, keyboard
routes, citation refs, and visible locale fallback when a requested locale falls
back to the source-language pack.

