# Missing-target recovery parity case. Recent-work row is in the
# missing_target state; the Locate, Open without restore, and
# Remove from recents actions all resolve through canonical
# commands rather than free-text "Locate" prose.
__fixture__:
  name: missing_target_recovery_recent_work
  case_category: missing_target_recovery
  scenario: >
    A recent-work row points at a local repo root that the user
    moved on disk. The row renders target_state = missing_target
    before click. Inline-affordance buttons (Locate, Open without
    restore, Remove from recents) resolve through canonical
    commands. The why-unavailable explainer attached to the row
    cites the missing-target reason; the contextual tip points at
    the canonical Open and Restore commands without minting a
    hidden mutation path.

record_kind: guided_entry_parity_case_record
guided_entry_parity_schema_version: 1
case_id: parity:missing_target_recovery:recent_work_local_moved:01
case_category: missing_target_recovery

command_id: cmd:workspace.open_folder
registry_entry_id: registry-entry:workspace.open_folder
coverage_row_ref: coverage:workspace.open_folder
hint_source_ledger_ref: cmd:workspace.open_folder

deployment_profile_refs:
  - individual_local
  - self_hosted
fuw_row_refs:
  - fuw_row:missing_target_reopen.local_repo_root_moved_on_disk

parity_floor_state: meets_floor_with_disclosed_exception
parity_finding_class: parity_complete_with_disclosed_exception
exception_class: deliberately_inline_only
exception_owner_role: recent_work_owner
exception_review_ref: review:recent_work_row:missing_target_inline_only:01

route_audit_rows:
  - surface_route_class: inline_affordance
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
    projection_ref: projection:workspace.open_folder:recent_work_locate
    parity_finding_class: parity_complete_with_disclosed_exception
  - surface_route_class: command_palette
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
    projection_ref: projection:workspace.open_folder:palette_row
    parity_finding_class: parity_complete
  - surface_route_class: context_menu
    route_role_class: contextual
    expected_role_class: contextual
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
    projection_ref: projection:workspace.open_folder:explorer_context_menu
    parity_finding_class: parity_complete
  - surface_route_class: contextual_tip_card
    route_role_class: contextual
    expected_role_class: contextual
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
      - kind: canonical_command_id
        ref: cmd:workspace.restore_from_checkpoint
    projection_ref: projection:workspace.open_folder:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: why_unavailable_explainer
    route_role_class: explain_only
    expected_role_class: explain_only
    source_anchor_refs:
      - kind: why_unavailable_explainer_ref
        ref: reason:workspace.open_folder:execution_context_unavailable
    projection_ref: projection:workspace.open_folder:why_unavailable_explainer
    parity_finding_class: parity_complete
  - surface_route_class: help_search
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:open_folder_overview
    projection_ref: projection:workspace.open_folder:help_search
    parity_finding_class: parity_complete

reuse_surfaces:
  - palette
  - context_menu
  - help_search
  - docs_help
  - onboarding
notes: >
  Inline-only is the declared posture for the recent-work row's
  Locate / Open without restore / Remove from recents buttons.
  Multi-route invocation is still met because palette and context
  menu mirror the canonical Open command.
minted_at: "mono:0005:00:00:00.0000"
