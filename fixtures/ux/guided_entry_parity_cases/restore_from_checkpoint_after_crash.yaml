# Restore parity case. After a crash loop the user reaches
# Restore via the Start Center restore card, the recent-work row
# Restore action, the palette, and the global menu. The crash-loop
# docs anchor and the restore-overview docs anchor mirror the
# canonical command record.
__fixture__:
  name: restore_from_checkpoint_after_crash
  case_category: restore_from_checkpoint
  scenario: >
    Compatible restore after a crash loop. The Start Center Restore
    card is the inline path; the palette, global menu, and recent
    work row Restore action all resolve through
    cmd:workspace.restore_from_checkpoint. The crash-loop docs
    anchor and the restore-overview anchor are linked. Onboarding
    card explains the recoverable-durable-mutation review posture
    without minting a hidden invocation path.

record_kind: guided_entry_parity_case_record
guided_entry_parity_schema_version: 1
case_id: parity:restore_from_checkpoint:after_crash:01
case_category: restore_from_checkpoint

command_id: cmd:workspace.restore_from_checkpoint
registry_entry_id: registry-entry:workspace.restore_from_checkpoint
coverage_row_ref: coverage:workspace.restore_from_checkpoint
hint_source_ledger_ref: cmd:workspace.restore_from_checkpoint

deployment_profile_refs:
  - individual_local
  - self_hosted
  - enterprise_online
fuw_row_refs:
  - fuw_row:restore.compatible_restore_after_crash

parity_floor_state: meets_floor
parity_finding_class: parity_complete
exception_class: none
exception_owner_role: null
exception_review_ref: null

route_audit_rows:
  - surface_route_class: command_palette
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.restore_from_checkpoint
    projection_ref: projection:workspace.restore_from_checkpoint:palette_row
    parity_finding_class: parity_complete
  - surface_route_class: global_application_menu
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.restore_from_checkpoint
    projection_ref: projection:workspace.restore_from_checkpoint:global_menu
    parity_finding_class: parity_complete
  - surface_route_class: inline_affordance
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.restore_from_checkpoint
      - kind: guided_tour_step_anchor
        ref: tour_step:start_center:restore_card:01
    projection_ref: projection:workspace.restore_from_checkpoint:start_center_restore_card
    parity_finding_class: parity_complete
  - surface_route_class: onboarding_card
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.restore_from_checkpoint
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:restore_from_checkpoint_overview
    projection_ref: projection:workspace.restore_from_checkpoint:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: contextual_tip_card
    route_role_class: contextual
    expected_role_class: contextual
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.restore_from_checkpoint
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:crash_loop_and_restore_fidelity
    projection_ref: projection:workspace.restore_from_checkpoint:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: help_search
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.restore_from_checkpoint
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:restore_from_checkpoint_overview
    projection_ref: projection:workspace.restore_from_checkpoint:help_search
    parity_finding_class: parity_complete
  - surface_route_class: docs_help_anchor
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:restore_from_checkpoint_overview
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:crash_loop_and_restore_fidelity
    projection_ref: docs:anchor:workspace:restore_from_checkpoint_overview
    parity_finding_class: parity_complete
  - surface_route_class: cli_help
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: alias_record
        ref: alias:workspace.restore_from_checkpoint:cli_restore
    projection_ref: projection:workspace.restore_from_checkpoint:cli_help
    parity_finding_class: parity_complete

reuse_surfaces:
  - onboarding
  - help_search
  - docs_help
  - palette
  - global_menu
  - support_handoff
notes: >
  Recoverable durable mutation. Onboarding card is mirror not entry;
  the Start Center Restore card and the recent-work row Restore
  action are the inline entry routes.
minted_at: "mono:0004:00:00:00.0000"
