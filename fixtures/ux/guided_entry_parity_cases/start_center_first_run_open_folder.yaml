# Start Center first-run case. The Open zone, palette row, global
# menu row, and first-run onboarding card all resolve through the
# same canonical command and docs anchor.
__fixture__:
  name: start_center_first_run_open_folder
  case_category: start_center_first_run
  scenario: >
    Fresh install, individual_local profile. The user reaches Open
    folder from the Start Center Open zone, the command palette,
    File > Open in the global menu, and the first-run onboarding
    card. All four routes resolve through the canonical
    cmd:workspace.open_folder record. Help search projects the
    same row; the docs anchor is the workspace open-folder
    overview.

record_kind: guided_entry_parity_case_record
guided_entry_parity_schema_version: 1
case_id: parity:start_center_first_run:open_folder:01
case_category: start_center_first_run

command_id: cmd:workspace.open_folder
registry_entry_id: registry-entry:workspace.open_folder
coverage_row_ref: coverage:workspace.open_folder
hint_source_ledger_ref: cmd:workspace.open_folder

deployment_profile_refs:
  - individual_local
  - self_hosted
fuw_row_refs:
  - fuw_row:local_open.first_run_start_center_local_folder

parity_floor_state: meets_floor
parity_finding_class: parity_complete
exception_class: none
exception_owner_role: null
exception_review_ref: null

route_audit_rows:
  - surface_route_class: command_palette
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
    projection_ref: projection:workspace.open_folder:palette_row
    parity_finding_class: parity_complete
  - surface_route_class: global_application_menu
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
    projection_ref: projection:workspace.open_folder:global_menu
    parity_finding_class: parity_complete
  - surface_route_class: inline_affordance
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
    projection_ref: projection:workspace.open_folder:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: onboarding_card
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
      - kind: guided_tour_step_anchor
        ref: tour_step:first_run:open_folder:01
    projection_ref: projection:workspace.open_folder:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: current_shortcut_display
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: current_shortcut_display_ref
        ref: projection:workspace.open_folder:current_shortcut_display
    projection_ref: projection:workspace.open_folder:current_shortcut_display
    parity_finding_class: parity_complete
  - surface_route_class: help_search
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.open_folder
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:open_folder_overview
    projection_ref: projection:workspace.open_folder:help_search
    parity_finding_class: parity_complete
  - surface_route_class: docs_help_anchor
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:open_folder_overview
    projection_ref: docs:anchor:workspace:open_folder_overview
    parity_finding_class: parity_complete

reuse_surfaces:
  - onboarding
  - help_search
  - docs_help
  - palette
  - global_menu
notes: >
  Floor command. No exceptions. Reviewers can read the multi-route
  invocation off the route_audit_rows table directly.
minted_at: "mono:0001:00:00:00.0000"
