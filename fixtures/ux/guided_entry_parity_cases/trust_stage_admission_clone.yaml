# Trust-stage admission parity case. The trust prompt that gates
# clone admission preserves canonical command identity through to
# the why-unavailable explainer and the docs anchor.
__fixture__:
  name: trust_stage_admission_clone
  case_category: trust_stage
  scenario: >
    A clone is admitted only after the workspace trust prompt
    resolves. The trust prompt cites cmd:workspace.clone_repository
    explicitly; the why-unavailable explainer for the
    pending-trust state and the help-search row mirror the same
    record. The migration bridge card is suppressed while trust is
    pending and re-renders against the canonical command record
    once trust is granted.

record_kind: guided_entry_parity_case_record
guided_entry_parity_schema_version: 1
case_id: parity:trust_stage:admission_clone:01
case_category: trust_stage

command_id: cmd:workspace.clone_repository
registry_entry_id: registry-entry:workspace.clone_repository
coverage_row_ref: coverage:workspace.clone_repository
hint_source_ledger_ref: cmd:workspace.clone_repository

deployment_profile_refs:
  - individual_local
  - self_hosted
  - enterprise_online
fuw_row_refs:
  - fuw_row:clone.clone_then_review_remote_repo

parity_floor_state: meets_floor
parity_finding_class: parity_complete
exception_class: none
exception_owner_role: null
exception_review_ref: null

route_audit_rows:
  - surface_route_class: command_palette
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
    projection_ref: projection:workspace.clone_repository:palette_row
    parity_finding_class: parity_complete
  - surface_route_class: global_application_menu
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
    projection_ref: projection:workspace.clone_repository:global_menu
    parity_finding_class: parity_complete
  - surface_route_class: why_unavailable_explainer
    route_role_class: explain_only
    expected_role_class: explain_only
    source_anchor_refs:
      - kind: why_unavailable_explainer_ref
        ref: reason:workspace.clone_repository:network_required
      - kind: why_unavailable_explainer_ref
        ref: reason:workspace.clone_repository:policy_blocks_remote_origin
    projection_ref: projection:workspace.clone_repository:why_unavailable_explainer
    parity_finding_class: parity_complete
  - surface_route_class: contextual_tip_card
    route_role_class: contextual
    expected_role_class: contextual
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_review
    projection_ref: projection:workspace.clone_repository:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: help_search
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_repository_overview
    projection_ref: projection:workspace.clone_repository:help_search
    parity_finding_class: parity_complete
  - surface_route_class: docs_help_anchor
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_repository_overview
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_review
    projection_ref: docs:anchor:workspace:clone_repository_overview
    parity_finding_class: parity_complete
  - surface_route_class: migration_bridge_card
    route_role_class: contextual
    expected_role_class: contextual
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: migration_bridge_ref
        ref: importer_outcome:workspace.clone_repository:from_legacy
    projection_ref: projection:workspace.clone_repository:migration_bridge_card
    parity_finding_class: parity_complete

reuse_surfaces:
  - palette
  - global_menu
  - help_search
  - docs_help
  - migration_bridge
  - support_handoff
notes: >
  Trust prompt is rendered by the trust contract; this case audits
  that the prompt cites the canonical command id rather than a
  per-prompt label. The why-unavailable explainer is the same
  projection ref the help-search row reads.
minted_at: "mono:0006:00:00:00.0000"
