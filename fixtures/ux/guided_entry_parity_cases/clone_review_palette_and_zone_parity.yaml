# Clone repository parity case. Palette row, global menu, Start
# Center Clone zone, contextual tip, onboarding card, help search,
# and docs anchor all resolve through cmd:workspace.clone_repository
# and the clone-repository docs anchor.
__fixture__:
  name: clone_review_palette_and_zone_parity
  case_category: clone_review
  scenario: >
    The user reaches Clone repository through the palette ("Clone
    repository" / "Get code" alias), File > Clone repository in the
    global menu, and the Start Center Clone zone. The clone-review
    docs anchor and the help-search row mirror the same record. The
    contextual tip near remote-repo discovery surfaces uses the
    same projection ref family without minting parallel copy.

record_kind: guided_entry_parity_case_record
guided_entry_parity_schema_version: 1
case_id: parity:clone_review:palette_and_zone:01
case_category: clone_review

command_id: cmd:workspace.clone_repository
registry_entry_id: registry-entry:workspace.clone_repository
coverage_row_ref: coverage:workspace.clone_repository
hint_source_ledger_ref: cmd:workspace.clone_repository

deployment_profile_refs:
  - individual_local
  - self_hosted
  - enterprise_online
fuw_row_refs:
  - fuw_row:clone.clone_then_review_remote_repo

parity_floor_state: meets_floor
parity_finding_class: parity_complete
exception_class: none
exception_owner_role: null
exception_review_ref: null

route_audit_rows:
  - surface_route_class: command_palette
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: alias_record
        ref: alias:workspace.clone_repository:get_code
    projection_ref: projection:workspace.clone_repository:palette_row
    parity_finding_class: parity_complete
  - surface_route_class: global_application_menu
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
    projection_ref: projection:workspace.clone_repository:global_menu
    parity_finding_class: parity_complete
  - surface_route_class: inline_affordance
    route_role_class: entry
    expected_role_class: entry
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: guided_tour_step_anchor
        ref: tour_step:start_center:clone_zone:01
    projection_ref: projection:workspace.clone_repository:start_center_zone
    parity_finding_class: parity_complete
  - surface_route_class: onboarding_card
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_repository_overview
    projection_ref: projection:workspace.clone_repository:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: contextual_tip_card
    route_role_class: contextual
    expected_role_class: contextual
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_review
    projection_ref: projection:workspace.clone_repository:onboarding_hint
    parity_finding_class: parity_complete
  - surface_route_class: help_search
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: canonical_command_id
        ref: cmd:workspace.clone_repository
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_repository_overview
    projection_ref: projection:workspace.clone_repository:help_search
    parity_finding_class: parity_complete
  - surface_route_class: docs_help_anchor
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: docs_help_anchor
        ref: docs:anchor:workspace:clone_repository_overview
    projection_ref: docs:anchor:workspace:clone_repository_overview
    parity_finding_class: parity_complete
  - surface_route_class: cli_help
    route_role_class: mirror
    expected_role_class: mirror
    source_anchor_refs:
      - kind: alias_record
        ref: alias:workspace.clone_repository:cli_clone
    projection_ref: projection:workspace.clone_repository:cli_help
    parity_finding_class: parity_complete

reuse_surfaces:
  - onboarding
  - help_search
  - docs_help
  - palette
  - global_menu
  - migration_bridge
notes: >
  Palette alias (Get code) is recorded on the registry. No alias
  drift across surfaces because every route projects through the
  same registry entry.
minted_at: "mono:0002:00:00:00.0000"
