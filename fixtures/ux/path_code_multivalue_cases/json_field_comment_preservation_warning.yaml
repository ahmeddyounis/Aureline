# yaml-language-server: $schema=../../../schemas/ux/code_backed_field.schema.json
schema_version: 1
record_kind: code_backed_field_record
code_backed_field_id: code_backed_field:settings.search_roots_overrides:001
field_rule_record_ref: field_rule:settings.search_roots_overrides:001
field_row_record_ref: field_row:settings.search_roots_overrides:001
surface_families:
  - settings_form
  - support_export
field_label: Search-roots overrides
code_format_class: json
syntax_state:
  syntax_highlighting_state: highlighting_active
  highlighter_source_class: builtin_highlighter
  syntax_summary: JSON is highlighted by the built-in highlighter; no extension highlighter is required for this format.
schema_hint:
  schema_hint_state: hint_loaded
  unknown_field_handling_class: warn_on_unknown_fields_preserve
  comment_preservation_class: comments_not_supported_by_format
  schema_ref: schema_ref:settings.search_roots_overrides:001
  schema_hint_summary: The settings schema is loaded; the user pasted JSONC-style comments which the JSON format does not support, so a comment-preservation warning is required before apply.
diff_preview:
  diff_preview_state: side_by_side_diff_available
  broad_change_requires_preview: true
  preview_skip_acknowledged: false
  diff_preview_summary: A side-by-side diff between the pasted entry and the canonical JSON output is shown so the user can compare comment-stripped output against the original input before apply.
normalization_report:
  normalization_classes:
    - comments_dropped
    - whitespace_canonicalized
  lossy_normalization_warning_required: true
  raw_entry_preserved_as_ref: true
  raw_entry_ref: raw_entry:settings.search_roots_overrides:001
  normalization_summary: Inline JSONC comments are not supported by the JSON format and would be dropped on canonical write; the raw entry is preserved as a ref so the original commented version can be inspected and converted to JSONC if desired.
presentation_class: canonical_visible_with_raw_inspect
narrative_refs:
  - docs/ux/path_code_and_multivalue_field_contract.md
  - docs/ux/field_rules_contract.md
  - docs/ux/forms_validation_contract.md
emitted_at: "2026-04-30T10:01:00Z"
__fixture__:
  name: json_field_comment_preservation_warning
  scenario: A settings JSON fragment containing JSONC-style comments cannot apply silently; the field surfaces a comment-preservation warning, preserves the raw commented entry as a ref, and offers a side-by-side diff before the user accepts the canonical comment-stripped output.
  exercised_axes:
    - json_format_comment_loss_warning
    - schema_hint_loaded
    - lossy_normalization_warning
    - side_by_side_diff_for_broad_change
