# yaml-language-server: $schema=../../../schemas/ux/path_field.schema.json
schema_version: 1
record_kind: path_field_record
path_field_id: path_field:repair.cache_root_drop:001
field_rule_record_ref: field_rule:repair.cache_path:001
field_row_record_ref: field_row:repair.cache_root_drop:001
surface_families:
  - repair_action_form
  - support_export
field_label: Cache root
path_kind_class: folder_only
basis_resolution:
  path_basis_class: workspace_root
  path_authority_class: remote_host_authority
  basis_resolved: true
  basis_ref: workspace_root:remote_host_workspace:001
  basis_summary: The dropped path was authored on the local client but resolves under the remote workspace host's root, not the local machine.
picker_compatibility_class: picker_supported_remote
drag_drop_compatibility_class: drag_drop_cross_root_requires_review
missing_target:
  missing_target_state_class: target_unknown_until_resolve
  recovery_class: accept_inspect_only_handoff
  blocks_apply: true
  missing_target_summary: The remote workspace host has not yet confirmed the path exists, so apply is blocked until resolution returns or the user accepts inspect-only.
normalization_report:
  normalization_classes:
    - separator_normalized
    - redundant_segment_collapsed
  normalization_warning_required: true
  raw_entry_preserved_as_ref: true
  raw_entry_ref: raw_entry:repair.cache_root_drop:001
  normalization_summary: Backslash separators from the local-client drop were rewritten to forward slashes for the remote host; the raw entry is preserved for inspection.
presentation_class: canonical_visible_with_raw_inspect
path_truth_chip_ref: path_truth_chip:repair.cache_root_drop:001
narrative_refs:
  - docs/ux/path_code_and_multivalue_field_contract.md
  - docs/ux/field_rules_contract.md
  - docs/fs/path_truth_packet.md
emitted_at: "2026-04-30T10:00:00Z"
__fixture__:
  name: local_vs_remote_path_basis
  scenario: A repair flow accepts a path dragged from the local client into a remote workspace host, discloses cross-root authority, blocks apply on missing-target until resolution, and preserves the raw entry through normalization.
  exercised_axes:
    - cross_root_authority_disclosure
    - drag_drop_cross_root_review
    - missing_target_unknown_until_resolve
    - lossy_path_separator_normalization
