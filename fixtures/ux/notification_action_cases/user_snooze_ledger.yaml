# yaml-language-server: $schema=../../../schemas/ux/notification_suppression_ledger.schema.json
$schema: ../../../schemas/ux/notification_suppression_ledger.schema.json
__fixture__:
  name: user_snooze_ledger
  scenario: >
    A user snoozes an OS-notification delivery instead of acting on the
    underlying reviewable item immediately. The ledger entry records a
    delayed disposition, names an explicit resume condition label, and
    preserves the canonical event id plus the durable linkback.
  contract_sections:
    - "Action grammar §2.3 snooze"
    - "Objects and identity"
    - "Suppression history is not deletion"
  exercised_axes:
    suppression_disposition_class: delayed
    suppression_reason: class_snoozed_by_user
    delay_until_condition_class: lock_screen_unlock

record_kind: notification_suppression_ledger_entry_record
notification_suppression_ledger_schema_version: 1
ledger_entry_id: ux:suppression-ledger:entry:snooze_until_unlock:01
canonical_event_id: ux:event:workspace-trust:remote-attach:revalidation:01
event_lineage_id_ref: ux:lineage:workspace-trust:remote-attach:revalidation:01
canonical_object_target_ref: obj:review-sheet:remote-attach:revalidation
source_subsystem: workspace_trust
intended_delivery_surface_class: os_notification
client_scope: desktop_product
suppression_disposition_class: delayed
suppression_reasons:
  - class_snoozed_by_user
active_quiet_hours_modes_at_decision:
  - mode_none
delay_until_condition_class: lock_screen_unlock
coalesced_into_grouped_burst_id_ref: null
coalesced_into_canonical_event_id_ref: null
snooze_resume_condition_label: When unlocked
muted_class_ref: null
preserved_durable_linkback:
  kind: review_sheet
  target_identity_ref: obj:review-sheet:remote-attach:revalidation
  announcement_label: Reopen remote attach review
  preserves_canonical_event_id: true
  suppression_is_not_deletion: true
decision_summary_label: User snoozed OS notification; reopen required to proceed.
policy_context:
  policy_epoch: pe:2026-05-06:01
  trust_state: trusted
  execution_context_id: exec-context:workspace:remote-attach
decided_at: "2026-05-06T00:01:00Z"

