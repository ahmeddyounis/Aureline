{
  "case_id": "imported-reads-as-editable",
  "kind": "narrowing",
  "description": "An imported value that reads as an editable local value floors below the review overlay.",
  "base_row_id": "row:import-mapping:0001",
  "stale_window": false,
  "overrides": {
    "field_state": "editable",
    "renderings[*].rendered_claim": "row_blocked"
  },
  "expected": {
    "effective_claim": "row_blocked",
    "narrowed": true,
    "active_narrowing_reasons": [
      "imported_value_reads_as_editable"
    ]
  }
}
