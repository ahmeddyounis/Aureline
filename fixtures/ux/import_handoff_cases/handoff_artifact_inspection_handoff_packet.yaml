# yaml-language-server: $schema=../../../schemas/ux/import_review.schema.json
__fixture__:
  name: handoff_artifact_inspection_handoff_packet
  scenario: >
    Paired with import_review_handoff_packet_compare_before_restore.
    The handoff_artifact_inspection_record carries
    handoff_inspection_class = bundle_like_handoff_inspection,
    artifact_class = handoff_packet, provenance_label_class =
    first_party_signed (a peer Aureline environment signed the
    packet). Demonstrates §4.2 bundle_like_handoff_inspection class.
  record_kind_exercised: handoff_artifact_inspection_record
  doc_sections:
    - "§4.2 handoff_inspection_class (bundle_like_handoff_inspection)"

record_kind: handoff_artifact_inspection_record
handoff_artifact_inspection_schema_version: 1
inspection_id: handoff-artifact-inspection-handoff-packet-0001
import_review_ref: import-review-handoff-packet-compare-0001
handoff_inspection_class: bundle_like_handoff_inspection
artifact_class: handoff_packet
artifact_descriptor_ref: artifact-descriptor-handoff-packet-signed-0001
provenance_label_class: first_party_signed
provenance_label: "Peer Aureline environment, signed by peer release key"
producer_label: "Aureline 0.0.9 (stable, peer environment)"
redaction_class: ui_string_only
redaction_posture_class: default_redaction_applied
signature_state: signed_verified
checksum_state: checksum_available
raw_body_inspectable: true
redacted_summary_available: true
presentation_label: "Handoff packet · peer-signed"
summary: "Bundle-like handoff packet from a peer environment. Signed; default redaction applied; raw body inspectable."
minted_at: "mono:0000:00:00:00.0000"
