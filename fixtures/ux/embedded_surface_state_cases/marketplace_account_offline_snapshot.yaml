# yaml-language-server: $schema=../../../schemas/ux/embedded_surface_state.schema.json
__fixture__:
  name: marketplace_account_offline_snapshot
  scenario: >
    Marketplace/account surface is available only as an offline snapshot.
    The state stays explicitly non-live and routes to a local inspect/export
    fallback rather than implying live account scope.
  exercised_axes:
    surface_family: embedded_marketplace_or_account
    state_class: offline_snapshot
    mapped_boundary_state: offline_snapshot
    degraded_state_tokens: [Offline]
    fallback_posture_class: external_open_unavailable_offline
    fallback_target_class: local_inspect_or_export
  contract_sections:
    - "Core invariants (frozen)"
    - "Embedded-surface state vocabulary (closed)"

record_kind: embedded_surface_state_record
embedded_surface_state_schema_version: 1
case_id: ux:case:embedded-surface-state:marketplace-account:offline-snapshot
surface_family: embedded_marketplace_or_account
state_class: offline_snapshot
mapped_boundary_state: offline_snapshot
liveness_claim: non_live
state_label:
  visible_label: Offline snapshot
  accessible_name: Offline snapshot of marketplace/account surface (not live)
  summary_label: Offline snapshot only; actions requiring live account access are disabled
degraded_state_tokens:
  - Offline
required_cues:
  - owner_origin_data_boundary_chrome_visible
  - state_label_visible
  - non_live_disclosure_visible
  - snapshot_age_visible
  - offline_notice_visible
  - export_disclaimer_visible
fallback:
  posture_class: external_open_unavailable_offline
  fallback_target_class: local_inspect_or_export
  summary_label: Network is unavailable; inspect or export the offline snapshot locally.
  return_target_label: Aureline desktop / Account panel
  handoff_preserves_object_identity: true
  handoff_preserves_return_path: true
export_posture:
  export_must_preserve_state_class: true
  export_must_preserve_object_identity: true
  export_must_remain_non_live_when_non_live: true
policy_context:
  identity_mode: self_hosted_org
  policy_epoch: epoch.marketplace.account.offline.0003
  trust_state: trusted
  execution_context_id: ctx:marketplace-account:offline-snapshot
redaction_class: metadata_safe_default
minted_at: "2026-05-06T13:35:00Z"

