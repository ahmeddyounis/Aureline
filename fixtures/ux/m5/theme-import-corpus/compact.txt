report: id=shell:m5_theme_import_report:v1:default, rows=5, ecosystems=5
translated=152/214 unresolved=15 blocked=2
every_import_reversible=true no_overclaimed_parity=true unresolved_counts_disclosed=true
VS Code dark theme import [vscode/1.97.0]
  outcome=applied mapping=translated parity=claimed_with_report
  translated=48/48 unresolved=0 syntax_coverage=100%
  rollback=restore_appearance_checkpoint reversible=true
JetBrains Darcula scheme import [jetbrains/2024.3]
  outcome=applied_with_warnings mapping=substituted_fallback parity=partial_claim_with_gaps
  translated=40/52 unresolved=2 syntax_coverage=78%
  rollback=restore_appearance_checkpoint reversible=true
  compatibility_note: Editor scheme colors translated, but two IDE-specific scopes and the gutter accent fell back to disclosed neutral defaults; review before relying on them.
  unresolved: Inline parameter-hint background has no semantic target and is left unresolved rather than guessed.
  unresolved: Breadcrumb background tint is unresolved; the shell keeps its native chrome rather than approximate it.
Zed theme import (rolled back) [zed/0.160.0]
  outcome=rolled_back mapping=blocked_honesty parity=denied_unresolved_or_blocked
  translated=36/44 unresolved=1 syntax_coverage=89%
  rollback=restore_appearance_checkpoint reversible=true
  compatibility_note: This theme used color alone to signal a trust state, which would have hidden a protected cue; Aureline rolled the import back rather than ship a misleading appearance.
  unresolved: The status accent slot could not be resolved without overriding a protected trust cue, so it is left unresolved.
Vim colorscheme import (review required) [vim/0.10.2]
  outcome=review_required mapping=unresolved parity=partial_claim_with_gaps
  translated=22/40 unresolved=8 syntax_coverage=56%
  rollback=reopen_import_review reversible=true
  compatibility_note: A Vim colorscheme covers syntax scopes but not the full IDE chrome; the unresolved chrome slots are listed so they are not silently defaulted.
  unresolved: The statusline palette has no direct chrome target and is left for review.
  unresolved: The tabline palette is unresolved; the shell keeps its native tab chrome pending review.
TextMate tmTheme import (blocked) [textmate/2.0]
  outcome=blocked mapping=unsupported parity=denied_unresolved_or_blocked
  translated=6/30 unresolved=4 syntax_coverage=27%
  rollback=discard_preview reversible=true
  compatibility_note: Only raw editor foreground and background could be read; the rest of the design system has no source slots, so a parity claim would be misleading.
  unresolved: The format carries no semantic token slots, so they cannot be resolved.
  unresolved: Diff tokens are absent from the source and are left unresolved rather than defaulted silently.
