# yaml-language-server: $schema=../../../schemas/ux/presentation_mode_state.schema.json
__fixture__:
  name: presentation_breakaway_and_waypoint_navigation
  scenario: >
    An audience member breaks away from the presenter view; the
    breakaway_event_record carries breakaway_recovery_class =
    manual_rejoin_invite_required and pins a follow_target_ref_after
    _recovery so the audience rejoins through the same collaboration
    follow lane. The audience then navigates to the next waypoint
    via the cited waypoint route — proving the audience and
    presenter share the same waypoint citation lane (the
    tour_waypoint_record record kind on the guided_tour schema is
    referenced for completeness but lives in the companion fixtures).
    Exercises §7 rule 9 (breakaway recovery is explicit and envelope-
    bounded) and §8 acceptance: cited waypoint navigation over the
    same underlying content surfaces.
  exercised_axes:
    record_kinds:
      - breakaway_event_record
    breakaway_recovery_class: manual_rejoin_invite_required
  contract_sections:
    - "§1.3 breakaway_event_record"
    - "§7 rule 9 breakaway recovery"
    - "§8 acceptance mapping (breakaway recovery + cited waypoint navigation)"

records:
  - record_kind: breakaway_event_record
    presentation_mode_state_schema_version: 1
    event_id: "ux:present:session:named_audience_walkthrough:01:breakaway:01"
    session_ref: "ux:present:session:named_audience_walkthrough:01"
    audience_actor_ref: "actor:audience:walkthrough:participant:03"
    breakaway_recovery_class: manual_rejoin_invite_required
    follow_target_ref_after_recovery: "collab:follow:named_audience_walkthrough:01"
    evidence_hooks_reserved:
      - presentation_role_review
    redaction_class: metadata_safe_default
    presentation_label: "Audience member temporarily broke away; rejoin requires invite."
    minted_at: "2026-04-24T10:06:00Z"
