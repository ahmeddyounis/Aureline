schema_version: 1
record_kind: route_class_example
example_id: route_class.unknown.blocked_requires_review

linked_route_class_matrix_ref: artifacts/network/route_classes.yaml#blocked_unknown_requires_review

source_flow:
  flow_class: forwarded_endpoint
  source_schema: schemas/remote/forwarded_endpoint.schema.json
  endpoint_class: browser_preview_route
  visibility_class: visibility_unknown_requires_review

resolution:
  route_exposure_class: blocked_unknown_requires_review
  ttl_class: no_ttl_applicable
  approval_expectation: exception_required
  revocation_path: revocation_not_applicable

decision:
  decision_class: blocked
  policy_source_class: local_default_policy
  policy_epoch_ref: null
  block_reason_class: unknown_visibility_requires_review
  reason_sentence: The route cannot be mapped deterministically; it is denied until reviewed.

support_export_projection:
  required_fields_present:
    - route_exposure_class
    - policy_source_class
  forbidden_raw_fields_excluded:
    - raw_url
    - raw_token

support_export_example:
  route_exposure_class: blocked_unknown_requires_review
  route_handle_ref: null
  policy_source_class: local_default_policy
  policy_epoch_ref: null
