{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/governance/freeze_exception_packet.schema.json",
  "title": "Freeze-exception packet (legacy alias)",
  "description": "Legacy compatibility alias for the canonical protected change-budget exception packet schema. Existing FE packets may keep packet_kind = freeze_exception_packet and FE- ids; new packets should use schemas/governance/exception_packet.schema.json directly.",
  "allOf": [
    {
      "$ref": "exception_packet.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "packet_kind": {
          "const": "freeze_exception_packet"
        },
        "packet_id": {
          "pattern": "^FE-[0-9]{3,4}$"
        }
      },
      "required": ["packet_kind", "packet_id"]
    }
  ]
}
