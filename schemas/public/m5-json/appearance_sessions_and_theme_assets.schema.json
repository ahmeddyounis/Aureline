{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/public/m5-json/appearance_sessions_and_theme_assets.schema.json",
  "title": "Aureline M5 durable artifact: Appearance sessions, theme assets, and design-token packages",
  "description": "Canonical JSON Schema package for the appearance_sessions_and_theme_assets durable M5 artifact family. The record-kind tag, the in-band schema version field, and the primary stable object identity are required; unknown fields are preserved (additionalProperties is true) so the artifact round-trips through export, support, and offline-mirror flows without stripping fields. Published at the lifecycle label recorded in x-aureline-contract.lifecycle_label; see the catalog at artifacts/contracts/m5-json-schema-catalog.json.",
  "type": "object",
  "required": [
    "record_kind",
    "theme_asset_schema_version",
    "package_id"
  ],
  "properties": {
    "record_kind": {
      "type": "string",
      "minLength": 1,
      "description": "Stable record-kind tag; safe to log and export."
    },
    "package_id": {
      "type": "string",
      "minLength": 1,
      "description": "Primary stable object identity for this family."
    },
    "theme_asset_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    },
    "appearance_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    },
    "design_token_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    }
  },
  "additionalProperties": true,
  "x-aureline-contract": {
    "catalog_id": "m5_json_schema_catalog:v1",
    "family_id": "appearance_sessions_and_theme_assets",
    "package_id": "m5.appearance_sessions_and_theme_assets",
    "contract_form": "asset_package_manifest",
    "lifecycle_label": "beta",
    "maturity_lane": "experimental",
    "version_fields": [
      "theme_asset_schema_version",
      "appearance_schema_version",
      "design_token_schema_version"
    ],
    "primary_version_field": "theme_asset_schema_version",
    "unknown_field_policy": "preserve",
    "compatibility_note_ref": "docs/ux/theme_and_visual_asset_contract.md"
  }
}
