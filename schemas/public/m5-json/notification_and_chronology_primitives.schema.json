{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/public/m5-json/notification_and_chronology_primitives.schema.json",
  "title": "Aureline M5 durable artifact: Notification envelopes and chronology primitives",
  "description": "Canonical JSON Schema package for the notification_and_chronology_primitives durable M5 artifact family. The record-kind tag, the in-band schema version field, and the primary stable object identity are required; unknown fields are preserved (additionalProperties is true) so the artifact round-trips through export, support, and offline-mirror flows without stripping fields. Published at the lifecycle label recorded in x-aureline-contract.lifecycle_label; see the catalog at artifacts/contracts/m5-json-schema-catalog.json.",
  "type": "object",
  "required": [
    "record_kind",
    "activity_event_envelope_schema_version",
    "canonical_event_id"
  ],
  "properties": {
    "record_kind": {
      "type": "string",
      "minLength": 1,
      "description": "Stable record-kind tag; safe to log and export."
    },
    "canonical_event_id": {
      "type": "string",
      "minLength": 1,
      "description": "Primary stable object identity for this family."
    },
    "activity_event_envelope_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    },
    "record_state_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    }
  },
  "additionalProperties": true,
  "x-aureline-contract": {
    "catalog_id": "m5_json_schema_catalog:v1",
    "family_id": "notification_and_chronology_primitives",
    "package_id": "m5.notification_and_chronology_primitives",
    "contract_form": "event_envelope_schema",
    "lifecycle_label": "beta",
    "maturity_lane": "experimental",
    "version_fields": [
      "activity_event_envelope_schema_version",
      "record_state_schema_version"
    ],
    "primary_version_field": "activity_event_envelope_schema_version",
    "unknown_field_policy": "preserve",
    "compatibility_note_ref": "docs/ux/attention_activity_taxonomy.md"
  }
}
