{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/public/m5-json/teaching_tour_and_learning_packets.schema.json",
  "title": "Aureline M5 durable artifact: Tour/teaching contracts and learning evidence packets",
  "description": "Canonical JSON Schema package for the teaching_tour_and_learning_packets durable M5 artifact family. The record-kind tag, the in-band schema version field, and the primary stable object identity are required; unknown fields are preserved (additionalProperties is true) so the artifact round-trips through export, support, and offline-mirror flows without stripping fields. Published at the lifecycle label recorded in x-aureline-contract.lifecycle_label; see the catalog at artifacts/contracts/m5-json-schema-catalog.json.",
  "type": "object",
  "required": [
    "record_kind",
    "learning_presentation_packet_schema_version",
    "learning_presentation_packet_id"
  ],
  "properties": {
    "record_kind": {
      "type": "string",
      "minLength": 1,
      "description": "Stable record-kind tag; safe to log and export."
    },
    "learning_presentation_packet_id": {
      "type": "string",
      "minLength": 1,
      "description": "Primary stable object identity for this family."
    },
    "learning_presentation_packet_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    },
    "guided_tour_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    }
  },
  "additionalProperties": true,
  "x-aureline-contract": {
    "catalog_id": "m5_json_schema_catalog:v1",
    "family_id": "teaching_tour_and_learning_packets",
    "package_id": "m5.teaching_tour_and_learning_packets",
    "contract_form": "teaching_content_pack",
    "lifecycle_label": "beta",
    "maturity_lane": "internal",
    "version_fields": [
      "learning_presentation_packet_schema_version",
      "guided_tour_schema_version"
    ],
    "primary_version_field": "learning_presentation_packet_schema_version",
    "unknown_field_policy": "preserve",
    "compatibility_note_ref": "docs/learning/learning_presentation_evidence_packet.md"
  }
}
