{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/public/m5-json/service_optional_api.schema.json",
  "title": "Aureline M5 durable artifact: Optional service API and browser-handoff family",
  "description": "Canonical JSON Schema package for the service_optional_api durable M5 artifact family. The record-kind tag, the in-band schema version field, and the primary stable object identity are required; unknown fields are preserved (additionalProperties is true) so the artifact round-trips through export, support, and offline-mirror flows without stripping fields. Published at the lifecycle label recorded in x-aureline-contract.lifecycle_label; see the catalog at artifacts/contracts/m5-json-schema-catalog.json.",
  "type": "object",
  "required": [
    "record_kind",
    "provider_handoff_schema_version",
    "packet_id"
  ],
  "properties": {
    "record_kind": {
      "type": "string",
      "minLength": 1,
      "description": "Stable record-kind tag; safe to log and export."
    },
    "packet_id": {
      "type": "string",
      "minLength": 1,
      "description": "Primary stable object identity for this family."
    },
    "provider_handoff_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    },
    "packet_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    }
  },
  "additionalProperties": true,
  "x-aureline-contract": {
    "catalog_id": "m5_json_schema_catalog:v1",
    "family_id": "service_optional_api",
    "package_id": "m5.service_optional_api",
    "contract_form": "openapi_family",
    "lifecycle_label": "beta",
    "maturity_lane": "experimental",
    "version_fields": [
      "provider_handoff_schema_version",
      "packet_schema_version"
    ],
    "primary_version_field": "provider_handoff_schema_version",
    "unknown_field_policy": "preserve",
    "compatibility_note_ref": "docs/adr/0010-connected-provider-browser-handoff-approval-ticket.md"
  }
}
