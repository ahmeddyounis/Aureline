{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/public/m5-json/policy_bundles.schema.json",
  "title": "Aureline M5 durable artifact: Policy bundles, caches, and permission prompt events",
  "description": "Canonical JSON Schema package for the policy_bundles durable M5 artifact family. The record-kind tag, the in-band schema version field, and the primary stable object identity are required; unknown fields are preserved (additionalProperties is true) so the artifact round-trips through export, support, and offline-mirror flows without stripping fields. Published at the lifecycle label recorded in x-aureline-contract.lifecycle_label; see the catalog at artifacts/contracts/m5-json-schema-catalog.json.",
  "type": "object",
  "required": [
    "record_kind",
    "admin_policy_schema_version",
    "artifact_id"
  ],
  "properties": {
    "record_kind": {
      "type": "string",
      "minLength": 1,
      "description": "Stable record-kind tag; safe to log and export."
    },
    "artifact_id": {
      "type": "string",
      "minLength": 1,
      "description": "Primary stable object identity for this family."
    },
    "admin_policy_schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "In-band schema version field; readers reject unknown majors."
    }
  },
  "additionalProperties": true,
  "x-aureline-contract": {
    "catalog_id": "m5_json_schema_catalog:v1",
    "family_id": "policy_bundles",
    "package_id": "m5.policy_bundles",
    "contract_form": "json_schema_backed_contract_doc",
    "lifecycle_label": "beta",
    "maturity_lane": "experimental",
    "version_fields": [
      "admin_policy_schema_version"
    ],
    "primary_version_field": "admin_policy_schema_version",
    "unknown_field_policy": "preserve",
    "compatibility_note_ref": "docs/policy/admin_policy_and_bundle_cache_contract.md"
  }
}
