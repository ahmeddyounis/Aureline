{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/commands/command_invocation_session.schema.json",
  "title": "Aureline command invocation-session contract",
  "description": "Canonical entrypoint for invocation-session packets. It tightens the shared command-descriptor packet shape by requiring resolved canonical identity and the alias block that explains how the caller reached that command.",
  "allOf": [
    { "$ref": "command_descriptor.schema.json#/$defs/invocation_session_packet_record" },
    {
      "required": ["canonical_verb", "alias_used"],
      "properties": {
        "canonical_verb": { "$ref": "command_descriptor.schema.json#/$defs/canonical_verb" },
        "alias_used": { "$ref": "command_descriptor.schema.json#/$defs/alias_used_block" }
      }
    }
  ]
}
