{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/commands/command_result.schema.json",
  "title": "Aureline command result packet (alias)",
  "description": "Alias entry point for the canonical command result packet schema. This file exists so reviewers and tooling can refer to a stable 'command_result' name while the underlying packet contract remains frozen in result_packet.schema.json.",
  "$ref": "result_packet.schema.json"
}
