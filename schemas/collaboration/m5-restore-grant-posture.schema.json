{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/collaboration/m5-restore-grant-posture.schema.json",
  "title": "Aureline M5 restore-grant posture",
  "description": "Canonical per-domain descriptor for the control posture a restored collaboration session declares when a participant reconnects from a desktop, companion, or support-follow lane. A session restore is view-only by default and never carries authority forward: this posture names the replay-free transcript class the restore renders under (replay-free render summary, metadata restore summary, text / comment timeline summary, or elevated support / regulatory evidence summary), the restore path the reconnect took, the target context being rejoined, and the restore outcome — restore disclosed view-only, observing view-only, control re-request pending, control re-granted, reopen target required, or replay blocked with no rerun — so a restored session always declares whether the user is observing, requesting control again, or needs to reopen the target from scratch, and no hidden rerun or hidden authority carry-over occurs. The audit-safe attribution names who reconnected or re-requested control so a restore stays attributable. Raw terminal / debug input, prior signals, breakpoint edits, command text, variable bodies, and clipboard contents never cross this boundary — only metadata about the restore posture does, and prior input is never replayed on restore.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "object_id",
    "object_class",
    "collaboration_control_association",
    "restore_transcript_class",
    "restore_path",
    "target_context",
    "restore_outcome",
    "attribution_ref"
  ],
  "properties": {
    "object_id": {
      "$ref": "#/$defs/non_empty_string"
    },
    "object_class": {
      "enum": [
        "restore_grant_posture"
      ]
    },
    "collaboration_control_association": {
      "$ref": "#/$defs/non_empty_string"
    },
    "export_class": {
      "enum": [
        "public_safe",
        "internal_only"
      ]
    },
    "restore_transcript_class": {
      "$ref": "#/$defs/restore_transcript_class"
    },
    "restore_path": {
      "$ref": "#/$defs/non_empty_string"
    },
    "target_context": {
      "$ref": "#/$defs/non_empty_string"
    },
    "live_control_re_requested": {
      "type": "boolean"
    },
    "replay_free_render_summary": {
      "$ref": "#/$defs/non_empty_string"
    },
    "restore_outcome": {
      "$ref": "#/$defs/restore_outcome"
    },
    "attribution_ref": {
      "$ref": "#/$defs/non_empty_string"
    }
  },
  "$defs": {
    "non_empty_string": {
      "type": "string",
      "minLength": 1
    },
    "restore_transcript_class": {
      "enum": [
        "replay_free_render_summary",
        "metadata_restore_summary",
        "text_comment_timeline_summary",
        "elevated_support_evidence_summary"
      ]
    },
    "restore_outcome": {
      "enum": [
        "restore_disclosed_view_only",
        "observing_view_only",
        "control_re_request_pending",
        "control_re_granted",
        "reopen_target_required",
        "replay_blocked_no_rerun"
      ]
    }
  }
}
