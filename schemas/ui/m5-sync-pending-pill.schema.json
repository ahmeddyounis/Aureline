{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/ui/m5-sync-pending-pill.schema.json",
  "title": "Aureline M5 sync-pending pill",
  "description": "Canonical reusable sync-pending pill. Names a work item's local-versus-provider state — synced, local-only draft, queued for publish, publish deferred, publish failed, or conflict held — so a pending publish is never silently dropped or shown as reconciled. Governed by the M5 work-item component matrix in crates/aureline-provider.",
  "type": "object",
  "additionalProperties": false,
  "required": ["work_item_object_id", "local_state"],
  "properties": {
    "work_item_object_id": { "$ref": "#/$defs/non_empty_string" },
    "local_state": { "$ref": "#/$defs/local_state" },
    "pending_change_count": { "type": "integer", "minimum": 0 }
  },
  "$defs": {
    "non_empty_string": { "type": "string", "minLength": 1 },
    "local_state": {
      "enum": [
        "synced_with_provider",
        "local_only_draft",
        "queued_for_publish",
        "publish_deferred",
        "publish_failed",
        "conflict_held"
      ]
    }
  }
}
