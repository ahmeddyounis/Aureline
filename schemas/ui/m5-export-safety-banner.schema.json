{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/ui/m5-export-safety-banner.schema.json",
  "title": "Aureline M5 export-safety banner component",
  "description": "Names what an export will and will not reveal \u2014 raw secret excluded, metadata only, handle reference only, redacted share, endpoints masked, or export blocked \u2014 alongside the reveal posture it honours, so no support or export flow ever implies a raw secret is export-safe. Canonical per-component contract frozen by the M5 credential component matrix (schemas/ui/m5-credential-component-matrix.schema.json); downstream M5 rows point at this schema instead of restating credential UI truth by hand. Raw secret values, tokens, passphrases, and private endpoints never cross this boundary.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "component_family",
    "reveal_posture",
    "export_safety_class",
    "degraded_state",
    "required_labels"
  ],
  "properties": {
    "component_family": {
      "const": "export_safety_banner"
    },
    "reveal_posture": {
      "enum": [
        "handle_only",
        "masked_last_four",
        "reveal_on_demand",
        "clipboard_scoped",
        "never_revealed",
        "policy_blocked_reveal"
      ]
    },
    "export_safety_class": {
      "enum": [
        "raw_secret_excluded",
        "metadata_only",
        "handle_reference_only",
        "redacted_share",
        "endpoints_masked",
        "export_blocked"
      ]
    },
    "degraded_state": {
      "enum": [
        "fully_available",
        "limited_capability",
        "stale_needs_reauth",
        "offline_cached",
        "policy_blocked",
        "unavailable"
      ]
    },
    "required_labels": {
      "type": "array",
      "minItems": 3,
      "uniqueItems": true,
      "items": {
        "enum": [
          "identity",
          "state",
          "keyboard_route",
          "storage_and_reveal_posture",
          "identity_and_delegation",
          "expiry_and_export_boundary"
        ]
      }
    }
  }
}
