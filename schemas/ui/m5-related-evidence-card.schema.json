{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/ui/m5-related-evidence-card.schema.json",
  "title": "Aureline M5 related-evidence card",
  "description": "Canonical reusable related-evidence card. Names the provenance of linked evidence — a test result, CI check, review thread, linked change, attached artifact, or external reference — so evidence never appears without disclosing what it is. Governed by the M5 work-item component matrix in crates/aureline-provider.",
  "type": "object",
  "additionalProperties": false,
  "required": ["work_item_object_id", "evidence_kind", "label"],
  "properties": {
    "work_item_object_id": { "$ref": "#/$defs/non_empty_string" },
    "evidence_kind": { "$ref": "#/$defs/evidence_kind" },
    "label": { "$ref": "#/$defs/non_empty_string" },
    "evidence_reference": { "$ref": "#/$defs/non_empty_string" }
  },
  "$defs": {
    "non_empty_string": { "type": "string", "minLength": 1 },
    "evidence_kind": {
      "enum": [
        "test_result",
        "ci_check",
        "review_thread",
        "linked_change",
        "attached_artifact",
        "external_reference"
      ]
    }
  }
}
