{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/config/setting_definition.schema.json",
  "title": "Aureline config setting-definition row",
  "description": "Compatibility entrypoint for configuration consumers. The settings crate is the schema of record; this schema delegates to the canonical settings definition boundary.",
  "$ref": "../settings/setting_definition.schema.json"
}
