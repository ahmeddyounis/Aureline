{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/config/setting_write_intent.schema.json",
  "title": "Aureline config setting write-intent packet (compatibility entry)",
  "description": "Compatibility entrypoint for configuration consumers. The settings crate is the schema of record; this schema delegates to the canonical settings write-intent and change-preview packet.",
  "$ref": "../settings/write_intent.schema.json"
}
