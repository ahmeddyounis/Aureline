{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/ai/ai-provider-model-registry.schema.json",
  "title": "Aureline AI provider, model, pack, and tool registry",
  "description": "Stable schema alias for the provider/model registry packet owned by aureline_ai::registry. The packet binds providers, models, prompt packs, tool-schema packs, local-model packs, external-tool/MCP entries, route policies, claimed surfaces, and consumer projections into one metadata-only source of truth.",
  "$ref": "provider_model_registry.schema.json"
}
