{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/providers/offline_handoff_packet.schema.json",
  "title": "Aureline provider offline handoff packet beta entry point",
  "description": "Provider-facing schema entry point for work-item offline-handoff packets. The canonical packet vocabulary lives in schemas/work_items/offline_handoff_packet.schema.json; provider beta records consume that vocabulary to keep local capture, publish-later replay, callback-confirmed acceptance, typed rejection, retry routes, export routes, redaction manifests, and durable publish targets distinct.",
  "allOf": [
    { "$ref": "../work_items/offline_handoff_packet.schema.json" }
  ]
}

