{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aureline.dev/schemas/provenance/m5-qualification-descriptor.schema.json",
  "title": "Aureline M5 qualification / support-class descriptor object",
  "description": "The shared qualification descriptor object: the frozen support-class vocabulary, its badge family, explanation drawer, first consumer, and the proof packet that keeps it current. The matrix narrows a claimed class down this ladder when evidence thins out and floors a blocked consumer at unavailable.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "family",
    "descriptor_object_id",
    "descriptor_label",
    "badge_family",
    "value_tokens",
    "badge_tokens",
    "explanation_drawer_message_id",
    "owner_role",
    "first_consumer",
    "schema_ref",
    "proof_packet_ref",
    "proof_freshness",
    "detail_message_id"
  ],
  "properties": {
    "family": { "const": "qualification" },
    "descriptor_object_id": { "$ref": "#/$defs/non_empty_string" },
    "descriptor_label": { "$ref": "#/$defs/non_empty_string" },
    "badge_family": { "const": "qualification_badge" },
    "value_tokens": {
      "const": ["stable", "beta", "preview", "experimental", "deprecated", "unavailable"]
    },
    "badge_tokens": {
      "const": ["stable", "beta", "preview", "experimental", "deprecated", "unavailable"]
    },
    "explanation_drawer_message_id": { "$ref": "#/$defs/descriptor_message_id" },
    "owner_role": { "$ref": "#/$defs/non_empty_string" },
    "first_consumer": { "const": "release_center" },
    "schema_ref": { "$ref": "#/$defs/non_empty_string" },
    "proof_packet_ref": { "$ref": "#/$defs/non_empty_string" },
    "proof_freshness": { "enum": ["current", "stale", "expired", "missing"] },
    "detail_message_id": { "$ref": "#/$defs/descriptor_message_id" }
  },
  "$defs": {
    "non_empty_string": { "type": "string", "minLength": 1 },
    "descriptor_message_id": { "type": "string", "pattern": "^public_truth_descriptor\\." }
  }
}
