<!-- Generated localized help asset. Source of truth: aureline_i18n::translated_help_packs. -->
# Restore from a checkpoint

> Locale: en-US · Asset: `asset:recovery:restore-checkpoint` · Source revision: `help-source-rev:restore-checkpoint:2026.05.18-01`

Restore your workspace to a previous checkpoint when local state is damaged.

- Restore from a checkpoint — command `cmd:workspace.restore_from_checkpoint` (`keyboard:path:workspace.restore_from_checkpoint`).
- If recovery does not complete, open the recovery runbook — command `cmd:support.open_recovery_runbook`.

Scope: `scope:recovery`.

Citations: `citation:recovery:restore-checkpoint:source`, `citation:recovery:restore-checkpoint:escalation`.

**Open in source language** — `cmd:help.open_in_source_language` (`keyboard:path:help.open_in_source_language`).
